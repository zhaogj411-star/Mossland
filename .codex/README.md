# Codex 配置

本目录保存仓库级 Codex 配置。

- `hooks.json` 把 Codex 生命周期事件连接到 `agent-code/scripts/codex-hook.sh`。

修改 hook 行为前，先读 `docs/memory/codex-hooks.md`。
新会话恢复从 `AGENTS.md` 和 `docs/agent-start.md` 开始。
显式验证入口位于 `agent-code/scripts/agent/`。
