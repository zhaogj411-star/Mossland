# 当前上下文

本文件是新 Codex 会话的短活跃上下文。保持简洁。

## 当前工作

- 2026-06-20 UTC 已修复 `mossland-codec-no-rvq` 学不到 task/source mapping 的关键 bug：此前 `MosslandCodecTrainingWrapper.training_step()` 只有 `model.has_quantizer` 时才从 `src_representation` 计算 `latent_override`；no-RVQ 时 `latent_override=None`，随后 `_consistency_loss(target_representation, latent_override=None, ...)` 把 `target_representation` 作为 `model.forward(latents, x, ...)` 的第一个参数传入，而 `MosslandCodec.forward()` 在 `latent_override is None` 且 `latents.shape == x.shape` 时会 `self.encoder(latents)`，等价于训练 target autoencoding，不是用 src latent 学 source->target。现在 no-RVQ 分支显式 `latent_override = self.model.encoder(src_representation)`，与 demo callback 的 `model.encode(src[i])` 和 RVQ 分支的 `quantize_representation(src_representation)` 对齐。新增测试 `test_mossland_no_rvq_training_uses_source_latent_override` 覆盖该路径；单卡 1-step no-RVQ smoke 通过。
- 2026-06-20 UTC 对比成功小模型 run `logs/mossland-codec-rvq/runs/2026-06-20_14-05-33` 与 500M run `logs/mossland-codec/runs/2026-06-20_15-23-51`：二者不是只差参数量。相同点是当前 `scripts.mossland_codec.models.MosslandCodec`、stereo、32x1024 RVQ、`quantizer_codebook_dim=null`、`rvq_latent_train_prob=0.25`、`rvq_detach_encoder=false`、5 个 task、bf16 trainer。主要差异：成功 run 用 `MosslandTaskPTDataset` 固化 34304-sample PT 小数据、8 卡、每卡 batch64、global batch 约512、warmup10、schedule/consistency 800k、小模型约57.66M；500M run 用在线 `MosslandTaskDataset(PreparedSeparationDataset)`、sample_size112500、8节点*8卡、每卡 batch1、global batch 约64、task weight reconstruct=6、warmup1000、schedule/consistency 500k、模型约496.94M、`model.mixed_precision=false`、grad clip 1。因此若要做严格 ablation，应先用 500M 结构对齐成功 run 的 PT 数据、样本长度、batch 和 schedule，或只改小 run 的模型规格。
- 2026-06-20 UTC 用户更正旧参考路径应为 `scripts/mossland_codec_old/`，不是 `scripts/codicodec/`。旧 `MosslandCodecTransformer` 的 task/sigma conditioning：`sigma_left/right` 拼成 batch 后做 `log(sigma)/4 -> PositionalEmbedding`，再与 `task_embedding(task_id)` 相加并过 `emb_proj` 得 `time_emb`；`time_emb` 同时进入 decoder frontend `GroupNorm` 调制、`gain_decoder`、`Transformer_Diffusion` 每层 `AdaptiveNorm`，而 sigma 还额外进入 EDM `get_c()` 的 `c_in/c_skip/c_out` 缩放。因此旧版 task_id 也是加性 embedding 路径，不是独立强路由；sigma 路径天然更强。
- 2026-06-20 UTC 对 `logs/mossland-codec-no-rvq/runs/2026-06-20_13-54-25/checkpoints/2-1500.ckpt` 做 task_id 实际推导：取 demo `1501_0_separate_vocals_raw_continuous_src_target_generated.mp3` 的 src 段，同一 latent、同一 decode seed 下分别用 5 个 task_id decode，输出保存在 `analysis/task_id_probe_no_rvq_1500/`。结论：task_id 不是完全无效，但影响很弱；`reconstruct` vs `super_resolution` 输出 SNR 约 `55dB`、相对 RMS diff 约 `0.18%`，`separate_vocals` vs 其它任务多在 `26-29dB`、相对 RMS diff 约 `3.6%-4.6%`。checkpoint 中 task embedding 存在，但 ResBlock 条件注入 `proj_emb` 多数仍是 `1e-3` RMS 量级，1500 step 时 task 条件还很弱。
- 2026-06-20 UTC `bash/start_train_multinode.sh` 已改为无 `PET_*` 环境变量时默认单机 8 卡启动：`PET_NPROC_PER_NODE=8`、`PET_NNODES=1`、`PET_NODE_RANK=0`、`MASTER_ADDR=127.0.0.1`、`MASTER_PORT=29500`。平台变量存在时仍按 `PET_MASTER_ADDR/PET_MASTER_PORT/PET_NPROC_PER_NODE/PET_NNODES/PET_NODE_RANK` 映射 Lightning 多节点参数。验证：`DRY_RUN=1 bash bash/start_train_multinode.sh` 输出 `trainer.devices=8 trainer.num_nodes=1`，不再因缺 `PET_NPROC_PER_NODE` 退出。
- 2026-06-20 UTC 用户澄清约 500M balanced 改动应落在正式 `scripts/configs/experiment/mossland-codec.yaml`，不是 `mossland-codec-rvq.yaml`。当前已把 `mossland-codec-rvq.yaml` 恢复为 Music2Latent 小规格 RVQ 版（`base_channels=64`、`bottleneck_base_channels=512`、`cond_channels=256`、`heads=4`、`layers_list=[2,2,2,2,2]`、`layers_list_encoder=[1,1,1,1,1]`，总参数 `57.66M`）。正式 `mossland-codec.yaml` 已改为约 500M balanced 结构：`base_channels=128`、`bottleneck_base_channels=1024`、`cond_channels=512`、`heads=8`、`layers_list=[3,4,5,5,4]`、`layers_list_encoder=[2,3,4,4,3]`、`num_bottleneck_layers=9`，总参数 `496.94M`。保持 `hop=512`、`freq_downsample_list=[1,0,0,0]`、`bottleneck_channels=64` 不变，所以 latent 压缩率不变；正式配置 shape 检查为 representation `(1,4,1024,64)`、continuous/discrete `(1,64,8)`、codes `(1,8,8)` 且 finite。
- 2026-06-20 UTC 已按用户要求把 `scripts/configs/experiment/mossland-codec-no-rvq.yaml` 改成 Music2Latent 小规格 + 多任务 no-RVQ 对照：训练超参/宽度对齐 `music2latent.yaml`（batch16、warmup10、base64、bottleneck_base512、heads4、cond256、34304-sample crop、bf16），保留 5 个 Mossland task embedding 和多任务 `src/target/task_id` wrapper。由于要保留 `mono_to_stereo` 与 separation，样本是 stereo，模型 `data_channels=4`，这是相对 Music2Latent mono `data_channels=2` 的必要差异。已从本机 `/inspire/sj-ssd3/project/embodied-multimodality/public/Sonata/data/source_seperation/NETEASE_SPIDER/index.list` 预生成 100 条多任务 `.pt` 到 `tmp/mossland_codec_no_rvq_music2latent_spec_pt/`，5 个任务各 20 条，每条 `src/target=(2,34304)`。配置直接读取 `MosslandTaskPTDataset`，`length=160000`。单卡 1-step smoke 通过：`python -m scripts.train experiment=mossland-codec-no-rvq trainer.devices=1 trainer.strategy=auto data.num_workers=0 trainer.max_steps=1 callbacks.demo_callback.demo_every=1000000 checkpoint_every_n_train_steps=1000000`，`loss/total=0.0702`。
- 2026-06-20 UTC `scripts/configs/experiment/music2latent.yaml` 当前指向本机 `tmp/music2latent_official_pt_10k/files.list`；该目录已有 10000 个预处理 `.pt`，用户说明此前手动把清单缩到 10 条，本轮已直接把清单重写为前 1000 条 `000000.pt` 到 `000999.pt`，没有重新预处理音频。Mossland codec 两个配置已从旧机器 `/inspire/qb-ilm2/...` 路径切到本机存在的 `/inspire/sj-ssd3/project/embodied-multimodality/public/Sonata/data/source_seperation/NETEASE_SPIDER/index.list`；`mossland-codec.yaml` 的旧机 `resume_from_ckpt` 已置为 `null`。
- 2026-06-19 UTC 正式 run `logs/mossland-codec/runs/2026-06-19_18-02-29` 在 global step 1356 崩溃：4 个 DDP rank 都在 `MosslandCodecTrainingWrapper.training_step()` 的 `_assert_finite("predicted")` 处失败，`predicted` shape `(1,4,1024,1752)` 全非有限，但各 rank 的 `paths` 不同；此前 `src/target_audio/src_representation/target_representation` finite 检查已通过。TensorBoard 最后写到 step 1349：`loss/total=0.0379`、`grad_norm/before_clip=0.063`，未见已记录的持续 grad 爆炸；但 `rvq/hidden_recon_loss` 已多次尖峰，step 509 达 `315`、step 1349 达 `284`，`rvq/commitment_loss` step 1349 为 `4.04`。配置为 0.5B、4 卡 bf16、每卡 batch1、`lr=1e-4`、warmup100、`gradient_clip_val=1`、`fail_on_nonfinite_grad=false`。结论：不是单个坏 mp3 直接导致输入非有限，更可能是 1350-1356 间某个未写入 TB 的 backward/optimizer step 产生非有限梯度或参数，随后下一次 forward 全 NaN；下一次复现应先开 `wrapper.fail_on_nonfinite_grad=true` 或加 forward 内部 finite probe 定位首个坏层/参数。
- 2026-06-19 UTC 对 `mossland_codec_debug/runs/2026-06-19_17-25-13` 的 `last.ckpt` 导出音频做底噪/失真排查：训练集推理不是单纯高频 hiss，而是 decode 尚未贴住 target。例 `08_super_resolution` 的 `src` 对 `target` SNR 约 `52.38dB`，但 continuous/discrete 生成只有约 `12.8~13.0dB`；`00/05 reconstruct` 也只有约 `9.9/12.1dB`。多步 decode probe 对 `08_super_resolution` 从 1/2/4/8 step 反而 `11.9 -> 10.9 -> 9.9 -> 8.3dB` 变差；临时降低 inference `sigma_max` 只有轻微改善（`sigma_max=2` 约 `13.0dB`）。结论：不是导出脚本问题，也不是 1-step 少导致；该 checkpoint 虽然 consistency loss 已低，但还没有真正把训练集 waveform decode 干净，`rvq/hidden_recon_loss` 最新约 `39` 也说明 discrete hidden 没贴稳。
- 2026-06-19 UTC 已按用户要求用 `logs/mossland_codec_debug/runs/2026-06-19_17-25-13/checkpoints/last.ckpt`（raw model，global_step 4000，demo 配置 use_ema=false）对 debug 训练集 `tmp/mossland_codec_debug_pt/files.list` 的 10 条物理样本做推理导出。输出目录：`logs/mossland_codec_debug/runs/2026-06-19_17-25-13/trainset_inference_last_raw/`；每条样本在 `items/<idx>_<task>/` 下保存 `src.wav`、`target.wav`、`discrete8.wav`、`discrete16.wav`、`discrete32.wav`、`continuous.wav`，根目录另有拼接 wav，顺序为 `src -> target -> discrete8 -> discrete16 -> discrete32 -> continuous`。验证：共 70 个 wav，manifest 10 条，每条拼接 wav 为 44.1kHz stereo、584325 frames。
- 2026-06-19 UTC 已检查 `logs/mossland_codec_debug/runs/2026-06-19_17-25-13` 的 Mossland codec debug RVQ：checkpoint 对比显示 codebook EMA 状态确实在更新，7000->8000->9000 step 的 layer0/layer31 `model.quantizer.vq.layers.*._codebook.{embed,cluster_size}` RMS delta 非零。该 run 的历史 `.hydra/config.yaml` 为 `quantizer_codebook_dim=16`、`bottleneck_base_channels=512`，不是当前 YAML 的 `quantizer_codebook_dim=null`。TensorBoard 中 `loss/total == loss/consistency` 且 `loss/rvq` 全程为 0；`loss/total` 从 step0 `0.0353` 降到 step8871 `0.000933`，但 `rvq/commitment_loss` 从窗口 0-100 均值 `0.0036` 升到 7900-8100 均值 `10.53`，`rvq/hidden_recon_loss` 从 `0.594` 升到 `204.6`。8001 demo 文件内部顺序为 `src -> target -> discrete8 -> discrete16 -> discrete32 -> continuous`。用 `checkpoints/3-8000.ckpt` 对该 demo MP3 里的 `src` 段实际推理，输出在 `analysis/8001_ckpt8000_src_probe/`；对齐后输入为 `[1,4,1024,168]`，eval-mode `hidden_recon_loss` 为 `84.74/84.41/83.91`（8/16/32 个 quantizer），train-mode 每个 rate fresh reload 的 `commitment_loss` 为 `0.413/0.123/0.142`、`hidden_recon_loss` 为 `80.15/90.71/84.42`。同一 src 重建相对 target SNR：continuous `16.32dB`，discrete8/16/32 为 `16.21/16.18/16.18dB`；continuous 与 discrete 输出之间 SNR 约 `24.6-25.3dB`。解释：RVQ aux 权重全为 0，commit/hidden recon 只是监控；10 条 overfit 下 decoder/consistency 目标可让 waveform 听起来较好，但不会强制 quantized hidden 贴近 encoder hidden，不能把 commit loss 上升解读为 codebook 没更新。
- 2026-06-19 UTC 新增正式无 RVQ 对照配置 `scripts/configs/experiment/mossland-codec-no-rvq.yaml`，命令 `python -m scripts.train experiment=mossland-codec-no-rvq`。该配置沿用 `mossland-codec.yaml` 的 0.5B stereo 多任务数据和 consistency 训练口径，但设置 `model.quantizer_num_quantizers=0`、`wrapper.rvq_latent_train_prob=0.0`、`wrapper.train_n_quantizers_choices=null`、`callbacks.demo_callback.demo_n_quantizers_choices=[]`，demo 只输出 `src -> target -> continuous`。`resume_from_ckpt=null`，避免无 RVQ 模型误加载带 quantizer state 的 RVQ checkpoint。
- 2026-06-19 UTC 最新 Mossland codec 包命名已切到 underscore：当前训练代码在 `scripts/mossland_codec/`，旧 hyphen 实现保存在 `scripts/mossland_codec_old/`，不要再新建或引用 `scripts/mossland-codec/` 作为当前包。当前公开模型类是 `scripts.mossland_codec.models.MosslandCodec`，wrapper 是 `scripts.mossland_codec.wrapper.MosslandCodecTrainingWrapper`，多任务 payload schema 是 `{"src","target","task_id"}`。新增 debug 数据路径：`scripts.mossland_codec.tasks.MosslandTaskPTDataset` 直接读取预处理好的 task `.pt`；`python -m scripts.mossland_codec.prepare_debug_pt --num-samples 10 --overwrite` 默认会从本机 prepared stem 根 `/inspire/sj-ssd3/project/embodied-multimodality/public/Sonata/data/source_seperation/NETEASE_SPIDER` 固化 `reconstruct/separate_vocals/separate_accompaniment/super_resolution/mono_to_stereo` 样本到 `tmp/mossland_codec_debug_pt/`。新增 debug 配置 `scripts/configs/experiment/mossland_codec_debug.yaml`，命令 `python -m scripts.train experiment=mossland_codec_debug`；该配置参考 `music2latent.yaml` 的小模型/pt-data debug 口径，使用 `base_channels=64`、`data_channels=4`、32 层 RVQ、训练/demo 码率 `[8,16,32]`、`train_batch_size=16`。为避免 DDP 开局在 step 1 跑 demo 导致其它 rank 等待，`MosslandCodecTrainingCallback` 支持 `demo_start_step`，debug 默认 `demo_num=1`、`demo_every=100`、`demo_start_step=100`、`use_ema=false`。
- 2026-06-19 UTC 当前 `scripts/mossland_codec.models.MosslandCodec` 已补显式 task embedding：`task_names` 默认 5 个主任务，配置中显式写出；模型用 `sigma_embedding + task_embedding(task_idx)` 后过 `emb_proj` 得到 `time_emb`，并让 `scale_inp/scale_out` 使用同一个 task-conditioned embedding。`MosslandCodecTrainingWrapper._consistency_loss()` 会把 batch 内 `task.task_id` 传进模型；demo decode 也按每个样本的 `task_id` 传入，避免 separation 不同目标只靠 `src/target` 隐式区分。
- 2026-06-19 UTC `scripts/configs/experiment/music2latent_localattention.yaml` 已按用户新定义从 NATTEN 全卷积替换改为 SAME temporal-only：模型 target 现在是 `scripts.music2latent.models.Music2Latent_sametemporal`。实现新增 `SameTemporalOp1d` / `SameTemporalResBlock1d`，直接使用 `scripts/same/transformer.py` 的 `TransformerBlock` 和 `self_attention_flash_sliding_window`；只替换 encoder/decoder bottleneck 中有时间局部计算的 `ResBlock(use_2d=False)`，`Conv1d(kernel=1)` channel projection 保持线性实现，原版 2D `Conv2d(kernel=3)`、频率 `(5,1)` down/up、denoising U-Net 主干、wrapper/loss/demo 语义也保持不变。默认 `same_sliding_window=[1,1]`，不走 full attention，也不依赖 NATTEN。
- 2026-06-19 UTC `scripts/music2latent/wrapper.py` 已按用户要求把 Music2Latent RVQ 从“纯 sidecar”改为 batch 内逐样本 mixed latent 训练：`rvq_latent_train_prob` 现在控制每个样本用 discrete latent 还是 continuous latent 作为 denoiser conditioning，`latent/source_discrete` 记录本 batch discrete 比例；`rvq_detach_encoder=false` 会传入 `quantize_representation()`，使被选中的 discrete consistency loss 通过 RVQ straight-through 路径回到 encoder hidden。正式 `music2latent_rvq.yaml` 和 `music2latent_rvq_0p5b_stereo.yaml` 默认改为 `rvq_latent_train_prob=0.25`、`rvq_detach_encoder=false`，仍使用训练/demo 码率 `[8,16,32]`，RVQ aux loss 权重保持 0。
- 2026-06-19 UTC 用户暂停继续追 `music2latent_axiallocal` / “卷积全替换 attention”方向，要求直接研究原版 `scripts/music2latent.models.Music2Latent` 的可改进点。当前形状/参数审计：默认 2 秒输入 representation `[1,2,1024,168]`，latent `[1,64,21]`，decoder pyramid 为 `[64,1024,168]`、`[128,256,168]`、`[256,128,84]`、`[256,64,42]`、`[256,32,21]`，总参数约 `57.66M`，encoder/latent decoder/denoiser down/denoiser up 分别约 `16.26M/16.76M/11.85M/11.85M`。初步建议优先保留原版 “1D latent + multi-scale 2D pyramid + 2D denoiser” 主干，优先改进 latent bottleneck/多尺度 conditioning、长音频 overlap encode/decode、STFT/损失辅助目标和训练稳定性；不要优先继续加重高分辨率 denoiser attention。
- 2026-06-19 UTC 新增 `scripts/configs/experiment/music2latent_rvq.yaml`，基于 `music2latent.yaml` 写正式 EMA RVQ 版本：`quantizer_num_quantizers=32`、`quantizer_codebook_size=1024`、`quantizer_codebook_dim=null`、`quantizer_dropout=1.0`，`train_n_quantizers_choices=[8,16,32]`，demo `demo_n_quantizers_choices=[8,16,32]`。当前 demo callback 已会把 `source -> discrete8 -> discrete16 -> discrete32 -> continuous` 拼到同一个 mp3 文件。验证：Hydra compose/instantiate 显示 `quantizer_num_quantizers=32`、`quantizer_dropout=1.0`、demo rates `[8,16,32]`；独立 `ResidualVectorQuantize` 前向对 `n_quantizers=8/16/32` 输出 codes 形状 `[16,8,21]`、`[16,16,21]`、`[16,32,21]`。
- 2026-06-19 UTC 新增 `scripts/configs/experiment/music2latent_rvq_0p5b_stereo.yaml`，基于 `music2latent_rvq.yaml` 的 0.5B 双声道 RVQ 配置。模型为 `data_channels=4`、`base_channels=192`、`bottleneck_base_channels=1536`、`bottleneck_channels=64`、`cond_channels=768`、`heads=8`；`bottleneck_channels` 必须保持 64，不能为扩参数量而改变 latent 维度/压缩率。RVQ 为 32 个 1024-size codebook，`quantizer_codebook_dim=16`、`quantizer_dropout=1.0`，训练/demo 码率 `[8,16,32]`。训练 batch 设为每卡 `1`，`trainer.accumulate_grad_batches=16`。正式数据现在直接用 `scripts.data.datasets.SampleDataset` 从 `/inspire/qb-ilm2/project/embodied-multimodality/public/zhaoguojie/data/NETEASE_SPIDER` 随机裁 8 秒 stereo raw audio：`sample_size=352800`、`num_channels=2`、`length=10000000`、`max_duration_seconds=300`，索引文件为数据根下 `index_audio.list`。
- 2026-06-19 UTC 新架构讨论：用户明确不要求把 Music2Latent 的每个 Conv 机械替成 NATTEN/attention，而是希望保留 Music2Latent 的 STFT 表示、consistency wrapper/loss、pyramid latent 注入和 U-Net skip 语义，同时借鉴 Stable Audio / SAME 的更新架构，尤其是用 `TransformerResamplingBlock` 风格重新设计下采样和低速率计算模块。当前建议方向是做“Music2Latent + SAME-style patch/grid resampling”混合架构：不要在高分辨率 `[B,64,1024,T]` 上铺 Transformer，也不要把整帧频谱直接压成单个 1D token；应先把频率维切成较小 band/patch grid token，在较低 token rate 上用局部 SAME resampling 做时间下采样和计算，再投回 Music2Latent 的多尺度 2D pyramid。Stable Audio 的变长支持主要靠 padding 与 chunked/overlap encode/decode；训练显存仍随 crop 长度增长，不能承诺训练时任意增加 T 不涨显存。
- 2026-06-19 UTC SAME chunking 结论：`scripts/same/autoencoders.py` 的 `TransformerResamplingBlock` 可学习用于 1D 时间 resampling，做法是把 frequency patch/grid 维临时 fold 到 batch 后沿时间调用 SAME block；`scripts/same/transformer.py` 的 `_sliding_window_chunked_sdpa()` 按 query chunk 做局部 SDPA，可降低 attention score 矩阵峰值，但常规 PyTorch 线性层仍会为整条序列保存 Q/K/V activation，所以 chunking 不会让训练显存与 T 无关。2D 不能简单把 `[F,T]` flatten 成一条长序列；正式方向应使用 axial 1D chunking（time 和 freq 分轴）或真正 2D query-tile local attention，使每个 query tile 只看局部 `(kF,kT)` 邻域，并保留 pad/crop 变长口径。
- 2026-06-19 UTC H200 4 卡运行 `music2latent_axiallocal`、每卡 batch16 在第 0 step OOM，单进程接近 138GB。根因不是 window 太大，而是当前 axial adapter 在高分辨率 `[B,C,F,T]` 上把每个频率 row / 时间 column 都送入 SAME `TransformerBlock`，sliding-window 只 chunk attention score，不会避免整轴 Q/K/V activation 保存；这不同于 `scripts/same` 验证过的先 patch/resample 到低速率 token 域再做 local transformer。已把 `Music2Latent_axiallocal` 默认改成 stage-wise：`axial_min_channels=256`，只替换 denoising U-Net 中 `out_channels >= 256` 的低分辨率/高通道 ResBlock；显式 `model.axial_min_channels=0` 才做全量替换。本轮验证：`py_compile` 通过，3 个 axiallocal 定向 pytest 通过，Hydra 默认实例化显示 `axial_min_channels=256`、`axial_replaced_modules=12`。该入口仍只是低分辨率 SAME-style axial denoiser 诊断版，真正对齐 SAME 的正式方向应新建 patch/grid + `TransformerResamplingBlock` 架构。
- 2026-06-19 UTC 已新增 SAME-style axial local denoiser 入口：`scripts/music2latent/local_transformer.py` 从 `scripts/same/transformer.py` 复制/适配 sliding-window SDPA、DyT/RMSNorm、RoPE、TransformerBlock，并提供 `[B,C,F,T]` channel-first 的 `AxialLocalAttention2d`、`AxialResidualAttention2d`、`AxialLocalAttentionResBlock`。`scripts/music2latent/models.py` 新增 `Music2Latent_axiallocal`，继承官方 `Music2Latent`，替换 denoising U-Net 的低分辨率 2D `ResBlock`，保留官方 encoder、latent pyramid decoder、down/up resampling、wrapper/loss 和 encode/decode 语义。新增配置 `scripts/configs/experiment/music2latent_axiallocal.yaml`，默认 `axial_time_window=[1,1]`、`axial_freq_window=[1,1]`、`axial_ff_mult=1`、`axial_differential=false`，不依赖 NATTEN。验证：新增 pytest 红灯后转绿；`python -m py_compile scripts/music2latent/local_transformer.py scripts/music2latent/models.py` 通过；Hydra 小尺寸 instantiate + forward 输出 `Music2Latent_axiallocal 4 (1, 6, 4) (1, 2, 32, 8) True`。
- 2026-06-19 UTC 为运行 `scripts/configs/experiment/music2latent_localattention.yaml`，已在当前 Python 3.11 / `torch 2.11.0+cu128` 环境安装官方 NATTEN wheel：`natten==0.21.6+torch2110cu128`，安装命令 `python -m pip install --no-deps --force-reinstall 'natten==0.21.6+torch2110cu128' -f https://whl.natten.org`。导入检查显示 `natten.__version__=0.21.6`、`HAS_LIBNATTEN=True`；4090/SM89 上 fp32/fp16/bf16 的最小 1D/2D CUDA forward 通过。H200/SM90 若使用同一 `torch 2.11.0+cu128` 环境可先用同一 wheel；若遇到架构/backend 问题，官方源码编译口径应带 `NATTEN_CUDA_ARCH="9.0"`，需要同时兼容 4090 和 H200 时带 `NATTEN_CUDA_ARCH="8.9;9.0"`。`music2latent_localattention.yaml` 的 dataset root/index 已同步改到本机 `tmp/music2latent_local_2s_pt`。为适配 NATTEN 0.21.6，`DownsampleFreqLocalAttention/UpsampleFreqLocalAttention` 的 `(5,1)` 单轴注意力改为沿频率轴重排后调用 1D NATTEN，最终 `conv_out` 改为 `ChannelLinear2d` 避免 2 通道输出头触发过小 head_dim。验证：Hydra instantiate 通过，单样本 CUDA forward 得 `representation=(1,2,1024,168)`、`latents=(1,64,21)`、`out=(1,2,1024,168)` 且 finite；单卡 batch1 `python -m scripts.train experiment=music2latent_localattention ... trainer.max_steps=1` 通过并记录 `loss/total≈0.059`。
- 2026-06-19 UTC 按用户指定的本机数据根 `/inspire/qb-ilm2/project/embodied-multimodality/public/zhaoguojie/data/NETEASE_SPIDER`，从 `audio/20260520` 下裁了 10 条 2 秒 mono 44.1 kHz 样本，保存为 `tmp/music2latent_local_2s_pt/000000.pt` 到 `000009.pt`；每条 payload 的 `audio` 为 `float32` shape `(88200,)`，`files.list` 10 行，并在 `index.jsonl` 记录源 mp3 与起点。`scripts/configs/experiment/music2latent.yaml` 已把 `Music2LatentOfficialPTDataset.root/index_file` 指向该本机目录，`length=160000` 保持原逻辑用于循环这 10 条样本。
- 2026-06-19 UTC 已按用户要求 `git fetch` 并合入 `origin/main`，本地 `main` 生成 merge commit `d23502f`；提交级冲突只在 `docs/memory/current.md`，已保留远端 2026-06-19 记录和本地 RVQ 摘要。合入前创建备份分支 `codex/backup-main-before-pull-20260619-133505`，并把未提交工作保存为 `stash@{0}: codex-before-pull-2026-06-19`。随后 `stash apply` 的工作区冲突已解除：启动脚本/`docs/README.md` 手工合并，其余冲突文件选择远端最新 `causal_rvq` 主线，避免被旧 stash 口径回退；stash 仍保留，未 drop。
- 2026-06-17 RVQ 方案已改为 time-aligned token RVQ：旧的 512-D chunk-summary RVQ spec/plan 已废弃，不要执行。最终目标规格是 `2 * 48000 Hz` stereo audio -> `25 Hz` latent token rate；连续表征为 `32` 维/token，即 `32 * 25Hz`；离散表征为每 token `16` 层 RVQ codebook（默认 `codebook_size=1024`，约 `4 kbps`），并支持动态 `n_quantizers <= 16`。encoder 应用 local Transformer 输出固定时间位置锚定 token，不能再用 learned-query global summary。demo 每个样本只保存一个 concat wav：`src -> target -> rvq/discrete decode -> continuous decode`。当前有效设计见 `docs/superpowers/specs/2026-06-17-mossland-codec-time-aligned-rvq-design.md`。
- 2026-06-19 UTC 最新 SAME ResBlock 排查：用户明确要求不要用 activation checkpointing 等 trick 掩盖问题，要回到模型行为本身，并指出原 Conv2d kernel=3，SAME local transformer 应尽量按卷积局部感受野对齐。已撤回刚尝试接入的 checkpointing 改动。`same_sliding_window=[1,1]` 更接近单层 3x3 卷积的时间半径；但显存测试显示窗口从 `[8,8]` 改成 `[1,1]` 几乎不降显存，因为当前 SAME attention 仍为整条序列保存 Q/K/V，窗口只影响局部 SDPA 范围。batch2 peak：time-only `[8,8]` `7.56GB`、time-only `[1,1]` `7.57GB`；freq-only `[8,8]` `7.42GB`、freq-only `[1,1]` `7.42GB`；全量 ResBlock+freq `[8,8]`/`[1,1]` 都约 `10.94GB`。根因是高分辨率 `[B,64,1024,64]` 被 token 化为 `B*1024` 条时间序列后，20 个 TransformerBlock 的 QKV activation 远大于卷积。
- 2026-06-19 UTC 已在 `scripts/same_flow_debug/music2latent_same_ablation.py` 新增累计 variant `denoiser_resblock_freq_resampling_same_time`，并新增 `same_resblock_min_channels` 用于 stage-wise 替换 ResBlock。该开关不是训练 trick，而是模块替换顺序控制：`same_resblock_min_channels=256` 只替换 12 个低分辨率/高通道 ResBlock，避开最贵的 64-channel/1024-frequency 高分辨率 ResBlock；`128` 替换 16 个；`0/64` 替换全部 20 个。batch2 peak（`window=[1,1]`，含 SAME freq resampling）：`min_channels=256` 为 `7.743GB`，`128` 为 `8.813GB`，全量为 `10.943GB`。固定 batch 对齐 `min_channels=256, window=[1,1]` 干净：official/candidate loss 都 `0.1537591666`，grad `0.312643` vs `0.312671`，head RMS 正常。曾启动 batch32/acc2、`min_channels=256` 300-step 短跑，能跑但每卡仍到 `90GB+`，已停止；不应把这个当常规口径。
- 2026-06-19 UTC 当前有效方向：不要再优先做 8/16 窗口超参比较；ResBlock 替换应按卷积等价局部窗口 `[1,1]` 及 stage-wise 模块替换推进。若要最终替换高分辨率 ResBlock，需要重新设计真正卷积等价/局部的 SAME adapter（避免在 1024 个频点上保留大量完整 QKV activation），而不是靠 checkpointing 或单纯降 batch。
- 2026-06-19 UTC 新设计方向：可以用真正的 2D local attention 模拟 Conv2d 局部行为，但不能复用当前 1D `TransformerBlock` tokenization。应在 `[B,C,F,T]` 上用 `unfold`/chunk 取每个位置的 `3x3` 邻域，做 per-position query 对 9 个局部 key/value 的 attention，复杂度约 `B*F*T*9*C`，天然支持变长时间且不会生成全序列 Q/K/V。若追求和 Conv2d 完全等价，可把 attention 初始化为局部静态位置权重/近似 depthwise dynamic conv；若保留 content-based attention，则是局部动态卷积的泛化，不是严格 Conv2d。
- 2026-06-19 UTC 已实现 2D local attention ResBlock adapter：`Local2DAttentionConv` 每个 `[F,T]` 位置只 attend `kxk` 邻域（默认 `k=3`），`SAMELocal2DResBlockAdapter` 保持 Music2Latent ResBlock 的 GroupNorm/SiLU/time embedding/residual/optional attention 结构，第二个 local-attn 输出 zero-init。新增 variant：`denoiser_resblock_same_local2d`、`denoiser_resblock_freq_resampling_same_local2d`，参数 `same_local2d_kernel_size`。固定 batch `denoiser_resblock_same_local2d,k=3` 对齐干净：official/candidate loss 都 `0.1537591666`，grad `0.312643` vs `0.307322`，head RMS 正常。batch2 peak：official `3.881GB`，旧 time-token `window=[1,1]` `7.571GB`，local2d k3 `5.921GB`，local2d+freq `9.311GB`。短跑 `debug_overfit10_music2latent_same_resblock_local2d_k3_bs32_acc2_warm10_300` 启动到约 step104 后手动停止，因停止时 TensorBoard 只 flush 到 step52；已知窗口：0-25 `0.104915`、25-50 `0.100696`，贴近 baseline 0-25 `0.104558`、25-50 `0.101856`。batch32/acc2 显存约 `73GB/GPU`，比旧 high-res time-token 路径健康，但速度慢约 `0.40 it/s`，因为当前实现是 Python 级 9 邻域循环，后续可优化。
- 2026-06-19 UTC 模块行为审计：用户要求替换模块必须和原 Music2Latent 在感受野、计算量和 activation 行为上接近。结论是旧 `SAMEResBlockAdapter` 的 `[B*F,T,C]` time-token 路径不等价于原 2D `3x3` Conv2d ResBlock，因为它只沿时间局部 attention、缺少单层频率局部混合，后续只保留为诊断。`SAMELocal2DResBlockAdapter(k=3)` 是当前最接近 ResBlock 的候选：两层后约 `5x5` 有效足迹，粗略 MAC 约为原两层 dense `3x3` Conv2d 的 `0.45x~0.48x`，batch2 activation peak 约官方 `1.5x`；但它仍是 content-adaptive softmax local attention，不是严格 Conv2d。`SAMEAttentionAdapter` 轴向匹配原 frequency attention（`[B*T,F,C]`），但默认局部窗口和 FFN 使其不再等同于原 full-frequency MHA。`SAMEFreqDownsample/UpsampleAdapter` 当前用 `TransformerResamplingBlock`，默认 window 下频率感受野远大于原 `(5,1)` stride/up conv，activation 行为也偏重；不应作为最终卷积等价替换，后续应改写成局部频率 `(5,1)` 足迹的 resampling attention，或暂缓替换 freq resampling。
- 2026-06-19 UTC 速度/显存对比：官方 baseline `logs/music2latent-official/runs/2026-06-19_17-20-09` 每卡 batch64、effective batch256，TensorBoard wall_time 估计 median `1.435s/step`、trimmed `0.697 it/s`。已有 SAME runs：attention-only batch32/acc2 median `1.697s/step`（约慢 18%）；旧 TransformerResamplingBlock freq batch32/acc2 `3.010s/step`；attention+旧 freq `3.250s/step`；旧 time-token ResBlock `3.022s/step`；local2d ResBlock 当前 Python 9 邻域循环 `4.879s/step`。新诊断版 `SAMELocalFreqDown/UpsampleAdapter(k=5)` 固定 batch 对齐干净（loss `0.1537591666`，grad `0.304122` vs official `0.312643`），但 batch64 4卡短跑 `debug_overfit10_music2latent_same_freqlocal_k5_bs64_warm10_30` 在第 0 step 前向 OOM，单进程约 `95GB`，失败点是 local freq attention 的 Q/K/V padding activation。因此它感受野对齐但 activation 行为仍不合格，只保留 `same_freq_resampling_kind=local` 显式诊断；freq resampling 不应作为当前正式替换方向。
- 2026-06-19 UTC 第二条 SAME-shape 1D patch 线已按用户判断先放松压缩率：`scripts/configs/experiment/music2latent-same-1d-patch.yaml` 当前默认 `hop=64, fac=4, center_pad=false`，representation 约 `[B,2,128,528]`，4 次 time stride 后 latent `[B,64,33]`（单样本 `2112` floats，约官方 `[B,64,8]` 的 `4.1x`），参数量 `54.10M`。300-step 结果随 `hop=256 -> 128 -> 64` 改善（200-300 分别 `0.092776/0.086346/0.077436`），说明降低频率 bins、增加 time/latent tokens 是有效方向。但 hop64 长跑已平台：resume run `debug_overfit10_same1dpatch_hop64_bs64_warm10_2000_resume625` 到 step1855，窗口 `700-900=0.070986`、`900-1200=0.071273`、`1200-1500=0.069463`、`1500-1800=0.069127`；官方同窗口已到 `0.023426/0.010833/0.006741/0.004511`。用户指出不能把问题简单归因于“频率无交互”：当前 `SpectralFrameAdapter` 和输出头确实是频率全连接。更准确结论是 token 粒度/信息分配不对：每个 time token 承载整帧频谱细节、skip、噪声条件和 latent 条件，训练需要在粗粒度 frame 向量上生成局部频谱 residual；Music2Latent 则把信息摊在 `[F,T]` 网格 feature cell 上逐层局部处理。`output_residual_scale=64` 只是补 frame-linear head 初始梯度的临时补丁，不应作为最终设计。下一步应让每个 token 承载更少频谱信息，保留 frequency patch/grid tokens 到 denoiser 与输出 head，再验证 overfit10。
- 2026-06-19 UTC 用户指定新的完美过拟合 baseline 为 `logs/music2latent-official/runs/2026-06-19_17-20-09`，取代此前 `16-48-51`：配置为 4 卡、每卡 `train_batch_size=64`、`bf16-mixed`、`wrapper.lr_warmup_steps=10`，物理 overfit 文件为 `tmp/music2latent_official_pt_10k/files.list` 指向的小集合（文件名 `000000.pt` 到 `000010.pt`，`wc -l` 因末尾换行显示 10）。该 baseline `loss/total` 窗口：0-25 `0.104558`、100-200 `0.078914`、200-300 `0.045322`、300-500 `0.037001`、500-700 `0.031622`、700-900 `0.023426`、1500-1900 `0.004135`，step1901 为 `0.002392`。已在 `scripts/same_flow_debug/music2latent_same_ablation.py` 增加累计 variant：`denoiser_attention_freq_resampling_same` 及 `_down_only/_up_only`，用于在 Music2Latent wrapper/loss/data 不变时逐步替换 SAME attention 和 SAME freq resampling。固定 batch 对齐显示 attention-only、freq-resampling-only、attention+freq-resampling 的初始 loss 均与官方一致（`0.1537591666`），grad/head 输入 RMS 接近官方。4 卡 batch64 的 SAME attention 会触发 PPU `GET was unable to find an engine` workspace/后端错误；可运行口径改为每卡 batch32 + `trainer.accumulate_grad_batches=2`，保持全局 effective batch 256。
- 2026-06-19 UTC SAME attention-only 的 batch32_acc2 短跑 `logs/same-flow-baseline/runs/debug_overfit10_music2latent_same_attention_freq_bs32_acc2_warm10_700` 已手动停在 step443，趋势与 `17-20-09` baseline 对齐：0-25 `0.104912`、100-200 `0.079114`、200-300 `0.046191`、300-443 `0.036596`。按用户要求已并行占用 8 卡继续跑两组 900-step 对照：0-3 卡 `debug_overfit10_music2latent_same_attention_freqresamp_bs32_acc2_warm10_900`（attention+freq resampling）当前仍在跑，最新 step279，窗口 100-200 `0.081304`、200-279 `0.046083`；4-7 卡 `debug_overfit10_music2latent_same_freqresamp_bs32_acc2_warm10_900`（freq resampling only）当前仍在跑，最新 step300，窗口 100-200 `0.081219`、200-300 `0.044420`。两者均已有 100/200 checkpoint 和 1/101/201 demo。结论：先换 attention 或 freq resampling 的训练语义正确，趋势正常；transformer 慢一些可接受，继续等 500/700/900 窗口和 demo。
- 2026-06-19 UTC 用户明确硬约束：为了支持变长音频编解码，**时间维度绝对不能 full attention**；替换原卷积时必须使用天然局部的 SAME/local transformer。已停止 `debug_overfit10_music2latent_same_resblock_time_ff1_nodiff_fullattn_bs32_acc2_warm10_700`，并在 `scripts/same_flow_debug/music2latent_same_ablation.py` 加防呆：`ablation_variant=denoiser_resblock_same_time` 若 `same_sliding_window=None` 会直接 `ValueError`。当前只保留时间局部 ResBlock 主体替换实验：0-3 卡 `[8,8]` run `debug_overfit10_music2latent_same_resblock_time_ff1_nodiff_win8_bs32_acc2_warm10_700`，4-7 卡 `[16,16]` run `debug_overfit10_music2latent_same_resblock_time_ff1_nodiff_win16_bs32_acc2_warm10_700`。
- 2026-06-19 UTC 继续排查 Claude Code + AI Gateway：`~/.claude/settings.json` 已按页面配置 `ANTHROPIC_BASE_URL=https://aigw.sotatts.online`、`ANTHROPIC_AUTH_TOKEN` 和 `model=claude-opus-4-7`；Claude Code 会读取配置，但请求仍失败。直接查网关 `/v1/models` 可见 `claude-opus-4-7/4-8`，但 `/v1/messages` 对所有 `claude-*` 返回 `no available upstream channel for model ...`。`gpt-5.4/gpt-5.4-mini/gpt-5.5/codex-auto-review` 直连 Anthropic messages 入口能返回，但 Claude Code 发出的 system role 会被这些后端拒绝。结论：当前不是本机安装或 token 格式问题，需要 AI Gateway 侧启用 Claude upstream，或修复非 Claude 模型的 Anthropic 兼容转换。记录见 `docs/agent/commands.md` 的 `Claude Code` 小节。
- 2026-06-19 UTC 已按用户要求绕过代理全局安装 Claude Code：用清空 `HTTP_PROXY/HTTPS_PROXY/ALL_PROXY` 及小写同名变量的 npm 命令安装 `@anthropic-ai/claude-code@latest`，当前 `/usr/local/bin/claude` 版本为 `2.1.183 (Claude Code)`。可复用命令记录在 `docs/agent/commands.md` 的 `Claude Code` 小节。
- 2026-06-19 UTC 用户指定新的 Music2Latent 对比 baseline 为 `logs/music2latent-official/runs/2026-06-19_16-48-51`，关键配置是每卡 `train_batch_size=64`、4 卡、`bf16-mixed`、`wrapper.lr_warmup_steps=100`；`16-47-16` 仍是 warmup 10000，不再作为当前 baseline。已在 `scripts/same_flow_debug/music2latent_same_ablation.py` 修正 SAME attention adapter 输出 dtype/contiguous，并新增/验证 `ablation_variant=denoiser_attention_same_freq`。4 卡 batch64 的 SAME attention candidate 因显存/workspace 压力 OOM/GET 失败；可运行设置为 batch48、`same_ff_mult=1`、`same_differential=false`，参数量约 `60.0M`。run `logs/same-flow-baseline/runs/debug_music2latent_same_attention_freq_bs48_warm100_ff1_nodiff_1500` 手动停在 step 547，loss 窗口几乎贴近新 baseline：100-200 `0.078919` vs baseline `0.078251`，200-300 `0.041774` vs `0.040822`，300-500 `0.031774` vs `0.032658`，500-600 `0.027862` vs `0.028330`。结论：SAME attention-only 在 Music2Latent wrapper/loss/data 下能快速学习；原 `scripts/same_flow` 不降更可能来自正式 same_flow 的输出尺度、head 梯度聚合、训练目标或残差语义不一致。记录见 `docs/exp/same_flow_music2latent_exp.md`。
- 2026-06-19 UTC 用户切换 same_flow 调试策略：先不要继续改 `scripts/same_flow`，改为以 `scripts/music2latent` 官方训练过程为基准，逐步替换为 `scripts/same` 中的等价模块，并把实验记录写入 `docs/exp/same_flow_music2latent_exp.md`。已停止 4-7 卡上旧 `debug_2dlegacy60m_spatialmix_s2p5_1500` 试跑；我刚加到 `scripts/same_flow.models.SameFlowBlock` 的 `spatial_mixing` 临时分支已手工撤回。新增 `scripts/same_flow_debug/music2latent_same_ablation.py`，其中 `Music2LatentSAMEAblation(ablation_variant=denoiser_resblock_same_time)` 只替换 denoising U-Net 的 20 个 ResBlock 为基于 `scripts.same.transformer.TransformerBlock` 的 adapter，encoder/decoder/audio/loss/wrapper 保持 Music2Latent。固定 batch 对齐：loss 与官方同为 `0.2226602733`，grad `0.4217` vs 官方 `0.4323`，`conv_out/input` RMS `0.631` vs 官方 `0.636`。4-7 卡 1500-step 短跑 `logs/same-flow-baseline/runs/debug_music2latent_same_resblock_time_1500` 完成：1250-1500 loss 均值 `0.074835`，官方同窗口约 `0.047786`；说明只换 denoiser ResBlock 后能学习但快速下降明显滞后。下一步应继续在 `same_flow_debug` 中拆 SAME block 因素（sliding window、DyT/RMSNorm、differential attention、FFN mult、zero-init），暂不大范围替换 encoder/decoder。
- 2026-06-19 UTC 用户反馈 0-3 卡跑 `music2latent`、4-7 卡跑 `same_flow_baseline` 时，`music2latent` loss 快速下降但 `same_flow_baseline` 基本不降。已新增调试脚本 `scripts/same_flow_debug/compare_training_dynamics.py`，可用固定 batch/sigma/noise 对 `experiment=music2latent` 与 `experiment=same_flow_baseline` 做一次 forward/backward，并写出 loss、latent/pyramid/head activation、模块 grad norm 和可选固定 batch descent。日志读取确认旧 same-flow run `logs/same-flow-baseline/runs/2026-06-19_15-15-15` 仍是 `precision: 16-mixed`，而 music run 是 `bf16-mixed`；但主因不是 precision。TensorBoard rolling mean：music loss 从前 100 step 均值约 `0.104` 降到 3000-4000 step `0.028`，same 0-2316 step 仍约 `0.095~0.10`。固定初始诊断：两边 loss 完全相同 `0.22266`，latent std 都约 `0.31~0.32`，output head 输入 RMS 都约 `0.60`，但 Music2Latent zero-init `conv_out` 梯度 norm `0.432`，SameFlow frame `Linear(64->2048)` output head 只有 `0.00537`；原因是 Music2Latent 2D `conv_out` 在 1024 个频率位置共享权重并聚合梯度，而 SameFlow 每个频点是独立输出通道，平均 loss 下有效函数更新太小。已给 `scripts/same_flow.models.ZeroTokenProjection` 增加 `output_scale`，`SameFlowConsistencyAutoencoder` 增加 `output_residual_scale`，并在 `same_flow_baseline.yaml` 设 `model.output_residual_scale: 64.0`。修正后同一诊断下 same grad norm 变为 `0.344`，接近 Music2Latent `0.432`，step0 loss 不变。验证：`py_compile scripts/same_flow/models.py scripts/same_flow_debug/compare_training_dynamics.py` 通过；单卡 5-step Lightning smoke `experiment=same_flow_baseline ... trainer.max_steps=5` 通过，显示 `bf16-mixed`、`54.4M` 参数。
- 2026-06-19 UTC 用户八卡/四卡跑 `experiment=music2latent` 到 step 4794 时，`scripts/music2latent/training_base.py` 的本地 fail-fast 检查因 `grad_norm/before_clip=inf` 直接抛错，首个非有限梯度是 `model.conv_out.weight` 中 `1/1152` 个元素；这更像 fp16 AMP 动态 loss scaling 的一次梯度 overflow，而不是官方训练代码前向 loss NaN。已新增独立开关 `fail_on_nonfinite_grad`：`fail_on_nonfinite` 仍检查前向/损失 tensor 非有限，梯度非有限检查可单独关闭。随后按用户要求将 `scripts/configs/experiment/music2latent.yaml` 和 `same_flow_baseline.yaml` 从官方 fp16 口径改为 Lightning `bf16-mixed`，其它训练参数保持不变；两者当前均显式 `fail_on_nonfinite: true`、`fail_on_nonfinite_grad: false`。未写该新字段的旧配置保持兼容，默认跟随 `fail_on_nonfinite`。
- 2026-06-19 UTC 按用户要求新增 `scripts/configs/experiment/same_flow_baseline.yaml`：除模型外，数据/训练/optimizer/loss 参数对齐当前 `scripts/configs/experiment/music2latent.yaml`。数据同样读取 `tmp/music2latent_official_pt_10k/files.list`，物理 10000 条 mono `(34304,)` `.pt`、逻辑 `length=160000`、八卡每卡 batch 2 全局 batch 16；训练同样 `RAdam lr=1e-4`、`bf16-mixed`、10k warmup、800k steps、cosine 到 `1e-6`、EMA、official lognormal sigma、`consistency_step_end_exp=3.0`。`scripts/same_flow/wrapper.py` 的主 consistency loss 已改成和 `scripts/music2latent/wrapper.py` 一样的逐元素 `pseudo_huber_loss(predicted,target)*1/(sigma_high-sigma_low)` 后 mean，不再用 same_flow 旧 flatten huber；`cosine_decay` 也改为使用 `final_learning_rate`。`scripts/same_flow/models.py` 的 `encode()` 已按 no-center-pad 34304/64-frame 公式对齐。baseline 模型约 `54.4M` 参数（Music2Latent 约 `57.7M`），配置为 `base_channels=64,bottleneck_base_channels=512,transformer_depth=1,dim_heads=64,data_channels=2,hop=512,center_pad=false`。验证：`py_compile scripts/same_flow/wrapper.py scripts/same_flow/models.py` 通过；Hydra instantiate 得 params `54387556`、batch `(2,34304)`、representation `(2,2,1024,64)`；CPU direct `training_step().backward()` loss `0.2155` 且 `unused_grad_params=0`。
- 2026-06-19 UTC 已按用户要求把 `scripts/configs/experiment/music2latent.yaml` 改成 `tmp/music2latent-training (1)/music2latent-training` 官方 public training code 复现口径：mono、`hop=512/fac=4/data_length=64`、34304-sample crop、`data_channels=2`、`base_channels=64`、continuous-only `quantizer_num_quantizers=0`、RAdam、10k warmup、cosine 到 `final_learning_rate=1e-6`、800k steps、EMA、official 10000-bin lognormal sigma weighted sampling、`base_step=0.1/end_exp=3`；当前 precision 按用户后续要求为 `bf16-mixed`（官方原始代码是 fp16 autocast + GradScaler）。新增 `scripts/music2latent/data.py`，提供 `Music2LatentOfficialPTDataset` 和 `python -m scripts.music2latent.data` 预读取工具；已从 `tmp/same/netease_spider_audio.index.list` 生成 `tmp/music2latent_official_pt_10k/`，包含 10000 个 `.pt`、`files.list` 和 `index.jsonl`，每个 tensor shape/storage 都是 `(34304,)`，目录约 `1.6G`。YAML 中 dataset 逻辑 `length=160000`，对齐官方 `iters_per_epoch=10000` 与全局 batch 16，训练时对 10000 个物理 `.pt` 取模复用。注意本地 NETEASE 索引几乎全是 mp3（8417605 mp3、23 flac），所以预读取阶段包含 mp3；训练热路径只读 `.pt`。验证：`py_compile scripts/music2latent/*.py` 通过；Hydra instantiate 得 batch `(2,34304)`、representation `(2,2,1024,64)`；单卡 1-step smoke `python -m scripts.train experiment=music2latent trainer.devices=1 trainer.strategy=auto trainer.max_steps=1 trainer.max_epochs=1 data.train_batch_size=1 data.num_workers=0 callbacks.demo_callback.demo_every=999999 logger.wandb=null trainer.enable_model_summary=false trainer.num_sanity_val_steps=0 extras.print_config=false` 通过，`loss/total=0.042`。八卡正式启动直接用 `python -m scripts.train experiment=music2latent`；若改卡数，要保持全局 batch 16。
- 2026-06-19 UTC same_flow debugfaithful 3000-step 虽然非静音样本 waveform corr 已高（debug_train raw `nonquiet_corr_mean=0.928, nonquiet_snr_db_mean=8.92dB`，试听总览 `tmp/same_flow_listen/assembled_debugtrain_debugfaithful_3000/overview_3000_src_recon_direct.wav`），但听感仍明显不同；原因不是采样器一步生成，`direct_sigma=80` 与普通 1-step decode 指标几乎相同，4-step 也只小幅提升。主要问题是频谱没拟合准：`lsd_db_mean≈29.8dB`、高频比例在不同样本上严重偏离（如 `hf_ratio` 从 0.196 到 5.565），低频平均也偏低（`lf_rms_ratio_mean≈0.716`）。已从 `tmp/same_flow_train/baseline_m2l_parity_debugfaithful_3000/last.ckpt` 续训到 `tmp/same_flow_train/debugfaithful_from3000_to10000/`（再跑 7000 step，save every 1000）观察频谱距离是否继续下降。另已修复 `scripts/train.py` 忽略 `cfg.seed` 的问题，现在会 `pl.seed_everything(cfg.seed, workers=True)`；此前 Lightning run 与 `debug_train.py` 不可比，因为 YAML 里的 `seed: 1234` 实际没有生效。
- 2026-06-19 UTC 为复刻旧 debug 成功口径，新增 `scripts/configs/experiment/same-flow-baseline-m2l-parity-debugfaithful.yaml`：生产模型仍是 `scripts.same_flow.models.SameFlowConsistencyAutoencoder`，但 `wrapper.consistency_step_schedule=constant, consistency_step=0.1`，显式模拟旧 `scripts/same_flow_debug.debug_train` 因未推进 `global_step` 而固定最大 consistency step 的训练行为。500-step 已和旧 debug baseline 对齐：production debugfaithful `corr_mean=0.1324,prediction_rms_mean=0.02233,direct_corr_mean=0.2436`，旧 debug 500 `corr_mean=0.1303,prediction_rms_mean=0.02198,direct_corr_mean=0.2627`；试听总览在 `tmp/same_flow_listen/assembled_baseline_m2l_parity_debugfaithful_500/overview_500_src_recon_direct.wav`。`scripts/same_flow/wrapper.py` 与 debug 副本会记录 `consistency/step_size`，两个 `debug_train.py` 已支持 `_debug_global_step_override`，避免以后误把固定 step0 行为当官方 schedule。当前后台 3000-step 复刻命令写到 `tmp/same_flow_train/baseline_m2l_parity_debugfaithful_3000/`，需完成后 eval 并生成 3000-step 听感。
- 2026-06-19 UTC `scripts/same_flow` 已整理成独立命名 baseline：主类为 `scripts.same_flow.models.SameFlowConsistencyAutoencoder`，配置 `same-flow-overfit10.yaml` 和新增 `same-flow-baseline-m2l-parity.yaml` 指向该类，不再用 `Music2LatentSameFlow` 作为生产入口。baseline 固定为 frame-direct full-frame residual head：`to_tokens()` 编码 frame token，末端 `ZeroTokenProjection(input_channels, data_channels*freq_bins)` 直接输出整帧 residual，再 reshape 回 `[B,C,F,T]`。已删除无用 0 权重训练项 `denoising_anchor_weight/high_freq_anchor_weight/latent_prior_loss_weight`，并删除 main baseline 的 `adapter.frame_unproj/patch_decode`、`prior_norm/prior_proj/latent_prior()`，这些正是用户启动 `same-flow-overfit10` 时 DDP unused parameters 的根因。验证：`py_compile scripts/same_flow/*.py` 通过；local backward `unused_count=0`；`python -m scripts.train experiment=same-flow-overfit10 trainer.max_steps=2 trainer.devices=1 trainer.num_nodes=1 callbacks.demo_callback.demo_every=999999 paths.output_dir=tmp/same_flow_train_smoke` 通过。
- 2026-06-19 UTC `m2l_parity_framedirect_3000` 已完成并生成新听感样例。checkpoint：`tmp/same_flow_debug_train/m2l_parity_framedirect_3000/last.ckpt`；全量 eval 输出：`tmp/same_flow_debug_listen/m2l_parity_framedirect_3000_last_raw_step1/`；最方便试听的是 `tmp/same_flow_debug_listen/assembled_3000_last/overview_3000_last_src_recon_direct.wav`，每段为 `source -> reconstruct_raw -> direct_sigma10_raw`。10 条 1-step deterministic：`corr_mean=0.7370,SNR=6.77dB,MRSTFT-SC=0.461,logmel_l1=2.299,pred_rms=0.1107/source_rms=0.1322`；好样本如 item001 corr `0.978`，失败样本集中在 item006/item008，后续需要专门排查失败样本。
- 2026-06-19 UTC 已用当前有效路径 `scripts/same_flow_debug` + `same-flow-debug-m2l-parity` 的 frame-direct 结构生成可听音频。checkpoint：`tmp/same_flow_debug_train/m2l_parity_framedirect_1500/last.ckpt`，override：`model.use_patch_denoiser=false, model.direct_patch_output=false, model.direct_frame_output=true`。输出目录：`tmp/same_flow_debug_listen/m2l_parity_framedirect_1500_raw_step1/`，每条含 `source.wav`、`reconstruct_raw.wav`、`direct_sigma10_raw.wav`；方便听的拼接目录：`tmp/same_flow_debug_listen/assembled/`，其中 `overview_src_recon_direct_selected.wav` 为多个样本的 `source -> reconstruct -> direct_sigma10` 总览。10 条 1-step deterministic：`corr_mean=0.4423,SNR=0.30dB,MRSTFT-SC=0.941,logmel_l1=2.415`；`direct_sigma=10`：`direct_corr_mean=0.5513,direct_SNR=0.66dB`。这次也修复了 `scripts/same_flow_debug/eval_checkpoint.py` 的 mono 保存/评估 bug，并让 parity eval 直接从 `representation -> model.encoder` 得到训练一致的 latent `[B,64,8]`，避免通用 `model.encode()` 在 no-center-pad 配置下误 padding 成 `[B,64,9]`。
- 2026-06-19 UTC same_flow 当前排查已切到“已验证小 Music2Latent fast baseline” parity，而不是官方默认大模型：baseline 是 `tmp/original_music2latent_official_overfit10_fast_config.py`（`base_channels=32,bottleneck_base_channels=256,cond_channels=128,heads=4,num_bottleneck_layers=2,bottleneck_channels=64,lr=3e-4,total_iters=3000,warmup=100,final_lr=1e-5`）。新增 SAME parity 配置 `scripts/configs/experiment/same-flow-debug-m2l-parity.yaml`，数据为 `tmp/same_flow/m2l_fast_baseline_cache_48000_mono_34304/index.jsonl`，从同一批 baseline wav 直接取 48k mono 34304 samples；STFT 为 official no-center-pad `hop=512,fac=4,data_channels=2`，得到 `representation [B,2,1024,64]`、`latent [B,64,8]`。`AudioProcessor(center_pad=false)` 与 wrapper/debug_train 裁剪逻辑已为 parity 修正。
- 2026-06-19 UTC trace 脚本 `scripts/same_flow_debug/compare_music2latent_trace.py` 已能在同一音频、同一 sigma/noise 下输出 Music2Latent vs SAME 每个等价 stage 的 `shape/std/rms`。关键 bug 已定位并在 debug 版修正：官方 `conv_out` 是 zero-init residual head，SAME 原 `output_proj=SameFlowBlock` 在 `in_channels==out_channels` 时有 residual identity，导致 random residual head 非零；现已新增 `ZeroTokenProjection`，parity 配置启用 `direct_patch_output=true`，random/init trace 中两边 `edm.output` RMS 已完全对齐为 `0.0249946`。修正记录在 `docs/exp/same_flow_exp.md`。
- 2026-06-19 UTC `m2l_parity_zerohead_1500` 已完成但仍弱：`tmp/same_flow_debug_train/m2l_parity_zerohead_1500/last.ckpt` step1500 probe RMS `0.0020`、MRSTFT-SC `0.997`，trace `tmp/same_flow_debug_trace/m2l_fast_parity_official1500_same1500_sigma10_item0.json` 显示 official step1500 final residual head RMS `0.4535`，SAME 只有 `0.0668`（约 15%），`edm.output` 约 17%。zero-head 梯度存在（`output_proj.proj.weight grad≈3.43`），不是断梯度。当前正在跑下一条结构对照 `m2l_parity_zerohead_patch64_1500`：只改 `model.patch_channels=64`，去掉 parity 下 `patch_dim=64 -> patch_channels=32` 的早期 patch 信息瓶颈，命令见 `docs/exp/same_flow_exp.md`。
- 2026-06-19 UTC 历史记录：`scripts/same_flow/` 与 `scripts/same_flow_debug/` 曾按官方 Music2Latent `utils.huber()` 对齐为 per-sample flatten huber，并记录在 `docs/exp/same_flow_exp.md`。当前主线已被 2026-06-19 后续用户要求覆盖：`scripts/same_flow/wrapper.py` 现在对齐本仓库当前 `scripts/music2latent/wrapper.py` 的逐元素 `pseudo_huber_loss * weight` mean；旧 flatten huber 只作为历史实验口径参考。仍保留的对齐项包括 `sigma_sampling=lognormal` 官方 10000-bin weighted multinomial + jitter、RAdam、cosine/final lr、`consistency_step_end_exp=3.0`。不能把 `max_denoise + MSE` debug loss 和当前 official-style consistency loss 对比。
- 2026-06-18 UTC 按用户要求把“用 `scripts/same` local transformer 替换 Music2Latent 卷积骨干、目标 `2*48000 -> 32*25` 且保留 skip connect”的第一版实现落到 `scripts/same_flow/`，并整理成自包含目录，不再 import `scripts/music2latent` 或 `scripts/same`。当前目录只保留 `audio.py`、`quantize.py`、`training_base.py`、`transformer.py`、`wrapper.py` 和 `models.py`；旧 `music2latent_base.py` 已删除。`models.py` 已按用户反馈改为显式定义 `SameFlowEncoder`、`SameFlowDecoder`、`Music2LatentSameFlow` 和 generator 层表，不再使用 `_replace_resblocks` 或“先构造旧卷积 Music2Latent 再递归替换”的写法。随后按用户要求重新以 `scripts/same` 和 `scripts/music2latent` 为准校准：`SameFlowResampling1d` 按 SAME `TransformerResamplingBlock` 语义实现，包含 chunk padding、learned output tokens、sliding-window transformer、SAME mapping 和输出 token 选择；二维频谱只做必要轴变换，`freq_downsample_list=1` 走频率 stride 4，`0` 走频率 stride 2 + 时间 stride 2，以保留 Music2Latent 的尺度、latent pyramid 和 U-Net skip 对齐。SAME mapping 使用 weight-norm 1D mapping，但改为 `torch.nn.utils.parametrizations.weight_norm`，避免旧 `torch.nn.utils.weight_norm` 与 Music2Latent wrapper EMA deepcopy 冲突。decode/reverse diffusion 恢复 Music2Latent 随机 reverse-step 口径；`encode()` 非整除长度保留向上 padding，避免推理时裁掉尾巴。配置 `scripts/configs/experiment/same-flow-overfit10.yaml` 为 48 kHz stereo、1 秒、`hop=240/fac=4`、continuous-only。已验证 `py_compile scripts/same_flow/*.py`、随机 `rep=(1,4,480,200)`、`latent=(1,32,25)`、pyramid 尺度 `[(1,32,480,200),(1,64,120,200),(1,128,60,100),(1,128,30,50),(1,128,15,25)]`、wrapper `training_step().backward()` finite；短输入 `120` samples、非整除 `48123` samples 也能 encode/decode，`48123` 输出 latent `(1,32,26)`。注意 transformer resampling 后 CPU decode 较慢，应优先在 GPU 上跑 overfit10 听 demo，再决定是否开启 RVQ。
- 2026-06-18 UTC 用户启动 `same-flow-overfit10` 时第 0 步前一直打印 `Skipping long audio ... max_duration_seconds=300`，原因是配置误指向 `tmp/music2latent/overfit10.sample.index.list`，该文件实际有 `8,417,628` 行全量索引，不是 overfit10；`SampleDataset.__getitem__()` 遇到长音频会从 `self.filenames` 移除并递归随机抽下一条，导致多卡 dataloader 启动阶段反复跳长音频。已从该索引筛出 10 条 `<300s` 音频写到 `tmp/same_flow/overfit10_audio.index.list`，并把 `scripts/configs/experiment/same-flow-overfit10.yaml` 的 `data.dataset.index_file` 切过去；同时 `model.audio_processor._target_` 改为 `scripts.same_flow.audio.AudioProcessor`，保持 same_flow 自包含。验证：Hydra dataloader `data.train_batch_size=1,data.num_workers=0` 首批 `(1,2,96000)` 正常返回；随机 wrapper `training_step().backward()` finite。
- 2026-06-18 UTC `scripts/same_flow/wrapper.py` 已按用户要求把 `latent/source_discrete` 从固定 `0.0` 改为当前训练实际 source latent 的 std，并同步记录 `latent/source_discrete_mean`、`latent/source_discrete_std`。continuous-only 配置下 wrapper 会显式 `self.model.encoder(representation)` 得到 `source_latent` 并作为 `latent_override` 传入 consistency loss，避免后续 forward 重复 encode；有 RVQ 时 `source_latent=quantized.continuous`，离散 RVQ stats 仍走原 `latent/discrete_*`。
- 2026-06-18 UTC 按用户确认“不用保留”旧 2D-axis 方案后，`scripts/same_flow.models.Music2LatentSameFlow` 已切成 SAME-L 风格 1D frame-token 结构：`SpectralFrameAdapter` 把 `[B,4,480,T]` 按 32-bin frequency patch 压成 `[B,512,T]`，后续 encoder/decoder/denoiser/skip 全部沿时间轴使用 SAME 1D resampling；旧类改名为 `Music2LatentSameFlow2DLegacy`，不再是公开配置入口。当前 `same-flow-overfit10.yaml` 显式 `frame_channels=512,freq_patch_size=32,patch_channels=32`，2 秒 `sample_size=96000` 验证形状为 latent `(1,32,50)`、pyramid `[(1,32,400),(1,64,400),(1,128,200),(1,128,100),(1,128,50)]`、输出 representation `(1,4,480,400)`；随机 wrapper `training_step().backward()` finite，`encode/decode` smoke 得 `(1,2,96000)->(1,32,50)->(1,2,96000)` finite。
- 2026-06-18 UTC `same_flow` 持续实验记录固定写入 `docs/exp/same_flow_exp.md`。新增 `scripts/same_flow/eval_checkpoint.py` 固定评估脚本，输出 source/reconstruct wav 和 `corr/SNR/LSD/latent_std` JSON，支持 raw/EMA checkpoint 与 `--direct-sigma` 诊断；新增 `debug_train.py` 单卡快速训练脚本。重要排查：debug loop 必须 step LR scheduler，否则 `LinearLR(start_factor=1e-8)` 会让 lr 卡在 `2e-12`，参数几乎不动。`SameFlowTrainingWrapper` 新增 `denoising_anchor_weight`，默认 0，当前 `same-flow-overfit10.yaml` 设 1.0 作为 from-scratch overfit 稳定项。quick300_anchor_sched 结果：1-step generative decode 2 条固定样本 `lsd_db_mean=38.83,corr_mean=0.00144`，比 step0 `lsd≈49.06` 和 quick100 `lsd≈40.65` 有改善；`direct_sigma=10` 的 `direct_corr_mean=0.0508`。同 checkpoint 4-step 随机 decode 更差 (`lsd≈55.04`)，说明当前多步 reverse diffusion 注入噪声，短期以 1-step decode 做主指标，后续应试 deterministic sampler 或更匹配训练的采样口径。
- 2026-06-18 UTC 按 CoDiCodec 理论重新 debug `same_flow` conditioning 链路：一致性生成式重建必须让 decoder 强条件于 encoder latent / pyramid skip，否则会退化成近似无条件去噪。旧 `SameFlowResampling1d` 在 encoder stride-2 learned output token 处把不同输入压成相同 latent；只修 encoder 后又发现 decoder upsample pyramid 除最深层外几乎相同，换 latent 后 `out_diff≈2e-8`。已把 `SameFlowResampling1d.forward()` 改成在没有 `override_new_tokens` 时把 input chunk mean content 加到 learned output token 上，encoder/decoder 都生效，仍保留 SAME learned token + local transformer 结构。验证：随机模型最终 latent diff `0.1785`；真实 dataloader 两条音频 latent distance `0.156`；decoder pyramid mismatch diff 约 `0.034~0.104`，不再全 0。`eval_checkpoint.py` 新增 `mismatch_direct_delta_rms`。两个补丁后 quick100 (`tmp/same_flow_debug_train/quick100_content_seeded_v2/last.ckpt`) step100 `latent_std=0.448`，4 条 eval `lsd_db_mean=35.91,corr≈0.001,direct_corr≈0.024,prediction_rms≈0.00174`；mismatch 输出影响仍弱，representation 相对 delta 约 `1.16%`。下一步从该结构跑 300/1000 step，看 `prediction_rms/direct_rms/mismatch_direct_delta_rms/corr/SNR` 是否随训练上升；若 conditioning 长期偏弱，再增强 denoiser 层内 latent 注入或 residual prior。
- 2026-06-18 UTC content-seeded 无 prior 主线继续训练有效。quick1000 (`tmp/same_flow_debug_train/quick1000_content_seeded_v2/last.ckpt`) 10 条 1-step deterministic：`corr_mean=0.0685`、`prediction_rms=0.000777`、`direct_corr=0.1305`、`mismatch_direct_delta_rms=0.00133`。quick2000 (`tmp/same_flow_debug_train/quick2000_content_seeded_v2/last.ckpt`) 10 条 1-step deterministic：`corr_mean=0.0964`、`snr_db_mean=0.0172`、`prediction_rms=0.00129`、`direct_corr=0.1560`、`mismatch_direct_delta_rms=0.00358`。这说明 conditioning 随训练增强，但输出 RMS 仍远低于 source RMS `0.1086`，还不能算完成重建。quick2000 采样：2-step corr `0.1025` 但 SNR `-1.65`、RMS `0.0148`；4-step SNR `-2.08`，所以主诊断仍用 1-step deterministic。尝试 residual prior 分支已失败：`use_latent_prior=true` + pseudo-Huber 或 MSE prior loss 的 quick300 都让 prior 基本塌零 (`prior_rms≈0.005~0.006, prior_corr≈0.001`)，指标弱于无 prior；默认 `same-flow-overfit10.yaml` 已回到 `use_latent_prior=false, latent_prior_loss_weight=0.0`，但代码开关保留。下一步优先继续无 prior 到 3000/5000 step，或改 denoiser 层内 latent AdaLN/FiLM 注入。
- 2026-06-19 UTC 已实现 denoiser 层内 latent FiLM：`Music2LatentSameFlow(use_latent_film=true)` 在每个 down/up denoise block 前用同尺度 latent feature `d` 生成 token-wise scale/shift 调制 `x`，FiLM projection 零初始化；配置 `same-flow-overfit10.yaml` 当前默认 `use_latent_film: true`、`use_latent_prior: false`。FiLM quick2000 (`tmp/same_flow_debug_train/quick2000_latent_film/last.ckpt`) 10 条 1-step：`corr_mean=0.1250`、`prediction_rms=0.00360`、`direct_corr=0.1828`、`mismatch_direct_delta_rms=0.00725`，优于无 FiLM quick2000。FiLM 长训 `tmp/same_flow_debug_train/quick5000_latent_film_sched6000/last.ckpt` 使用 `wrapper.lr_schedule_total_steps=6000, wrapper.consistency_step_total_steps=6000`；10 条 1-step deterministic：`corr_mean=0.2762`、`snr_db_mean=0.4373`、`prediction_rms=0.01731`、`direct_corr=0.3120`、`mismatch_direct_delta_rms=0.03273`。2/4-step deterministic 增加 RMS 但 SNR 变差，主诊断仍用 1-step。部分样本已明显重建（item1 corr `0.7662`、item7 `0.6337`），但 item8/9 仍失败；目标未完成。下一步继续 FiLM 主线更长训练/听 `tmp/same_flow_eval/quick5000_latent_film_sched6000_raw_10_step1/` wav，并考虑提升模型容量或针对失败样本分析。
- 2026-06-19 UTC 已按用户要求整理可听推理样例到 `tmp/same_flow_listen/`：`best_item001_corr0.766_source_then_reconstruct.wav`、`best_item007_corr0.634_source_then_reconstruct.wav`、`mid_item004_corr0.373_source_then_reconstruct.wav`、`fail_item008_corr0.002_source_then_reconstruct.wav`，以及总览 `same_flow_best_mid_fail_source_then_reconstruct.wav`；每段都是 `source -> silence -> reconstruct`。`eval_checkpoint.py` 新增 optimal-gain 指标：当前最佳 checkpoint 重评 `tmp/same_flow_eval/quick5000_latent_film_sched6000_raw_10_step1_gain/` 得 `corr_mean=0.2782`、`prediction_rms=0.01756`、`optimal_gain_mean=2.31`、`optimal_gain_snr_db_mean=0.7622`，说明有音量偏低但不是纯音量问题。`debug_train.py` 新增 `--checkpoint/--checkpoint-variant`，当前后台续训进程 `finetune5000_latent_film_from5000_lr5e5` 从 FiLM 5000 ckpt 加载成功后仍在跑，尚未评估。
- 2026-06-19 UTC 已新建 `scripts/same_flow_debug/` 作为不破坏主线的逐步验证区。新增 `training_objective=max_denoise`、`clean_loss_type=mse|pseudo_huber`、`debug_train.py --probe-every --fixed-probe-batch`，用于固定样本/固定 latent/固定噪声/固定 `sigma=80` 逐步观察 `probe_mse/SNR/LSD/hf_ratio/RMS`。最小实验 `tmp/same_flow_debug_train/fixed_loss_g5_mse_lr1e3_cont500/last.ckpt` 已证明 local transformer 在固定噪声到干净频谱任务上能学：500 step 后 `probe_snr=17.26 dB`、`probe_mse=0.00663`、`probe_lsd=26.11`、`probe_hf_ratio=0.687`、prediction RMS `0.194` 接近 source RMS `0.202`。可听文件：`tmp/same_flow_debug_listen/fixed_noise_maxdenoise_step500_source_then_reconstruct.wav`。下一步从该最小可行点逐步放宽到固定噪声多样本、随机噪声单样本、随机噪声多样本，再回 consistency pair。
- 2026-06-18 UTC `scripts/configs/experiment/same.yaml` 临时改为 10 条 raw audio overfit list：原全量 `tmp/same/netease_spider_audio.index.list` 已在 YAML 中注释，当前使用 `tmp/same/overfit10_audio.index.list`，`data.dataset.length=10`。
- 2026-06-18 UTC `scripts/same/pretransforms.py` 新增 `Music2LatentSTFTPretransform`，按 `tmp/music2latent/audio.py` 的 real/imag STFT 表示移植到 SAME，支持 stereo `[B,C,T]`。STFT 实验配置 `scripts/configs/experiment/same-stft.yaml` 和 `same_stft_exp.yaml` 保证时间步和 `PatchedPretransform(patch_size=256)` 对齐：`hop_size=256`，因此输出 `encoded_channels=2 * 4 * 256 = 2048`，同步设置 `SAMEEncoder.in_channels=2048`、`SAMEDecoder.out_channels=2048`，总 `downsampling_ratio` 保持 `4096`。为让 STFT 表示尺度接近 waveform patch，YAML 使用 `alpha_rescale=0.65,beta_rescale=0.2,fac=4`；类默认仍保留 Music2Latent 训练口径 `beta_rescale=0.34`。PPU torch 2.7 上 complex64/float32 CUDA 路径会产 NaN，因此该 pretransform 不能使用 `torch.fft`、`torch.complex` 或 `torch.angle`；当前实现用纯实数 DFT basis + real/imag tensors 做 STFT/iSTFT，并用 `sqrt(real^2+imag^2+eps^2)` 保证零点反传稳定。bf16 AMP 下 decode 会先转 fp32；反归一化前用 `max_normalized_magnitude=100.0` 裁剪 normalized magnitude，避免训练早期 decoder 输出爆掉后 `abs ** (1/alpha)` 产生 non-finite loss。
- 2026-06-18 UTC `scripts/same/wrapper.py` 支持可选 EMA：`wrapper.use_ema=true` 时只在 generator optimizer step 后更新 EMA。`SAMECallback` demo 会保存 raw 和 EMA 两套文件，文件名后缀分别为 `_raw_input_reconstruct.wav`、`_ema_input_reconstruct.wav`；`scripts/configs/experiment/same_exp.yaml` 已显式启用 `use_ema=true, ema_beta=0.9999, ema_update_after_step=2000`。
- 2026-06-18 UTC STFT SAME 在 `bf16-mixed` 下第 0 步会让 encoder/bottleneck latents 直接 NaN；`scripts/same/wrapper.py` 的 `_forward_autoencoder()` 现在在 `torch.autocast(..., enabled=False)` 中以 fp32 跑 encode/decode，再把输出交给 loss。真实 `same_stft_exp` 模型短输入 forward smoke 已验证 decoded/latents/softnorm 全 finite。
- 2026-06-18 UTC `scripts/same/wrapper.py` 已从简化 L1/STFT 训练升级为 SAME 论文/公开 Stable Audio Tools autoencoder wrapper 的核心训练口径：Lightning manual optimization，生成器/判别器交替；`wrapper.loss_config` 必须完全来自 YAML，代码不再保留 `default_loss_config()` 或旧顶层 loss 权重覆盖入口。当前 `same.yaml` 显式写官方默认 7 尺度 `[2048,1024,512,256,128,64,32]`、75% overlap MRSTFT，`perceptual_weighting=true`、`w_sc=1.0`、`w_log_mag=1.0`、`w_lin_mag=0.0`；官方 auraloss 支持 `w_phs` phase loss 但默认 `0.0`，当前 YAML 不启用 phase 项。stereo 同时算 mid/side 和 left/right，两者共用 `spectral.weights.mrstft`；自包含 EnCodec MS-STFT discriminator 使用 relativistic paired GAN (`rpgan`) + feature matching，YAML 权重 adversarial `0.1`、feature matching `5.0`；SoftNorm 权重按官方 fallback 为 `1e-5`。因为 wrapper 使用 Lightning manual optimization，`same.yaml` 必须覆盖基础 `train.yaml` 的自动裁剪：`trainer.gradient_clip_val=null`、`trainer.gradient_clip_algorithm=null`；实际梯度裁剪走 wrapper 的 `clip_grad_norm=1.0`。验证：`py_compile`、随机 loss/disc smoke、极小 SAME autoencoder CPU Lightning `max_steps=2`、Hydra compose `experiment=same trainer.devices=8` 确认 `gradient_clip_val=None` 通过。仍未完全覆盖论文 stage/auxiliary：PQMF/chroma 多视图、latent diffusion、semantic chroma/ILD regressors、contrastive/JEPA、transformer discriminator stage 和 staged configs；细节见 `docs/same-training.md`。
- 2026-06-18 UTC `scripts/same/` 已改成纯递归 `_target_` 模型构造：`SAMEAutoencoder` 只接收已实例化的 `AudioAutoencoder`，不再有 `config_path`/`checkpoint_path`/旧 `model_config.json` 兼容构造。训练配置 `scripts/configs/experiment/same.yaml` 的 `model:` 现在是完整递归结构，不再用 `scripts.factory.load_model` 或 `ckpt_dir`；SAME-L 初始化权重走顶层 `pretrain_ckpt_path=/.../ckpt/SAME-L/checkpoint.ckpt`。`scripts/train.py` 的 `pretrain_ckpt_path` 加载逻辑已从只拷贝 `named_parameters()` 改为按 `model.state_dict()` 形状匹配加载参数和 buffer，适配 SAME 的 `autoencoder.bottleneck.running_std` 等状态。SAME-L 官方 `checkpoints/SAME-L/model.safetensors` 已一次性导出为 factory/推理可读目录 `ckpt/SAME-L/{config.yaml,checkpoint.ckpt}`，checkpoint key 加 `autoencoder.` 前缀；该目录仍可用于 `infer_office_demo.py` 或 `scripts.factory.load_model()`。默认 44.1 kHz stereo、`sample_size=196608`、downsampling `4096`、latent dim `256`。推理必须默认关掉 SAME-L 的随机核：`bottleneck.noise_regularize=false` 和所有 `mask_noise=0`；`SAMEAutoencoder.disable_inference_noise()` 与 `infer_office_demo.py` 默认已处理，`--keep-inference-noise` 仅用于复现随机训练配置。2026-06-18 排查“输出全噪声”时确认官方 `stable_audio_3.loading_utils.load_autoencoder()` 用同一 `checkpoints/SAME-L` 对 `demo_gt.wav` 1 秒重建正常（corr `0.994259`、MSE `1.202e-4`）；本地移植错误是 `TransformerResamplingBlock` 的 `sinusoidal_blocks` 判定写成 `<=`，官方是 `<`，导致 SAME-L decoder 多一层 FF 用 `sin(pi*x)` 激活。修正后本地同输入 corr `0.994266`、MSE `1.201e-4`，输出 `tmp/same-infer/local_demo_gt_reconstruct_fixed1_source_reconstruct.wav` 可听对齐官方。
- 2026-06-18 UTC 已按用户要求绕过代理直连下载 `stabilityai/SAME-L` 到 `checkpoints/SAME-L/`，包含 `model.safetensors`、`model_config.json`、README 和 license 文件；目录大小约 `3.2G`。旧 Stable Audio 3 SAME 评测 cache 仍在 `checkpoints/stable-audio-3/`。
- 2026-06-18 UTC `scripts/mossland-codec` 已新增并修正 `bottleneck_type=causal_rvq`：参考 `scripts/MOSS_Audio_Tokenizer` 的 local-causal SDPA 思路实现非 streaming 的 `LocalCausalSelfAttention/CausalLatentEncoder`，编码侧不再按旧 `spec_length` chunk 硬切，长音频整段进入 causal encoder，输出 token 数按时长线性增长；decoder 侧仍按每 25 token 分组只是为了复用原 consistency decoder 形状。当前 `mossland-codec.yaml`、`mossland-codec-rvq1024.yaml`、`mossland-codec-rvq1024-small-overfit10.yaml` 均为 48 kHz、2 秒窗口 `sample_size=96000`，`hop=768/fac=2/spec_length=62`，2 秒输出 `(B,50,32)`、4 秒输出 `(B,100,32)`，即 `32-d @ 25 Hz`。`causal_rvq_context=620` 作用在 frontend pooling 后约 62Hz 的时间步上，约 10 秒历史；注意力内部按 query chunk 分块计算局部 causal mask，避免长音频构造全长 attention mask。离散分支为 lucidrains EMA RVQ，`rvq_num_quantizers=32`、`rvq_codebook_size=1024`、`rvq_codebook_dim=null`，codes 为 `(B,Q,T)`，demo/log rate 为 `[8,16,32]`。旧 `packed_rvq` 分支仍保留用于兼容历史 checkpoint；新 `causal_rvq` 不再使用 FSQ bound 或 FSQ indexes。验证：`py_compile scripts/mossland-codec/{models,wrapper,inference,infer_demo}.py`；small overfit Hydra instantiate + 随机 `training_step().backward()` 通过；精确 2 秒/4 秒 encoder 输出 `(1,50,32)`/`(1,100,32)`；推理 helper 导出 2 秒 continuous `(32,50)`、4 秒 continuous `(32,100)`、2 秒 RVQ8 codes `(8,50)`。
- 2026-06-17 UTC 排查 `mossland-codec-rvq1024-small-overfit10` step100 后“卡住”：8 卡对照显示 no-callback、checkpoint-only、demo-only 都能到 `max_steps=150`，但 `wrapper.rvq_sync_debug_every_step=100` 单独开启会停在 TensorBoard step 99；`rvq_rank_sync_debug.jsonl` 已写出 step100 且 checksum 一致，随后不再进入下一步。结论：该调试 all_gather/日志 hook 在当前 PPU/NCCL 环境会破坏后续训练进度，不应默认开启。已把 `scripts/configs/experiment/mossland-codec-rvq1024-small-overfit10.yaml` 的 `rvq_sync_debug_every_step` 改为 `null`；组合验证 `demo_every=100 + checkpoint_every_n_train_steps=100 + sync_debug=null` 正常跑到 150。
- 2026-06-17 UTC 新增 Mossland codec packed RVQ 小模型 overfit10 配置 `scripts/configs/experiment/mossland-codec-rvq1024-small-overfit10.yaml`，配套 10 条 prepared separation index `tmp/mossland-codec/overfit10.prepared.index.list`。该配置 reconstruction-only、固定 crop、从零训练，模型缩到 `dim=256/head_dim=64/num_layers=4/num_layers_encoder=4/frontend_base_channels=32`，但保留当前 packed RVQ 核心口径 `128` 个 quantizer、`1024` 词表、`rvq_codebook_dim=null`、`rvq_quantizer_dropout=1.0/cutoff=15/multiple_of=16`，用于快速验证 continuous overfit 与 RVQ16/128 demo 行为。Hydra compose、模型 instantiate 和首个 train batch 已通过，模型约 `13.8M` 参数，batch payload `src/target=(2,4,2,66560)`。
- 2026-06-17 UTC Mossland codec RVQ demo/checkpoint mismatch 已调试：先清理了仍在运行的 `mossland-codec-rvq1024` 8 卡进程组 `1591026`，然后用 `tmp/debug_mossland_rvq_repro.py` 和 1 卡 3 step 短跑复现。结论：RVQ codes 还原本身没错，`latent_from_codes_mse=0`；同一个 checkpoint/source/RVQ32 在固定 seed 下两次 `generate_waveform()` 完全一致，MSE=0；不同 seed 的 RVQ32 输出 MSE 约 `0.039~0.064`，和 demo 段 vs 复推段差异同量级。因此训练 demo 听起来和 checkpoint 复推不一致，主要来自 `generate_waveform()` 每次从 `torch.randn_like(representation) * sigma_max` 随机起点做 one-step denoise，且长任务中 demo step/live state/checkpoint 保存时间不完全对齐；不能用未固定 seed 的拼接 demo mp3 直接判断 checkpoint 解码是否复现。调试产物保留在 `tmp/mossland-codec-infer/rvq_repro_debug_raw500_501src/`、`tmp/mossland-codec-infer/codex-debug-step1-demo-split/`、`tmp/mossland-codec-infer/codex-debug-0-1-raw-repro-step1src/`，短跑大 checkpoint 已删除，只保留 unwrap raw `tmp/mossland-codec-infer/codex-debug-0-1-raw/`。后续若要严格比较 demo 与 checkpoint，应给 demo callback 加固定 `demo_seed`，保存单段 source/codes/packed latent sidecar，并让 checkpoint 与 demo 在同一 step/state 下生成。
- 2026-06-17 UTC `mossland-codec.yaml` 数据索引已显式固定到 prepared 数据同目录：`PreparedSeparationDataset.index_file=/inspire/sj-ssd3/project/embodied-multimodality/public/Sonata/data/source_seperation/NETEASE_SPIDER/index.list`。`PreparedSeparationDataset` 首次缺索引时现在会创建 `${index_file}.lock`，只有拿到锁的进程扫描并原子写入 index，其余 rank/worker 等待索引出现后读取，避免多卡第一次启动同时递归扫共享盘；后续启动直接读 index，日志显示 `Loaded ... prepared separation items from index`。主配置 `wrapper.index_data_every_step=null`，训练中不再周期性 `rebuild_index()` 刷目录；确实需要边生成边训练时才临时改成正整数。新增参数 `index_wait_timeout_seconds` 默认 `3600`。验证：`main_env` 下 `py_compile scripts/data/datasets.py scripts/mossland-codec/models.py scripts/mossland-codec/wrapper.py`；`pytest -q tests/test_separation_dataset.py -k 'prepared_separation_dataset_reuses_existing_index_list or prepared_separation_dataset_rebuild_index_updates_item_dirs or lazily_filters'` 通过。
- 2026-06-17 UTC Mossland codec packed RVQ 的 TensorBoard 日志语义已调整：不再把 lucidrains 内部 aux loss 记录成 `rvq/commitment_loss`。当前 `BottleneckOutput.commitment_loss` 对 packed RVQ 返回 `MSE(packed_quantized, packed_continuous.detach())`，wrapper 记录为 `rvq/quantization_error`。由于正式配置 `rvq_commitment_weight=rvq_codebook_weight=rvq_distill_weight=0`，该值只用于观察 EMA codebook 对 512-d packed hidden 的重建误差，不参与主网络或 RVQ optimizer loss。之前 TensorBoard 中 `rvq/commitment_loss` 的巨大尖峰主要是 lucidrains aux loss + kmeans/EMA 早期不稳定 + smoothing 拖尾，不代表 continuous latent/std 发散。
- 2026-06-17 UTC Mossland codec 多码率 RVQ 训练改为使用 lucidrains 内置 `quantize_dropout`，不再由 wrapper 每步手动采样 `train_n_quantizers_choices`。正式配置：`rvq_quantizer_dropout=1.0`、`rvq_quantizer_dropout_cutoff_index=15`、`rvq_quantizer_dropout_multiple_of=16`，表示训练态每次 RVQ forward 随机截断到 16/32/48/.../128 一类的 prefix；wrapper `train_n_quantizers_choices=null`，demo 仍显式保存 `[16,32,64,128]`。wrapper 新增 `rvq_log_n_quantizers_choices=[16,32,64,128]`，每步用 eval/no-grad 旁路记录 `rvq/quantization_error_16/32/64/128`，不会因为日志 forward 更新 EMA codebook。
- 2026-06-17 UTC Mossland codec RVQ 分布式同步已修正：lucidrains `ResidualVQ` 默认在构造时用 `is_distributed()` 决定 `sync_codebook`，但 Lightning 模型构造通常早于 DDP 初始化，导致默认可能没有跨 rank all-reduce。`scripts/mossland-codec/quantize.py` 新增 `ResidualVectorQuantize.set_distributed_sync()`，`MosslandCodecTrainingWrapper.on_train_start()` 在 DDP 已初始化后显式开启/关闭 RVQ codebook 的 kmeans sampling、dead-code replacement 和 EMA `cluster_size/embed_sum` all-reduce。EMA 模型的 RVQ 状态也已修正：`on_before_zero_grad()` 先 `self.ema.update()` 更新 encoder/decoder EMA 权重，再把 `ema.ema_model.rvq` 的 state_dict 精确复制为 raw `model.rvq`，避免 RVQ codebook buffers 被 `ema_pytorch` 当普通 buffer 平滑；demo 的 raw/EMA 区别现在主要来自主网络权重，RVQ codebook 使用同一份最新 raw codebook。
- 2026-06-17 UTC Mossland codec RVQ 8 卡短跑已验证：用用户指定 `ckpt/mossland-codec0613/checkpoint.ckpt` warm start continuous 主网络，`rvq_quantizer_dropout=0.0` 仍支持 `n_quantizers=16/32/64/128` 多码率推理；它只是不启用 lucidrains 训练时随机截断。真正导致离散输出爆炸的是 `rvq_decay=0.8` 对 `128` 层、每层 `1024` codebook、每步每 rank 约 `10` 个 packed 512-d token 的 EMA 更新太猛。8 卡 30 step 对比：run `logs/mossland-codec/runs/2026-06-17_13-51-15` (`decay=0.8`) 的 continuous std 稳在 `0.51~0.61`，但 discrete/packed_quantized std 最高 `144076`、`rvq/quantization_error` 最高 `2.088e10`；run `logs/mossland-codec/runs/2026-06-17_13-59-14` (`decay=0.99`) 的 `rvq/quantization_error` 全程 `0.220842~0.58786`，packed_quantized std `0.186508~0.904427`，没有爆炸。正式 `scripts/configs/experiment/mossland-codec.yaml` 已把 `rvq_decay` 改为 `0.99`。
- 2026-06-17 UTC Music2Latent RVQ 已按用户要求改成最干净的旁路 EMA tokenizer：`scripts/music2latent/quantize.py` 现在包装 lucidrains `vector_quantize_pytorch.ResidualVQ`，codebook 由 EMA/kmeans 维护，不再用可训练 `nn.Embedding` codebook；`scripts/music2latent/models.py` 抽出 encoder 共享 `hidden_to_latent()`，continuous path 为 `hidden_512 -> hidden_to_latent() -> latent_64`，discrete 推理为 `codes -> quantized_hidden_512 -> hidden_to_latent() -> latent_64`，不再有单独 `quantized_out`。`scripts/music2latent/wrapper.py` 的主 consistency loss 只用 continuous latent；RVQ 只吃 `hidden.detach()`，正式/overfit 配置均为 `quantizer_codebook_dim=null`、`rvq_commitment_weight=0`、`rvq_codebook_weight=0`、`rvq_hidden_recon_weight=0`，即 RVQ 完全不通过 optimizer loss 影响主网络，只作为 EMA codebook assignment/update 旁路。正式 `music2latent.yaml` 已设 `rvq_latent_train_prob=0.0` 并暴露 `quantizer_decay/kmeans_init/kmeans_iters/threshold_ema_dead_code`；新增 `scripts/configs/experiment/music2latent-ema-rvq-overfit10.yaml` 用 10 条 raw audio index、较小模型和 `[2,4,8]` RVQ 多码率做验证。DDP unused 参数报错来源已确认并修复：`Decoder` bottleneck ResBlock 曾创建未使用的 `proj_emb`，因为 `Decoder.forward()` 不传 `time_emb`；现已不再给这些 ResBlock 传 `cond_channels`。验证：`py_compile`；Hydra instantiate；随机 batch `training_step().backward()`；unused grad count 为 0；`encode(... return_codes=True, n_quantizers=4)` 得到 int64 codes，`decode_codes()` 回 `[1,2,65536]`。
- 2026-06-17 UTC `scripts/mossland-codec` 已按同一思路改为 packed embedding EMA RVQ 旁路：`scripts/mossland-codec/quantize.py` 包装 lucidrains `ResidualVQ`，不再用可训练 `nn.Embedding` codebook；正式 `mossland-codec.yaml` 为 `rvq_codebook_dim=null`，每个 chunk 的 `[128,4]` latent pack 成 512-d embedding 直接 EMA/kmeans 聚类，`rvq_commitment_weight=rvq_codebook_weight=rvq_distill_weight=rvq_latent_train_prob=0`。wrapper 对 `packed_rvq` 仍调用 `encode_bottleneck(... quantize=True, detach_encoder=True)` 以更新/记录 RVQ codes，但训练 latents 永远取 `rvq_encoded.continuous`，RVQ loss 不加入 optimizer loss。正式参数 smoke 通过：`packed_dim=512`、RVQ `project_in/project_out=Identity`、`codes=(1,16,1)` int64、losses 全 0、`latent_from_codes(codes)` 回 `(1,128,4)`。
- 2026-06-17 UTC 新增快速 RVQ 词表实验配置 `scripts/configs/experiment/mossland-codec-rvq1024.yaml`，完整复制主 `mossland-codec.yaml`，只把 `project_name/tags` 改为 `mossland-codec-rvq1024/rvq1024`，并把 `model.rvq_codebook_size` 从 `15000` 降为 `1024`，`rvq_num_quantizers` 仍为 `128`、`rvq_codebook_dim=null`、多码率日志/demo 仍为 `[16,32,64,128]`。用途是验证较小词表是否显著加快 packed 512-d EMA RVQ 训练；Hydra compose 和模型 instantiate 已通过。
- 2026-06-17 UTC `scripts/mossland-codec/infer_demo.py` 已适配 packed RVQ 推理：默认用当前仓库路径、`ckpt/mosslandcodec0617` 和 prepared `mixture.mp3`，一次保存 `reconstruct_continuous.mp3`、`reconstruct_rvq16.mp3`、`reconstruct_rvq128.mp3`。`scripts/mossland-codec/inference.py` 支持 `encode(..., discrete=True, n_quantizers=N)` 输出 `[N,chunks]`/`[B,N,chunks]` RVQ codes；`quantize.py` 新增 codes-only encode，并把 `from_codes()` 改为按实际 codebook 层逐层累加，避免 16 层推理也 stack 全 128 层 codebook 造成数 GB 额外显存。验证：`py_compile scripts/mossland-codec/infer_demo.py scripts/mossland-codec/inference.py scripts/mossland-codec/quantize.py scripts/mossland-codec/models.py`；`infer_demo.py --duration-seconds 0.2` 和默认 6 秒均成功保存三路 mp3 到 `tmp/mossland-codec-infer*`。
- 2026-06-17 UTC Mossland codec demo callback 输出已收敛为每个样本只保存顺序拼接 mp3，不再写单独 `quantized16/32/64/128/continuous` wav。若 `use_ema=true` 且 wrapper 有 EMA，则 raw model 和 EMA model 各保存一个文件，文件名带 `_raw_...mp3` / `_ema_...mp3`，内容均为 `src -> target -> quantized16 -> quantized32 -> quantized64 -> quantized128 -> continuous`。
- 2026-06-17 UTC 用户发现 `logs/mossland-codec/runs/2026-06-17_13-20-23/demos/1_1_reconstruct_rank6_quantized16/32...wav` 低码率混入其它音乐。诊断：该 run 的 RVQ 是新加 EMA codebook，旧 `pretrain_ckpt_path=ckpt/mosslandcodec0617/checkpoint.ckpt` 没有训练好的 RVQ codebook；step 1/11 demo 时 codebook 只经过极少量 local batch 更新。旧 demo callback 还在训练模式下用 EMA model 做 quantized 推理，可能让 demo 自己更新 codebook，且 EMA model 的 RVQ codebook 不一定是训练中 raw model 的最新 EMA codebook。已修：demo 推理期间对 raw/EMA model 调 `eval()` 并恢复原 training 状态，quantized 多码率固定用 `module.model` raw training model 的 RVQ codebook，continuous 仍按 `use_ema` 可用 EMA model。验证：`py_compile scripts/mossland-codec/wrapper.py`。
- 2026-06-17 UTC 128 rank Music2Latent run 遇到的 `Gloo connectFullMesh failed` 栈位于 Lightning `prepare_data()` 的 `_InfiniteBarrier()`，不是 NCCL 初始化或 RVQ decoder 本身。`scripts/train.py` 已改为在入口显式 `data.setup()` 后构造 `train_dataloader/val_dataloader` 并传给 `trainer.fit(..., train_dataloaders=..., val_dataloaders=...)`，不再把 `datamodule=data` 交给 Lightning，因此不会为 no-op `Experiment_Dataset.prepare_data()` 额外创建 128 rank Gloo full-mesh group。Lightning 仍会接管 dataloader 的分布式 sampler 注入。
- 2026-06-17 UTC 用户提出 `mossland-codec` 后续 RVQ 方向：保留当前 `mossland-codec.yaml` 的 summary-token 模型主体，但把每个 audio chunk 的 `num_latents=128` 个 summary embeddings pack/concat 成一个时间步，连续 latent 存储为 `[chunk, 128*bottleneck_channels]`；离散分支改为 packed-vector RVQ，codes 形如 `[B, n_quantizers, num_chunks]`，例如 128 层 RVQ 表示一个 chunk 只有一个离散时间步。该设计不要求改 encoder/decoder 主体参数，只需要在 bottleneck/inference/wrapper 增加 pack/unpack 和 RVQ 训练逻辑；仍需按 chunk 边界 seek，不能从半个 chunk 内开始。
- 2026-06-17 UTC `mossland-codec` 的 `packed_rvq` 仍必须保留 FSQ 的 `dont_quantize()` bound 作为 continuous latent 归一化层，但不使用 FSQ indexes 作为离散瓶颈。原因是旧 Mossland codec decoder 是在 bounded FSQ continuous latent 分布上训练的；若 `use_fsq=false` 后完全移除 FSQ，旧 checkpoint 非严格 warm start 的 continuous demo 也会坏。当前实现中 `bottleneck_type=packed_rvq` 会创建 `self.fsq` 用于 bound，RVQ 再量化 packed `[B,512,chunks]` bounded latent；FSQ buffers 设为 non-persistent，避免新旧 checkpoint 因 `_levels_tensor/_basis_tensor` 严格加载不匹配。
- 2026-06-17 UTC Music2Latent wrapper 补齐 mossland-codec 同类训练日志和 LR schedule：新增 `optimizer_name`、`weight_decay`、`lr_schedule`、`lr_warmup_steps`、`lr_schedule_total_steps`，默认 `adamw + cosine_decay`、`10000` step linear warmup、总步数 `2000000`。TensorBoard 日志已按功能分组，不再全塞 `train/`：`loss/total`、`loss/consistency`、`loss/rvq`；`sigma/*`；`latent/selected_mean/std`、`latent/continuous_mean/std`、`latent/discrete_mean/std`、`latent/source_discrete`；`rvq/*`；`optim/lr`；`grad_norm/*`。`scripts/configs/experiment/music2latent.yaml` 已同步这些参数，并再次固定 `data.dataset.length: 10000000`。
- 2026-06-17 UTC Music2Latent demo decode 步数已改为 4：`Music2LatnetTrainingCallback` 新增 `denoising_steps` 参数，默认 `4`，并在 continuous 与各多码率 discrete decode 时传入；`scripts/configs/experiment/music2latent.yaml` 的 `callbacks.demo_callback.denoising_steps` 显式设为 `4`。
- 2026-06-17 UTC `SampleDataset` 的长音频跳过逻辑已确认生效：`music2latent` 当前配置 `max_duration_seconds=300`，随机抽样 NETEASE_SPIDER 120 条有 18 条 `>=300s`；强制把一条 1333.616s mp3 放到 `filenames[0]` 会先打印 skip 并返回下一条短音频。为避免 DataLoader worker stdout 块缓冲导致训练日志里迟迟看不到 skip 信息，`scripts/data/datasets.py` 已把 long-audio skip 改成 `print(..., flush=True)`，消息包含 `duration_seconds`，并同步写入 `dataloading_info.log`。正在运行的旧训练进程不会自动加载该改动，需要重启训练或重建 worker。
- 2026-06-16 UTC Music2Latent 正式 stereo/RVQ 路径已按用户要求更新：`scripts/music2latent/audio.py` 支持 stereo waveform `[B,2,T] -> [B,4,F,T]` 与 inverse 回 `[B,2,T]`；`scripts/configs/experiment/music2latent.yaml` 设 `data.num_channels=2, no_channel_dim=false, model.data_channels=4`。`scripts/music2latent/models.py` 在 encoder 512-d hidden (`bottleneck_base_channels`) 上新增自包含 RVQ，不引用 `tmp/` 或其它项目目录；连续 64-d latent 仍是主路径，离散分支经 `quantized_out` 映射回 64-d latent。wrapper 每 step 只跑一次 consistency decoder：先得到 continuous/RVQ latent，再按 `rvq_latent_train_prob=0.1` 选择本 step 的 latent 来源为 continuous 或 discrete；另加 RVQ commitment/codebook/distill loss，`rvq_detach_encoder=true`。离散表征支持多码率：训练默认 `train_n_quantizers_choices=[4,8,16,32]` 每步采样激活码本数，`encode(..., quantize=True, n_quantizers=N)` 和 `encode(..., return_codes=True, n_quantizers=N)` 可导出对应码率，`decode_codes(codes)` 可从 codes 重建。demo callback 默认保存单个 stereo 顺序对比 mp3 `${step}_${idx}_rank{rank}_gt_discrete4_8_16_32_continuous.mp3`，内容为 `gt -> discrete@4 -> discrete@8 -> discrete@16 -> discrete@32 -> continuous`，段间 0.25s 静音；callback 进入 demo 前和 `finally` 退出后都会 `gc.collect()` + `torch.cuda.empty_cache()`，避免 demo 解码临时显存和训练显存叠加。验证通过：`py_compile`；小模型 smoke 覆盖 RVQ 多码率 training_step/backward、codes int64、`decode/decode_codes` 回 `[B,2,T]`、demo 输出 2-channel mp3 可读回；Hydra model/wrapper instantiate 显示 `data_channels=4, quantizers=32, train choices=[4,8,16,32]`。
- 2026-06-16 UTC `scripts/configs/experiment/music2latent.yaml` 的 `SampleDataset` 已设 `max_duration_seconds: 300`，超过或等于 5 分钟的 raw audio 会先用 `ffprobe` 读容器 duration metadata，超限则从当前 worker 的 `filenames` 移除并取下一条，不进入 `load_file()` / `librosa.load()` 解码。`scripts/data/datasets.py` 的 `SampleDataset` 新增该参数和 duration cache，并修复文件列表被跳过/删除到空时的异常边界。验证：`py_compile scripts/data/datasets.py`；Hydra instantiate 显示 `dataset.max_duration_seconds=300.0`；monkeypatch smoke 证明第一条 long mp3 会在读入前跳过，第二条 short mp3 才进入 load。
- 2026-06-16 UTC `SampleDataset` 新增可配置 `length` 参数，默认保持旧值 `1000000`；`scripts/configs/experiment/music2latent.yaml` 显式设 `data.dataset.length: 10000000`，因此该实验 `len(dataset)=10000000`。验证：`py_compile scripts/data/datasets.py`；Hydra instantiate 显示 `cfg length=10000000, dataset.length=10000000, len(dataset)=10000000`。
- 2026-06-16 UTC 按用户要求把 `facebookresearch/dacvae` 和 `descriptinc/descript-audio-codec` 源码 clone 到 `tmp/dacvae` 与 `tmp/descript-audio-codec`，并新增 `scripts/moss_audio_flowcodec/`。新 codec 是无判别器直接自编码器：48 kHz stereo waveform 走 DAC-style 1D conv encoder/decoder，总 stride `1920`，所以 `2*48000 -> 25 Hz`；离散瓶颈用 RVQ `num_quantizers=32` 输出 `codes=[B,32,T/1920]`，`codebook_dim` 独立可配、不等于也不绑定 32；连续瓶颈输出 `continuous=[B,32,T/1920]`，两条瓶颈各自 projection 后共享 decoder。新增配置 `scripts/configs/experiment/moss-audio-flowcodec.yaml` 和 `moss-audio-flowcodec-overfit10.yaml`；正式配置已扩到约 `300.28M` 参数（`encoder_dim=76, latent_dim=1024, decoder_dim=1024`），overfit10 小配置保持不变。wrapper 已支持变码率训练参数 `train_n_quantizers` / `train_n_quantizers_choices`，推荐用 `train_n_quantizers_choices=[4,8,16,32]` 训练同一 checkpoint 支持多档离散码率，推理时传 `n_quantizers` 选择码本数；`quantizer_dropout` 保留为 DAC 原版随机 dropout。`MultiScaleSTFTLoss` 内部必须把 audio/window 转 float32，因为 CUDA cuFFT 不支持 bf16 STFT；外层仍可用 Lightning `bf16-mixed`。demo callback 默认只保存 `${step}_${idx}_rank{rank}_input_discrete_continuous.wav`，不再写分开的 input/discrete/continuous wav 或 token pt；需要旧行为时设置 `save_separate_files=true` 或 `save_tokens=true`。验证通过：`py_compile`、随机 forward/backward 输出 `codes=(2,32,25), continuous=(2,32,25)`、wrapper `training_step().backward()`、Hydra instantiate 显示 `downsample_ratio=1920, token_rate=25.0, num_quantizers=32, continuous_dim=32`，变码本 smoke 覆盖 `n_quantizers=4/8/16/32`，bf16 autocast 下 wrapper `training_step().backward()` 通过，callback smoke 只生成 `201_0_rank7_input_discrete_continuous.wav`。
- 2026-06-16 17:24-17:33 UTC 按用户要求同时用 4 卡对比 `music2latent-overfit10.yaml` 与 `music2latent-overfit10-moss-tokenizer-flow.yaml`，命令均追加 `+trainer.max_steps=2000`、`trainer.log_every_n_steps=10`、`callbacks.demo_callback.demo_every=100000000`，Music2Latent 用 GPU 0-3，Flow 用 GPU 4-7。旧 run `logs/music2latent-overfit10-moss-tokenizer-flow/runs/2026-06-16_17-03-00` 只剩 orphaned rank 2-7 占卡，已清理后重跑。新 run：`logs/music2latent-overfit10-fast/runs/compare-4gpu-20260616-172407-pty` 与 `logs/music2latent-overfit10-moss-tokenizer-flow/runs/compare-4gpu-20260616-172407-pty`。TensorBoard loss 对比：Music2Latent 0-500/500-1000/1000-1500/1500-2000 step mean 分别 `0.02336/0.01048/0.00745/0.00759`，最后 `0.00180`；Flow 分别 `0.02695/0.00561/0.12897/0.54282`，最后 `0.10665`，并在 1000 step 后明显发散。因此当前 Flow 与 Music2Latent 训练行为不一致，Flow 的 late training instability 是下一步优先排查点。
- 2026-06-16 14:05 UTC 按用户要求把 `scripts/MOSS_Audio_Tokenizer_Flow` 重新写成“只替换模型内部架构”的 Music2Latent-compatible 版本：数据流、STFT representation、pseudo-Huber loss 和 demo encode/decode 语义都参考 `scripts/music2latent`，不沿用旧 Flow 的 residual consistency/RVQ/patch-refiner/采样器实验结论，也不保留显式 `prior_tokens` 分支。该初版后来已被 14:45 的 staged `MossAudioTokenizerPatchedPretransform` 版本取代；2026-06-17 起 wrapper 不再引用已删除的 `scripts.music2latent.consistency`，而是复用 `scripts.music2latent.training_base` 工具。
- 2026-06-16 14:18 UTC 为让 `music2latent-overfit10-moss-tokenizer-flow.yaml` 的压缩行为更接近 `music2latent`，将 `spec_frames_per_token` 从 `1` 改为 `8`，因此 Flow latent 时间长度从 `T` 变成 `T/8`；同一 `100000` sample 输入会裁到 `98304`，STFT 为 `192` frames，latent 为 `24` tokens。将 `context` 从 `3` 改为 `4`，在 8-frame token 下跨 encoder/latent-conditioner/denoiser 12 层传播约 `37*8` 个 STFT frames（约 `3.44s`），接近 `music2latent` 主 denoiser 路径约 `3.35s` 的时间感受野。验证：Hydra instantiate 显示 `spec_frames_per_token=8, context=4`；小模型 forward/backward 输出 `rep=(2,2,1024,192), lat=(2,24,16)`；wrapper `training_step().backward()` loss 有限。
- 2026-06-16 14:30 UTC 按用户要求 `music2latent-overfit10-moss-tokenizer-flow.yaml` 支持双声道且压缩率为 `128x`：数据集改为 `num_channels=2, no_channel_dim=false`；Flow 模型改为 `number_channels=2, data_channels=4`（L/R 的 real/imag 展平成 4 个 representation channels），`continuous_dim=256`。在 `spec_frames_per_token=8` 下，stereo token 输入维度为 `4*1024*8=32768`，latent 为 `256`，压缩率为 `128x`。`MossAudioTokenizerFlow.to_representation_encoder()` / `to_waveform()` 已支持 `[B,2,L] <-> [B,4,F,T]`。验证：Hydra instantiate 显示 `data_channels=4, spec_frames_per_token=8, latent_dim=256, ratio=128.0`；小模型 `crop=(2,2,98304), rep=(2,4,1024,192), lat=(2,24,256)`；wrapper stereo `training_step().backward()` loss 有限。
- 2026-06-16 14:45 UTC 继续按用户要求参考 MOSS 原版同功能算子，把 `MossAudioTokenizerFlow` 的一次性 `spec_frames_per_token=8` flatten 改成 staged `MossAudioTokenizerPatchedPretransform(patch_size=2)` x3。当前 encoder/denoiser 都是 `T -> T/2 -> T/4 -> T/8`，decoder/denoiser up path 反向 `T/8 -> T/4 -> T/2 -> T`，与 `music2latent` 的 3 次 temporal stride-2 下采样/上采样行为对齐；`spec_frames_per_token=8` 现在只表示总 patch/downsample ratio 和 wrapper 裁剪约束。配置显式写 `patch_sizes: [2, 2, 2]`，stereo 压缩率仍为 `4*1024*8 -> 256 = 128x`。验证：默认 Hydra instantiate 显示 `patch_sizes=[2,2,2], spec_frames_per_token=8, latent_dim=256, ratio=128.0`；小模型 staged forward/decode/backward 输出 `rep=(2,4,1024,192), lat=(2,24,256), wav=(1,2,98304)`；wrapper `training_step().backward()` loss 有限。
- 2026-06-16 15:05 UTC 按用户要求继续补齐与 `music2latent` 的前三个主要差异：`MossAudioTokenizerFlow` 新增多尺度 latent conditioner，从 bottleneck latent 经 `PatchedPretransform` up path 生成 `T/8,T/4,T/2,T` condition pyramid，并在 denoiser 每个 stage 注入；新增每个 stage 的 sigma condition projection，不再只在 bottleneck 注入 sigma；新增轻量 2D `SpectralResidualBlock` 输入/输出 frontend/head（配置 `spectral_frontend_channels=16`），在 token flatten 前后保留频率-时间局部卷积归纳偏置但不改变压缩率。验证：小模型 `condition_latent_pyramid()` 输出 `[(2,192,64),(2,96,64),(2,48,64),(2,24,64)]`；forward/decode/backward 和 wrapper `training_step().backward()` loss 有限；默认 Hydra instantiate 显示 `patch_sizes=[2,2,2], spectral=16, ratio=128.0`。
- 2026-06-16 15:20 UTC 解释并修复 Flow demo 保存格式：旧 `MossAudioTokenizerFlowCallback` 在双声道且 `silence_seconds=0` 时把 source `[2,L]` 和 recon `[2,L]` 沿 channel 拼成 4-channel wav，例如 `logs/music2latent-overfit10-moss-tokenizer-flow/runs/2026-06-16_16-57-20/demos/901_0_rank0.wav` 是 `num_channels=4`，前两个 channel 是 source stereo，后两个 channel 是 recon stereo，不是顺序 A/B。callback 现默认 `silence_seconds=0.25`，每次 demo 写 `${step}_${i}_rank{rank}_source.wav`、`..._recon.wav`，以及 `${step}_${i}_rank{rank}.wav` 作为 2-channel stereo 顺序 A/B（source + silence + recon）。验证：`py_compile`；torchaudio smoke 显示新 comparison wav 为 `channels=2`。
- 2026-06-16 15:35 UTC 针对用户反馈 Flow loss 很低但 demo 没重建出来，修复一个与 `music2latent` 的关键采样不一致：旧 Flow `decode()` 忽略 `denoising_steps`，只从 `sigma_max` 随机噪声一步出图；现在 `MossAudioTokenizerFlow.decode()` 新增与 `music2latent._decode_to_representation()` / `_reverse_diffusion()` 同形的 Karras sigma reverse diffusion，支持 `denoising_steps`。`MossAudioTokenizerFlowCallback` 也接入 `denoising_steps` 参数并传给 `model.decode()`。验证：`py_compile`；小模型 `decode(lat, denoising_steps=1/2)` 均输出 `(1,2,16384)`。
- 2026-06-16 13:25 UTC 针对用户反馈 `music2latent-overfit10.yaml` 跑 20 分钟仍有明显噪声，将该配置改成快速验证模型/训练方式能否记忆固定 10 条：`sample_size=32768`（64 个 STFT frames，比 100000 快约 3 倍且满足 8-frame 下采样整除）、`random_crop=false`（固定每首开头片段，不再从 10 首歌无限随机抽不同 crop）、`val_split=null`、`num_workers=2`、`use_ema=false`（demo 直接听 raw model，避免旧 wrapper 的 EMA 2000 step 后才更新导致早期误判）、`learning_rate=3e-4`、`consistency_total_step=5000`、`demo_every=100`、`checkpoint_every_n_train_steps=500`。同时修复 `scripts/music2latent/wrapper.py`：`use_ema=false` 时不再调用不存在的 `self.ema.update()`，demo callback 会在无 EMA 时使用 `module.model`。验证：`py_compile` 通过；OmegaConf 加载确认上述关键参数；10 行 index 校验通过。
- 2026-06-16 13:18 UTC 按用户要求新增 `scripts/configs/experiment/music2latent-overfit10-local-transformer.yaml`，作为 `music2latent-overfit10.yaml` 的 local transformer 版本。新配置复制原 overfit10，只改 `project_name=music2latent-overfit10-local-transformer`、tags 加 `local_transformer`、`model.block_type=local_transformer`，其它数据、训练、wrapper、callback 和模型参数保持一致，`local_transformer_window=3`。验证：Hydra compose 对比 `data` 完全相同，`model` 除 `block_type/local_transformer_window` 外相同；`hydra.utils.instantiate(local.model)` 成功，得到 `Music2Latent block_type=local_transformer window=3`。
- 2026-06-16 12:35 UTC 按用户要求基于 `scripts/configs/experiment/music2latent.yaml` 新增正式模型的 overfit10 配置 `scripts/configs/experiment/music2latent-overfit10.yaml`。除 `project_name/tags` 和数据集新增 `index_file: tmp/music2latent/overfit10.sample.index.list` 外，模型、wrapper、callback、batch、num_workers 等保持正式配置不变。注意旧 `tmp/music2latent/overfit10.index.list` 当前实际有 8417628 行，不是 10 条；已新增 `tmp/music2latent/overfit10.sample.index.list`，内容是大 index 前 10 条 raw mp3。验证：OmegaConf 可加载配置，`SampleDataset` 从新 index 加载 `10` 个 filenames；`SampleDataset.__len__` 仍返回逻辑 epoch 长度 `1000000`，实际 `__getitem__` 对 10 条取模。
- 2026-06-16 12:20 UTC 修复正式 `experiment=music2latent` 在 8 卡训练第一步报 `x` 195 frames 与 decoder pyramid `d` 200 frames 不能相加的问题。原因是当前 `scripts/music2latent/audio.py` 的 reflect padding 下，STFT frame 数约为 `floor(samples / hop)`；旧 wrapper 裁剪公式来自 `samples = hop * (8k + 3)` 假设，会把 `sample_size=100000` 裁成 `99840`，产生 `195` frame，而 3 次 stride-2 下采样/上采样会回到 `200` frame。`scripts/music2latent/wrapper.py` 现改为按 `cropped_frames = (samples // hop) // downscaling_factor` 裁剪，正式配置 `100000 -> 98304` samples、`192` frames，能被时间下采样倍率 `8` 整除。验证通过：`python -m py_compile scripts/music2latent/wrapper.py scripts/music2latent/models.py scripts/music2latent/audio.py`；小模型 smoke 对 `100000` 长度 batch 执行 `training_step` 返回有限 loss。
- 2026-06-16 12:00 UTC 解决 `scripts/configs/experiment/music2latent-mini-overfit10.yaml` 训练被原始音频读取卡住的问题：新增 `scripts/music2latent/prepare_pt_cache.py`，用 `python -m scripts.music2latent.prepare_pt_cache --index-file tmp/music2latent/overfit10.index.list --output-dir tmp/music2latent/overfit10_pt --max-items 10 --sample-size 441000 --sample-rate 44100 --num-channels 2` 从 index 前 10 行预读取音频并保存为 10 个 `.pt`，每个 payload 主键为 `audio`，并写 `manifest.jsonl` 记录源路径和 shape。`music2latent-mini-overfit10.yaml` 已从 `SampleDataset` 切到 `PTDataset(folder_path=tmp/music2latent/overfit10_pt, mainkey=audio, target_length=441000, sub_length=10, nums=1000000)`，训练热路径现在直接 `torch.load` 这 10 个 `.pt`。同时修复 `PTDataset` 在样本长度刚好等于 `target_length` 时随机/非随机裁剪都会报错的边界问题。验证通过：`python -m py_compile scripts/data/datasets.py scripts/music2latent/prepare_pt_cache.py scripts/music2latent/wrapper.py scripts/music2latent/models.py`；`python -m pytest -q tests/test_pt_dataset.py`；Hydra dataloader smoke 显示 batch `(1,2,441000)`；真实入口单卡 1 step `python -m scripts.train experiment=music2latent-mini-overfit10 trainer.devices=1 trainer.max_steps=1 callbacks.demo_callback.demo_every=999 +trainer.enable_progress_bar=false` 成功结束。
- 2026-06-16 11:00-11:15 UTC 按用户要求把 `tmp/music2latent-training/music2latent-training` 和 Music2Latent 论文/官方训练代码整理进当前 Hydra/Lightning 训练框架，新增 `scripts/music2latent/`。实现是自包含版本，不再使用官方全局 `hparams.py` 或自带 DDP loop：`audio.py` 提供 complex STFT real/imag 表示、`alpha_rescale=0.65/beta_rescale=0.34` 归一化和 iSTFT；`models.py` 提供 Music2Latent-style consistency autoencoder，包含 2D encoder、64-d continuous bottleneck、latent pyramid decoder、frequency-wise attention、frequency learned scaling、U-Net cross connections 和 EDM consistency `c_skip/c_out/c_in`；`wrapper.py` 提供 Lightning wrapper、EMA、RAdam/AdamW、cosine warmup、consistency step schedule、pseudo-Huber loss 和 demo callback；`inference.py` 提供从 run `.hydra/config.yaml` + Lightning checkpoint 加载的 `EncoderDecoder`。新增配置 `scripts/configs/experiment/music2latent.yaml`（正式参数接近官方默认：`hop=512/fac=4/data_length=64/base_channels=64/latent_dim=64`）和 `music2latent-mini-overfit10.yaml`（5.76M 参数小模型）。验证通过：`py_compile`；Hydra instantiate；随机 batch `training_step().backward()`；`generate_waveform()` 输出 `(1,2,34304)`，latents `(2,64,8)`；真实入口单卡 1 step `python -m scripts.train experiment=music2latent-mini-overfit10 trainer.devices=1 trainer.max_steps=1 callbacks.demo_callback.demo_every=1` 成功结束并写出 `logs/music2latent-mini-overfit10/runs/2026-06-16_11-12-07/demos/1_0_src_generated.wav`。长度约束后来按当前 `audio.py` 修正：默认 `hop=512/fac=4/freq_downsample_list=[1,0,0,0]` 时，训练 wrapper 应让 STFT frame 数能被时间下采样倍率 `8` 整除；正式配置 `sample_size=100000` 会裁到 `98304` samples、`192` frames。模型新增 `fixed_training_length`，训练默认固定 `data_length`，设为 `false` 时会裁到最近合法长度。
- 2026-06-16 10:23-10:45 UTC 继续对齐 Flow 与 `scripts/mossland-codec` 的 2D frontend 差异。给 Flow decoder 新增可选 `PatchwiseSpectralAdapter`：在 `LocalConsistencyDenoiser` 输入侧把 `noisy_residual * c_in` reshape 成 `[stft_channels, stft_bins, spec_frames_per_token]`，做轻量 cond-FiLM Conv2d + mean pool，生成每个 12.5Hz token 的 embedding 加到 local transformer input；`wrapper` 的 `patch_refiner_lr_scale` 也覆盖 `decoder.patch_*` 参数。该 adapter 默认关闭，mini/formal 配置当前明确 `patch_input_channels=0, patch_input_layers=0`。实验中临时打开 mini `16x1` 并 from-scratch 跑到 3000：run `logs/moss-audio-tokenizer-flow-mini-overfit10/runs/2026-06-16_10-23-32/`，fixed10 2-step `0-3000.ckpt` continuous/discrete `snr=13.200/12.680`、`corr=0.952/0.948`、`lsd=18.883/19.084`、`quiet=-49.511/-48.689`。对比当前稳定版 3000 `snr=12.925/12.653`、`lsd=18.778/18.875`、`quiet=-50.249/-49.624`：continuous SNR 只小幅增加，discrete 基本无收益，LSD/quiet 变差。结论：token-level pooled input adapter 不足以复现 `mossland-codec` 的 2D frontend 优势，不作为默认；后续若继续追结构，应实现更接近 `mossland-codec` 的完整 2D down/up path 和 multiscale feature，而不是简单 pooled embedding。
- 2026-06-16 09:50-10:18 UTC 继续验证 Flow patch refiner 稳定性。`eval_checkpoint.py` 新增 `--override`，可在离线评估时用 OmegaConf dotlist 覆盖模型配置（例如 `--override model.patch_refine_residual_scale=0.5`），便于不重训测试解码超参。对稳定版 `09-27-24/0-7500.ckpt` 直接把 `patch_refine_residual_scale` 从训练值 `0.25` 改成 `0.5` 或 `0.0` 都会严重抬谱底：`scale=0.5` fixed10 continuous/discrete `snr=12.901/12.743`、`lsd=33.844/34.047`、`quiet=-22.045/-21.726`；`scale=0.0` 类似，说明 refiner scale 是训练-推理解码尺度的一部分，不能事后随意调。随后 from-scratch 跑 `model.patch_refine_residual_scale=0.5` run `logs/moss-audio-tokenizer-flow-mini-overfit10/runs/2026-06-16_09-50-22/`，单条 demo 早期涨得更快，但 3000 后 loss/grad 开始不稳，3500 demo 回落；已在 3500 后手动停止。fixed10：`0-3000.ckpt` continuous/discrete `snr=13.242/12.872`、`corr=0.960/0.956`、`lsd=20.076/20.200`、`quiet=-46.337/-46.039`，比 `scale=0.25` 的 3000（`snr=12.925/12.653`、`lsd=18.778/18.875`、`quiet=-50.249/-49.624`）只有轻微 SNR 优势但底噪差很多；`0-3500.ckpt` 退化到 `snr=10.125/9.808`、`lsd=21.643/21.885`、`quiet=-42.233/-41.587`。结论：`scale=0.5` 不是好方向。为后续实验给 `PatchwiseSpectralRefiner` 增加可选 `patch_refine_residual_clip`（tanh soft clip，默认 `0.0` 关闭，并在 mini/正式配置显式写入），但对已有 `scale=0.5` 3000 checkpoint 做推理侧 `clip=0.5/1.0/2.0` 都更差，clip 不能修复已训练权重；若使用 clip 必须从零训练验证。目前推荐仍是 `patch_refine_residual_scale=0.25, patch_refine_residual_clip=0.0`。
- 2026-06-16 08:51-09:45 UTC 继续调试 `scripts/MOSS_Audio_Tokenizer_Flow` mini overfit10。为处理 patch-refiner 早中期有效但 9000+ collapse 的问题，给 `PatchwiseSpectralRefiner` 增加固定 `patch_refine_residual_scale`，mini/正式配置设 `0.25`；wrapper optimizer 增加 `patch_refiner_lr_scale` 和 `quantizer_lr_scale` 参数组，mini/正式配置设 `0.25/0.5`；mini 配置把 `quantizer_loss_weight` 从 `0.1` 降到 `0.05`，`lr_schedule_total_steps` 改为 `7500`。验证：`py_compile` 通过，Hydra instantiate + 小输入 `training_step().backward()` 通过。8 卡 from-scratch run `logs/moss-audio-tokenizer-flow-mini-overfit10/runs/2026-06-16_08-51-57/` 跑到 `0-5000.ckpt` 后 fixed10 2-step：continuous/discrete `snr=14.953/14.672`、`corr=0.967/0.966`、`lsd=18.079/17.948`、`quiet=-51.118/-51.145`；1-step SNR 略高但 `lsd/quiet` 更差。随后从 `0-5000.ckpt` resume 到 `logs/moss-audio-tokenizer-flow-mini-overfit10/runs/2026-06-16_09-27-24/0-7500.ckpt`，训练自然结束且无旧 collapse 尖峰；fixed10 deterministic 2-step：continuous/discrete `snr=15.451/15.239`、`corr=0.971/0.970`、`lsd=16.859/16.971`、`quiet=-53.680/-53.015`；1-step：`snr=15.657/15.432` 但 `lsd=18.011/18.041`、`quiet=-51.131/-50.681`，所以仍推荐 2-step。结论：稳定版 SNR 低于旧全强度 patch-refiner best 约 1 dB，但 LSD/quiet 更好、后期更稳；当前优先听 `09-27-24/demos/7500_0_{continuous,discrete}_src_generated.wav` 和 fixed10 wav 目录 `tmp/moss-flow-eval-20260616-092724-step7500-stable-refiner/`。
- 2026-06-16 07:31-08:43 UTC 针对 Flow 与可训通 `scripts/mossland-codec` 的主要结构差异，给 `scripts/MOSS_Audio_Tokenizer_Flow/models.py` 新增可选 `PatchwiseSpectralRefiner`：decoder denoise 后把每个 12.5 Hz token reshape 成 `[stft_channels, stft_bins, spec_frames_per_token]` 小 2D patch，用 cond-FiLM 的轻量 Conv2d residual refine；默认关闭以兼容旧 ckpt，mini 配置打开 `patch_refine_channels=16, patch_refine_layers=2`，正式配置打开 `32/2`。验证：`py_compile` 通过，Hydra instantiate + 小输入 forward/backward 通过，wrapper `training_step()` + backward 通过。mini patch-refiner from-scratch run `logs/moss-audio-tokenizer-flow-mini-overfit10/runs/2026-06-16_07-31-11/` 到 `0-3000.ckpt` fixed10：continuous/discrete `snr=13.475/13.170`、`corr=0.957/0.955`、`lsd=18.751/18.783`、`quiet=-51.694/-51.125`，相对无 refiner 3000 的 `snr=12.984/12.888`、`lsd=21.022/21.090`、`quiet=-44.930/-44.797` 大幅降底噪。续训 run `logs/moss-audio-tokenizer-flow-mini-overfit10/runs/2026-06-16_07-53-10/0-5000.ckpt` fixed10：continuous/discrete `snr=16.098/15.694`、`corr=0.981/0.980`、`lsd=17.454/17.744`、`quiet=-53.076/-52.298`。再续训到 `logs/moss-audio-tokenizer-flow-mini-overfit10/runs/2026-06-16_08-08-52/`：最佳 checkpoint 是 `0-7500.ckpt`，2-step fixed10 continuous/discrete `snr=16.463/16.183`、`corr=0.984/0.984`、`lsd=17.258/17.423`、`quiet=-52.786/-52.248`；1-step `snr=18.120/17.656` 但 LSD/quiet 较差。`0-9000.ckpt` 开始明显退化，`0-10000.ckpt` 崩到 `snr=8.561/7.125`、`lsd=21.326/21.748`、`quiet=-42.608/-41.853`，与 step9449 loss/quantizer spike 对上。结论：patchwise spectral refiner 是目前最有效结构改动，当前 best 使用 `08-08-52/checkpoints/0-7500.ckpt` + deterministic 2-step；mini overfit 默认 `trainer.max_steps` 已改为 `7500`，避免默认 last.ckpt 跑到 collapse。下一步应围绕 7500 后训练稳定性处理：降低/冻结 patch refiner LR、减小/关闭 quantizer loss 后期权重、提高 late-stage spectral/quiet 约束，或加 best-by-fixed-eval/early-stop，而不是继续盲目跑到 10000。
- 2026-06-16 06:14-06:49 UTC 完成 `scripts/MOSS_Audio_Tokenizer_Flow` mini overfit10 token-halves 调试长跑。当前 mini 配置使用 `sigma_batch_mode=token_halves`、10 秒窗口、12.5 Hz token、deterministic 2-step sampling，训练内 fixed eval 仍关闭，只写 demo/checkpoint。新从零 run `logs/moss-audio-tokenizer-flow-mini-overfit10/runs/2026-06-16_06-14-01/` 跑到 `0-3000.ckpt`，demo 在该 run 的 `demos/{500,1000,1500,2000,2500,3000}_0_{continuous,discrete}_src_generated.wav`，离线 fixed10 `tmp/moss-flow-eval-20260616-061401-step3000-tokenhalves-scratch/results.json`：continuous/discrete `snr=12.984/12.888`、`corr=0.933/0.933`、`lsd=21.022/21.090`、`quiet=-44.930/-44.797`。随后从 `0-3000.ckpt` resume 到 run `logs/moss-audio-tokenizer-flow-mini-overfit10/runs/2026-06-16_06-35-11/` 的 `0-5000.ckpt`，demo 在 `demos/{3500,4000,4500,5000}_0_{continuous,discrete}_src_generated.wav`，fixed10 `tmp/moss-flow-eval-20260616-063511-step5000-tokenhalves-scratch/results.json`：continuous/discrete `snr=14.428/14.509`、`corr=0.952/0.953`、`lsd=19.973/20.004`、`quiet=-46.705/-46.546`。对比上一条有效 run `2026-06-16_05-07-23` 的 step5000 `snr=14.414/14.442`、`lsd=20.558/20.651`、`quiet=-45.461/-45.077`，token-halves 从零 5000 step 主体 SNR 基本持平但 LSD/quiet 明显更好；结论：per-token-halves sigma 方向有效，建议继续跑到 10000 step 验证是否保持 step10000 的底噪优势。
- 2026-06-16 05:07 UTC 继续调试 `scripts/MOSS_Audio_Tokenizer_Flow` mini overfit10：训练内 `MossAudioTokenizerFlowFixedEvalCallback` 在 8 卡 DDP 下仍可能在 fixed eval 目录刚创建后硬退出且不写 `eval_error.json`，前一个 resume run `logs/moss-audio-tokenizer-flow-mini-overfit10/runs/2026-06-16_04-52-00/` 到 step4000 demo 后退出、`fixed_eval/step_004000/` 为空、无 checkpoint。已把 `scripts/configs/experiment/moss-audio-tokenizer-flow-mini-overfit10.yaml` 改成调试长跑默认：`trainer.strategy=ddp`、`trainer.log_every_n_steps=50`、`trainer.enable_progress_bar=false`、`callbacks.demo_callback.demo_every=500`、`callbacks.fixed_eval_callback.eval_every=0`，训练内只写 demo/checkpoint，固定 10 条评估改用离线 `eval_checkpoint.py`。新有效 run `logs/moss-audio-tokenizer-flow-mini-overfit10/runs/2026-06-16_05-07-23/` 从 `2026-06-16_04-29-59/checkpoints/0-3000.ckpt` resume 到 `0-10000.ckpt`，无训练进程残留，`py_compile` 通过。离线 deterministic 2-step fixed10 关键点：3500 continuous/discrete `snr=13.385/13.283`、`corr=0.939/0.939`、`lsd=21.478/21.432`、`quiet=-43.766/-43.861`；4000 `13.817/13.769`、`0.945/0.945`、`20.821/20.839`、`-44.959/-44.863`；6000 `14.783/14.931`、`0.958/0.959`、`19.986/20.052`、`-46.552/-46.296`；8000 `15.266/15.490`、`0.962/0.963`、`18.178/18.208`、`-49.871/-49.680`；10000 `15.363/15.606`、`0.963/0.964`、`16.940/17.126`、`-51.776/-51.238`。结论：当前逻辑能继续过拟合，主体 SNR/corr 稳定上升，底噪指标最终明显优于旧 `2026-06-16_02-40-44/0-10000.ckpt` 的 LSD `18.265/18.362`、quiet `-48.399/-48.206`；可听 demo 在该 run 的 `demos/`，固定 10 条 eval wav 在 `tmp/moss-flow-eval-20260616-050723-step{3500,4000,4500,5000,5500,6000,7000,8000,10000}/`。
- 2026-06-16 step sweep 结论：`logs/moss-audio-tokenizer-flow-mini-overfit10/runs/2026-06-16_02-40-44/checkpoints/0-10000.ckpt` 用 deterministic sampler 对 `denoising_steps={1,2,4,8,16}` 做固定 10 条离线评估，最佳/推荐值改为 `2`。结果：1 step continuous/discrete `snr=15.571/15.897`、`lsd=19.080/19.128`、`quiet=-47.588/-47.427`；2 step `snr=15.619/15.942`、`lsd=18.265/18.362`、`quiet=-48.399/-48.206`；4 step `lsd=18.296/18.391`；8 step `lsd=18.343/18.434`；16 step `snr=15.472/15.780`、`lsd=18.457/18.537`、`quiet=-47.870/-47.658`。已把 `scripts/configs/experiment/moss-audio-tokenizer-flow-mini-overfit10.yaml` 和正式 `moss-audio-tokenizer-flow.yaml` 的 `default_denoising_steps` / demo `denoising_steps` / fixed eval `denoising_steps` 统一改为 `2`；后续 demo 和离线 eval 默认应优先用 deterministic + 2 steps。
- 2026-06-16 关键修复：Flow 主要底噪来自采样器，不是模型训练本身。`MossAudioTokenizerFlow.decode_condition()` 新增 `sampling_mode={"deterministic","stochastic"}`，当前默认已切到 `deterministic`；`deterministic` 在多步 denoise 时直接把 `tokens=denoised` 送到下一 sigma，不再每步 `reverse_step(..., randn)` 重新注入随机噪声。`generate_waveform()`、`eval_checkpoint.py`、demo callback、fixed eval callback 均已支持 `sampling_mode`，其中模型、离线 eval、demo/fixed eval callback 默认都是 deterministic；显式 `--sampling-mode stochastic` 仍保留用于复现旧行为。用同一个 `0-10000.ckpt`、`--denoising-steps 16 --seed 1234` 对比：stochastic continuous/discrete `snr=11.327/11.420`、`corr=0.936/0.938`、`lsd=44.611/44.615`、`quiet=-3.993/-3.977`；deterministic continuous/discrete `snr=15.472/15.780`、`corr=0.967/0.968`、`lsd=18.457/18.537`、`quiet=-47.870/-47.658`。prior-only 仍为 `snr=5.997/6.055`、`corr=0.788/0.792`，说明 deterministic denoiser 既补主体又不抬谱底。验证：不传 `--sampling-mode` 的 `tmp/moss-flow-eval-checkpoint-step10000-default/results.json` 已等于 deterministic 结果；显式 stochastic 的 `tmp/moss-flow-eval-checkpoint-step10000-stochastic-explicit/results.json` 复现旧结果。mini 配置的 demo/fixed eval 也显式 `sampling_mode: deterministic`。
- 2026-06-16 02:40 UTC 针对用户反馈 Flow 重建仍有明显底噪，参考可训通的 `scripts/mossland-codec` decoder 后把 `scripts/MOSS_Audio_Tokenizer_Flow` 改成 residual consistency decoder：`LocalConsistencyDenoiser` 新增 `prior_proj(cond_tokens)`，采样从 `prior + sigma_max * noise` 开始，网络在 `noisy - prior` 残差空间做 EDM/consistency denoise，最后输出 `prior + residual`；wrapper 新增 `prior_loss_weight`（mini 配置为 `0.2`）让 latent 条件先学主体结构，扩散只修细节。验证通过：`py_compile` 和小模型 forward/generate smoke。新 8 卡 from-scratch mini overfit10 run `logs/moss-audio-tokenizer-flow-mini-overfit10/runs/2026-06-16_02-40-44/` 已正常跑到 `0-10000.ckpt`，无训练进程残留；固定 10 条评估：step1000 continuous/discrete mean SNR+corr `6.71 dB/0.794`、`6.44 dB/0.780`，step2000 `8.88/0.882`、`8.70/0.880`，step3000 `9.58/0.908`、`9.53/0.908`，step4000 `10.59/0.916`、`10.60/0.916`，step5000 `10.89/0.924`、`10.93/0.925`，step8000 `11.30/0.937`、`11.38/0.939`，step10000 `11.33/0.936`、`11.42/0.938`，最差 corr 最终约 `0.797/0.804`。这明显超过旧 block-conditioned 实现 step10000 的约 `8.5 dB/0.856`，说明底噪主要来自无 latent prior 的裸高斯到全 STFT token 恢复；8000 后收益基本平台。
- 2026-06-16 后续调试：`MossAudioTokenizerFlowFixedEvalCallback` 追加频谱诊断，除 `snr_db/corr/pred_rms` 外，每条 continuous/discrete 还写 `lsd_db`、`spectral_error_db`、`quiet_pred_db`、`quiet_error_db`，summary 同步统计 mean/min/max 并 log mean/min。`lsd_db` 使用 2048 FFT/512 hop 的 mono log-spectrum RMS，`quiet_*` 只看 reference magnitude 最低 20% 频点，用来专门盯谱底噪。已对 run `2026-06-16_02-40-44` 的 step10000 wav 做 post-hoc 计算并写 `fixed_eval/step_010000/results_with_spectral.json`：continuous mean `lsd=44.616`、`spectral_error=-1.533 dB`、`quiet_pred=-3.984 dB`；discrete mean `lsd=44.617`、`spectral_error=-1.590 dB`、`quiet_pred=-3.978 dB`。LSD 高的样本不一定 waveform corr 最差，说明它捕获的是低能量频点/谱底差异，适合作为下一轮 decoder/frontend 改动的噪声诊断指标。
- 2026-06-16 后续调试：新增 `scripts/MOSS_Audio_Tokenizer_Flow/eval_checkpoint.py`，用于离线评估任意 Flow Lightning checkpoint。它读取 run 目录 `.hydra/config.yaml` 实例化 `cfg.model`，从 checkpoint `state_dict` 剥 `model.` 前缀加载模型，不依赖训练 wrapper；数据复用 run 配置里的 `SampleDataset`，输出 `source/continuous/discrete` wav 和包含 waveform + spectral 指标的 `results.json`。验证命令：`python -m scripts.MOSS_Audio_Tokenizer_Flow.eval_checkpoint --run-dir logs/moss-audio-tokenizer-flow-mini-overfit10/runs/2026-06-16_02-40-44 --checkpoint logs/moss-audio-tokenizer-flow-mini-overfit10/runs/2026-06-16_02-40-44/checkpoints/0-10000.ckpt --output-dir tmp/moss-flow-eval-checkpoint-step10000 --max-items 10 --denoising-steps 16 --device cuda:0`，结果 continuous `snr=11.321/corr=0.936/lsd=44.614/quiet=-3.989`，discrete `snr=11.411/corr=0.938/lsd=44.613/quiet=-3.984`，与训练 callback step10000 口径对齐。
- 2026-06-16 继续分析 denoiser vs prior：`eval_checkpoint.py` 新增 `--include-prior` 和 `--seed`。用 `0-10000.ckpt`、`--seed 1234` 评估得到 denoised continuous/discrete `snr=11.327/11.420`、`corr=0.936/0.938`、`lsd=44.611/44.615`、`quiet=-3.993/-3.977`；prior-only continuous/discrete 只有 `snr=5.997/6.055`、`corr=0.788/0.792`，但 `lsd=19.615/19.825`、`quiet=-46.559/-45.967`。结论：denoiser 确实补了主体细节，但显著抬高谱底噪，下一步应针对 denoiser 生成噪声而不是 prior。已加可选 `quiet_token_loss_weight/quiet_token_quantile`，mini 配置设为 `0.2/0.2`；从 `0-10000.ckpt` 微调 500 step 到 run `logs/moss-audio-tokenizer-flow-mini-overfit10/runs/2026-06-16_04-00-45/` 的 `0-10500.ckpt` 后，离线评估 `tmp/moss-flow-eval-checkpoint-step10500-quiet/results.json` 基本不变：continuous/discrete `lsd=44.609/44.613`、`quiet=-3.993/-3.977`。说明这个小权重 quiet-token 正则短期无效，后续更应考虑直接频谱 loss、噪声调度或 decoder/frontend 结构改动。
- 2026-06-16 继续尝试直接频谱约束：`MossAudioTokenizerFlowTrainingWrapper` 新增可选 `waveform_spectral_loss_weight`、`waveform_spectral_n_fft`、`waveform_spectral_hop_length`，在 anchor denoising path 上把 predicted/clean tokens 转回 waveform 后，对 mono STFT `log1p(magnitude)` 做 L1；mini 配置当前设 `waveform_spectral_loss_weight=0.05`、`n_fft=2048`、`hop=512`。小模型 backward smoke 通过。从 `0-10000.ckpt` 微调 500 step 到 run `logs/moss-audio-tokenizer-flow-mini-overfit10/runs/2026-06-16_04-09-01/` 的 `0-10500.ckpt`，离线评估 `tmp/moss-flow-eval-checkpoint-step10500-spectral/results.json` 仍基本不变：continuous/discrete `snr=11.304/11.400`、`corr=0.936/0.937`、`lsd=44.608/44.612`、`quiet=-3.994/-3.978`。结论：后期 500 step 小权重频谱微调无法压下 denoiser 谱底噪；该 loss 开关保留，后续应从头训练打开、更大权重实验，或改 sampling/decoder 结构。
- 2026-06-16 稳定性修正：训练内 `MossAudioTokenizerFlowFixedEvalCallback` 现在有 `fail_on_error` 参数，默认 `false`，评估异常时写 `${step_dir}/eval_error.json` 并清理 CUDA cache，不再直接杀训练；mini 配置显式 `callbacks.fixed_eval_callback.fail_on_error=false`。原因：一次从 `0-10000.ckpt` resume 到 `11000` 并在训练内 `eval_every=500` 的 run `2026-06-16_03-56-27` 到 10500 demo 后异常退出，`fixed_eval/step_010500/` 目录为空且无 checkpoint；关闭 fixed_eval 后同样微调到 10500 正常完成。严肃评估优先用离线 `eval_checkpoint.py`，训练内 fixed_eval 主要作快速 demo。
- 2026-06-16 02:05 UTC 为 `scripts/MOSS_Audio_Tokenizer_Flow` 新增 `MossAudioTokenizerFlowFixedEvalCallback`，避免只看单条 cached demo 误判。该 callback 用固定 `SampleDataset`/`index_file` 重读样本，按 step 写 `${eval_dir}/step_XXXXXX/{idx}_source.wav`、`{idx}_continuous.wav`、`{idx}_discrete.wav` 和 `results.json`，并 log `fixed_eval/*_mean`/`*_min` 指标。mini 配置已接入 `callbacks.fixed_eval_callback`，默认 `eval_every=1000`、`max_items=10`、输出 `${paths.output_dir}/fixed_eval`。验证通过：`py_compile`；单卡 1 step smoke 命令触发 fixed eval，产物在 `tmp/moss-flow-fixed-eval-smoke/step_000001/`，包含 3 个 wav 和 `results.json`。用 `hydra.compose()` 离线 instantiate callbacks 会因为 `${paths.output_dir}` 依赖 HydraConfig 报错，真实 `python -m scripts.train ...` 入口正常。
- 2026-06-16 02:00 UTC 调通 `scripts/MOSS_Audio_Tokenizer_Flow` mini overfit10 的关键改动：`LocalConsistencyDenoiser` 不再只在输入层 concat latent/sigma 条件，而是在每个 local causal Transformer block 加 latent+sigma AdaLN/FiLM 条件调制；mini 配置恢复 32 个 RVQ codebook、1024 词表，约 `22.0M` 参数，并回到 consistency pair 为主、direct denoising anchor 为辅（`consistency_pair_loss_weight=1.0`、`denoising_anchor_loss_weight=1.0`、`sigma_max_anchor_loss_weight=0.5`），避免旧 direct-only hard switch。验证通过：`py_compile`、Hydra instantiate、10 秒 smoke (`rep=(1,4,1920,250)`, `tokens=(1,125,15360)`) 和 8 卡 DDP 训练。新 run `logs/moss-audio-tokenizer-flow-mini-overfit10/runs/2026-06-16_01-20-14/` 从零到 step2200，`eval_overfit10_unique_step2000/results.json`：continuous mean SNR/corr `6.68 dB / 0.805`，discrete `6.76 dB / 0.799`。续训 run `logs/moss-audio-tokenizer-flow-mini-overfit10/runs/2026-06-16_01-36-47/` 从 `0-2000.ckpt` 到 `0-5000.ckpt`，最终 `eval_overfit10_unique_step5000/results.json`：continuous mean SNR/corr `8.20 dB / 0.850`，discrete `8.16 dB / 0.847`；最难第 6 条仍低（continuous corr `0.455`、discrete `0.444`），但已比旧失败版 step6000 的 10 条 mean corr `0.411/0.353` 大幅改善。可听 demo 在该 run 的 `demos/` 和 `eval_overfit10_unique_step5000/`。
- 2026-06-16 01:20 UTC 旧失败诊断保留供参考：`decode_condition()` 必须真正使用 `denoising_steps`，按 Mossland/CoDiCodec consistency 采样做多步 `denoise -> reverse_step`，不能忽略参数只从 `sigma_max` 一步出图；已在 `models.py` 修复。旧 run `logs/moss-audio-tokenizer-flow-mini-overfit10/runs/2026-06-16_00-58-29/` 虽然 cached demo corr 约 continuous `0.846`、discrete `0.829`，但 10 条固定 eval 仍差，`eval_overfit10_unique_step6000/results.json`：continuous mean corr `0.411`、discrete `0.353`。低/高噪声诊断确认 STFT/低噪声路径没坏：`sigma=0.1` denoise 10 条 mean corr 约 `0.966`，但 `sigma=80` continuous mean corr 约 `0.329`、discrete `0.275`，问题集中在高噪声端“仅靠输入 concat latent condition 恢复 clean token”。从旧 pair-consistency `0-6000.ckpt` 硬切 direct denoising run `2026-06-16_01-11-13/` 会退化，不建议用该 checkpoint 判断最终效果。
- 2026-06-15 23:50 UTC 按用户要求重写 `scripts/MOSS_Audio_Tokenizer_Flow/`：不依赖预训练 tokenizer，不用 `importlib`/`sys.path` hack，模型从零训练；旧 rFFT-token/EDM endpoint 方案已替换为 Mossland-style normalized STFT representation + local Transformer token stream + consistency pair loss。默认 STFT 对齐参数为 `hop=1920`、`fac=2`、`spec_frames_per_token=2`，所以每个 latent token 正好覆盖 `3840` samples / `80ms`，10 秒 `sample_size=480000` 会 pad 到 `481920` samples、`250` STFT frames、`125` latent tokens，输出再 crop 回原长。`models.py` 自包含 STFT normalize/denormalize、local causal Transformer encoder/decoder、低维 continuous branch、RVQ discrete branch；latent condition 以同时间步 concat + decoder block 条件调制形式进入 local consistency denoiser，无 chunk/learned-query bottleneck。`wrapper.py` 训练 continuous/discrete 两条路径的 consistency loss：同一噪声下采样 `sigma_high/sigma_low`，`pred=noisy_high` denoise，`target=noisy_low` denoise under `no_grad`，pseudo-Huber 并按 `1/(sigma_high-sigma_low)` 加权；噪声尺度回到 Mossland representation 口径 `sigma_max=80`、`sigma_data=0.5`。
- 2026-06-15 21:28 UTC 新增并验证 `scripts/MOSS_Audio_Tokenizer/infer.py`：默认用本地 `checkpoints/OpenMOSS-Team/MOSS-Audio-Tokenizer-v2` 加载 `MossAudioTokenizerModel`，读取 `tmp/34078087/mixture.mp3`，encode/decode 重建后保存到 `tmp/34078087/mixture_reconstructed.wav`。默认 `--chunk-duration=0.96`，这是 48 kHz / `downsample_rate=3840` 下小于 1s context 且可整除的最大安全 chunk；`0.08` 会把 186.24s 音频拆成约 2328 个 streaming steps，明显慢。验证命令：`python -m py_compile scripts/MOSS_Audio_Tokenizer/infer.py`；`python scripts/MOSS_Audio_Tokenizer/infer.py --codec-weight-dtype bf16 --device cuda:0`。输出为 48 kHz、双声道、186.24 秒，codes shape `(32, 1, 2328)`。
- 2026-06-15 14:35 UTC 排查 Mossland codec 370k EMA vs CoDiCodec official “STFT distance 接近但 Mel/LSD 差很多”的 item 级差异。当前工作区没有旧 `runs/baselines_musiccaps_full_codicodec` 全量 per-item `results.jsonl`，`runs_old/` 也只有很小的 Mossland 旧目录；因此只对 Mossland parallel run 的高 LSD item 补跑 official CoDiCodec 小子集。临时产物在 `tmp/codicodec_vs_mossland_item_probe/`：`top250_mossland_lsd_manifest.jsonl`、`codicodec_prediction_manifest_top250.jsonl`、`codicodec_eval_none_top250/results.jsonl`、`top250_compare.tsv`。结论：Mossland 全量 LSD `mean=28.207/median=27.101/p95=39.722/p99=55.484/max=96.434`，MRSTFT `mean=0.522/median=0.537/p99=0.696`，但 `EC_JNTVDrok` 单条 MRSTFT `12.443` 是异常点；LSD 高值主要来自静音/稀疏/瞬态/窄带或低质量片段，因为 `metrics.py` 的 LSD 是 `20*(log10(pred_mag)-log10(ref_mag))` 的 RMS，会放大低能量频点和谱底噪差异，而 MRSTFT 是线性幅度 L1 除以 reference mean。对 Mossland LSD top250 补跑 official CoDiCodec 后，top250 mean：Mossland LSD `49.851` vs CoDiCodec `44.084`，MRSTFT `0.510` vs `0.431`，SI-SDR `-6.621` vs `2.638`；最大 LSD gap item 为 `musiccaps__R_HAtyDbw1M__30_40`（`41.714` vs `20.092`，gap `21.622`）、`musiccaps__OMcoFfaCaGM__30_40`（gap `21.053`）、`musiccaps__Uvn7waPvseo__30_40`（gap `18.858`）、`musiccaps__6-XEA0zqo8M__50_60`（gap `16.678`）、`musiccaps__c_a74UO2ftg__330_340`（gap `13.833`）。这些大 gap 多是打击/瞬态、drum machine、合成器、单音/调音、窄带独奏、静音/背景噪声片段。若要严格全量 top gap，需要重建或重跑 full official CoDiCodec per-item 结果。
- 2026-06-15 13:55 UTC 按用户要求把 Mossland codec 370k EMA reconstruction 用正确推理口径写回飞书 `https://acnc6zeentra.feishu.cn/wiki/H3pdwppcUikX6LkmN0Mcamo4nJd`。没有重跑旧 `fulltrack_infer.py` xfade；使用已有正确 run `scripts/mossland-codec/eval_benchmark/runs/mossland_codec_370000_ema_parallel_decode/`（`EncoderDecoder.encode()` + `decode(mode="parallel", task_id="reconstruct")`，prediction/source 均 `441000` frames）。先尝试重生成本地全表时发现当前工作区缺少部分 baseline summary，会把已复现行回退成 `未复现`，因此改为从飞书当前 reconstruction 表读取完整表格，只定向替换 `Mossland codec 370k (EMA)` 行。写入 docx `DC6cdohbXoGpyjx01wac1gDynHF` 成功，`block_replace` 返回 revision `857`；读回确认 `EncoderDecoder parallel decode`、`-9.930`、`3.013`、`28.207`、`0.522`、`0.098`、`0.490` 各出现 1 次，旧 `full-clip chunked xfade` 在该关键词结果中为 0。新 reconstruction table block id 为 `doxcn9TUoVJ2bs8AQW6yHNa1zfg`；已从飞书新表反向同步本地 `scripts/mossland-codec/eval_benchmark/tables/feishu_reconstruction_repro.{csv,md,xml}`，保留其他 baseline 数值。
- 2026-06-15 13:30 UTC 回答用户关于 Mossland codec 370k EMA reconstruction 指标异常的核查：`scripts/configs/experiment/mossland-codec.yaml` 的 `PreparedSeparationDataset.sample_size=66560` 是正确的训练窗口长度，来自 `waveform_length_for_stft_frames(2 * spec_length=64, hop=1024, fac=2) = fac*hop + hop*(64-1) = 66560`，对应 `64` 个 STFT frames / 两个 `spec_length=32` 块，约 `1.509s @ 44.1kHz`；模型内部 `data_length=512` 是 frontend token 数，不是 waveform sample 数。旧 `runs/mossland_codec_370000_ema_fullclip_xfade/` reconstruction 通过 10 个随机 chunk + waveform xfade 拼 10s，会影响 SI-SDR/LSD 等逐样本距离；当前主口径 `runs/mossland_codec_370000_ema_parallel_decode/` 走 `EncoderDecoder.encode()` + `decode(mode="parallel", task_id="reconstruct")`，manifest 抽检为 `source_frames=prediction_frames=441000`。parallel 指标仍为 SI-SDR `-9.930`、LSD `28.207`、MRSTFT `0.522`、ViSQOL `3.013`、FAD-CLAP `0.098`、FAD-VGGish `0.490`，说明长度/短窗推理错误已排除；与 CoDiCodec official SI-SDR/LSD 差距更可能来自模型 checkpoint/训练数据/训练收敛/随机 diffusion decoder 的波形相位对齐，而不是 `66560` 配置本身。
- 2026-06-15 01:10 UTC 按用户指出 SR 表中 STFT distance `371` 异常后，确认旧 `runs/mossland_codec_370000_ema_sr300/` 使用 `run.py`/`generate_waveform()` 只输出模型固定窗口 `66560` frames，非严格 10s；其中 `0BFauf6TGGU` 三档预测近静音导致 MRSTFT mean 被单个 outlier 拉到 `371`。已把 `codec_reconstruct_infer.py` 扩展为支持 `super_resolution`：通过 `build_task_input()` 构造 10s 低带宽输入，再用 `EncoderDecoder.encode()` + `decode(mode="parallel", task_id="super_resolution")` 做整段并行 SR。strict 10s SR run 在 `runs/mossland_codec_370000_ema_sr300_strict10s/`，900 条 prediction manifest 首/中/末抽检均为 `441000` frames。三档 16/24/32 kHz 指标：LSD `26.466/28.284/28.346`、LSD-HF `31.993/36.101/36.023`、MRSTFT `0.500/0.521/0.520`、SI-SDR `-9.507/-9.592/-9.574`、ViSQOL `3.231/2.947/2.922`、FAD-VGGish `0.590/0.608/0.612`。`generate_feishu_existing_task_tables.py` 已改用 strict 10s SR 数值，飞书 SR existing 表已替换到 docx `DC6cdohbXoGpyjx01wac1gDynHF` revision `841`，读回 keyword `strict 10s` 确认落表。Mono-to-stereo 和 Source separation 仍沿用上一轮结果：mono `runs/mossland_codec_370000_ema_stereo100/`，separation `runs/mossland_codec_370000_ema_musdb18hq_fulltrack_chunked/`。验证通过：`py_compile` 和 `main_env python -m pytest -q tests/mossland_codec/test_eval_benchmark.py`（69 passed）。
- 2026-06-14 23:42 UTC 按用户纠正，Mossland 370k reconstruction 评测不再用外部 overlap-add/crossfade，而是新增 `scripts/mossland-codec/eval_benchmark/codec_reconstruct_infer.py`，直接调用 `scripts/mossland-codec/inference.py` 的 `EncoderDecoder.encode()` + `decode(mode="parallel", task_id="reconstruct")` 做整段 MusicCaps-HF reconstruction。`bash/eval_mossland_codec_checkpoint.sh` 已改为默认 `RUN_NAME=mossland_codec_<step>_<variant>_parallel_decode`，调用新入口，并支持 `PYTHON_BIN`、`MAX_BATCH_SIZE_ENCODE`、`MAX_BATCH_SIZE_DECODE`；旧 `fulltrack_infer.py` 仍保留给 full-track separation/历史 xfade 对比。已用 `logs/mossland-codec/runs/2026-06-12_12-46-36/checkpoints/last.ckpt/last.ckpt` 的 EMA 导出目录 `ckpt/mossland-codec-370000-ema/` 重测 MusicCaps-HF full `5355/5521`：run dir `scripts/mossland-codec/eval_benchmark/runs/mossland_codec_370000_ema_parallel_decode/`，prediction manifest 5355 行，首/中/末抽检均为 `441000` frames/10s 且 metadata `inference_mode=codec_parallel_reconstruct`。指标：SI-SDR `-9.930`、ViSQOL `3.013`、Mel/LSD `28.207`、STFT/MRSTFT `0.522`、FAD-CLAP `0.098`、FAD-VGGish `0.490`。`generate_feishu_reconstruction_table.py` 已切换 Mossland 370k 行到该 run；飞书 wiki `H3pdwppcUikX6LkmN0Mcamo4nJd` 实际 docx `DC6cdohbXoGpyjx01wac1gDynHF` reconstruction 表已替换到 revision `806`，新表 block id `doxcnTl2pNiffrxeRmlprVqazUd`，读回完整表与本地 CSV 逐格一致：35 行含表头、13 列、0 diff。验证通过：`py_compile`、`bash -n bash/eval_mossland_codec_checkpoint.sh`、`main_env python -m pytest -q tests/mossland_codec/test_eval_benchmark.py`（69 passed）。
- 2026-06-14 21:45 UTC 按用户要求新增 `bash/eval_mossland_codec_checkpoint.sh`，用于给定 Mossland codec Lightning checkpoint 后直接启动 MusicCaps-HF full-clip reconstruction 评测流水线。无参数默认使用 `logs/mossland-codec/runs/2026-06-12_12-46-36/checkpoints/last.ckpt/last.ckpt`；脚本会读取 `global_step` 派生默认 `RUN_NAME=mossland_codec_<step>_ema_fullclip_xfade`，默认导出 EMA 到 `ckpt/mossland-codec-<step>-ema/`，用 `fulltrack_infer.py` 做 full-clip overlap-add/crossfade 推理，再跑 CLAP、VGGish、ViSQOL。结果固定写入 `scripts/mossland-codec/eval_benchmark/runs/<RUN_NAME>/`，日志写入 `scripts/mossland-codec/eval_benchmark/logs/<RUN_NAME>.log`；支持 `GPUS`、`INFER_SHARDS`、`METRIC_SHARDS`、`VISQOL_SHARDS`、`CHUNK_BATCH_SIZE`、`OVERWRITE_PREDICTIONS` 等环境变量覆盖。验证通过：`bash -n bash/eval_mossland_codec_checkpoint.sh` 与 `bash bash/eval_mossland_codec_checkpoint.sh --help`。21:46 已启动默认 370k EMA 评测，主进程 PID `1516788`，run dir `scripts/mossland-codec/eval_benchmark/runs/mossland_codec_370000_ema_fullclip_xfade/`，日志 `scripts/mossland-codec/eval_benchmark/logs/mossland_codec_370000_ema_fullclip_xfade.log`；当前阶段为 16 shard full-clip 推理，完成后脚本会自动合并 manifest 并继续 CLAP/VGGish/ViSQOL。
- 2026-06-14 20:55 UTC 按用户要求评测 `logs/codicodec-paper-repro/runs/2026-06-14_13-41-25/checkpoints/2-18000.ckpt` reconstruction raw/EMA，并回填飞书已有 reconstruction 复现表。checkpoint 已 unwrap 到 `ckpt/codicodec-paper-repro-18000-raw` 与 `ckpt/codicodec-paper-repro-18000-ema`；有效 full-clip overlap-add/crossfade 产物在 `scripts/mossland-codec/eval_benchmark/runs/codicodec_paper_repro_18000_{raw,ema}_fullclip_xfade/`，prediction manifest 各 5355 行，首/中/末抽检均为 `441000` frames/10s。指标：raw SI-SDR `-35.471`、ViSQOL `3.902`、LSD `27.215`、MRSTFT `0.606`、FAD-CLAP `0.132`、FAD-VGGish `2.024`；EMA SI-SDR `-35.566`、ViSQOL `3.891`、LSD `27.295`、MRSTFT `0.606`、FAD-CLAP `0.136`、FAD-VGGish `2.073`。`generate_feishu_reconstruction_table.py` 已新增 `CoDiCodec paper-repro 18k (raw)` 与 `(EMA)` 两行；飞书 docx `DC6cdohbXoGpyjx01wac1gDynHF` reconstruction 表已替换到 revision `781`，新表 block id `doxcnaAko6DRhtIEvoWj5gZOuic`，读回整表与本地 CSV 逐格一致：34 数据行、13 列、0 空单元格。验证通过：相关 `py_compile` 与 `PYTHONPATH=$PWD python -m pytest -q tests/mossland_codec/test_eval_benchmark.py`（68 passed）。
- 2026-06-14 20:35 UTC 按用户要求评测 `logs/mossland-codec/runs/2026-06-12_12-46-36/checkpoints/last.ckpt/last.ckpt` reconstruction 并回填飞书 reconstruction 复现表。该 Lightning checkpoint 为 `global_step=370000`，已按主 `mossland-codec.yaml` 的 5-task 旧顺序导出 EMA 到 `ckpt/mossland-codec-370000-ema/`；MusicCaps-HF full `5355/5521` full-clip chunked xfade 推理产物在 `scripts/mossland-codec/eval_benchmark/runs/mossland_codec_370000_ema_fullclip_xfade/`，prediction manifest 5355 行，首/中/末抽检均为 `441000` frames/10s。为提高吞吐，`fulltrack_infer.py` 新增 `--chunk-batch-size`，本次使用 16 shards、每卡 2 进程、每条内 chunk batch 8。指标：SI-SDR `-9.804`、ViSQOL `2.853`、Mel/LSD `28.757`、STFT/MRSTFT `0.536`、FAD-CLAP `0.083`、FAD-VGGish `0.697`。`generate_feishu_reconstruction_table.py` 已新增 `Mossland codec 370k (EMA)` 行；该行已包含在当前飞书 reconstruction 表 revision `781`，block id `doxcnaAko6DRhtIEvoWj5gZOuic`。
- 2026-06-14 20:10 UTC 按用户要求把“代码改回 5 任务”：`scripts/mossland-codec/tasks.py` 的主 `TASK_NAMES` 恢复为 5 个 Mossland codec 任务（`reconstruct`、`separate_vocals`、`separate_accompaniment`、`super_resolution`、`mono_to_stereo`），因此 `MosslandCodecTransformer` 默认 task embedding 重新是 5 行，可兼容旧 5-task checkpoint；`separate_drums/separate_bass/separate_other` 保留在 `SUPPORTED_TASK_NAMES`，供 MUSDB 四 stem 数据构造和评测使用，但不再进入主 codec 默认 embedding。`scripts/mossland-a2a/models.py` 改为在 A2A 模块内显式声明自己的 6 个任务（vocals/drums/bass/other/SR/mono-to-stereo），不再借用主 codec `TASK_NAMES`。验证通过：`python -m py_compile scripts/mossland-codec/tasks.py scripts/mossland-codec/models.py scripts/mossland-a2a/models.py`；`python -m pytest -q tests/mossland_codec/test_tasks.py tests/mossland_codec/test_wrapper_imports.py`（38 passed）；小实例化检查显示 codec `task_embedding=(5, 64)`。
- 2026-06-14 19:35 UTC 按用户要求仔细比对 `tmp/music2latent-training/music2latent-training` 与 `scripts/configs/experiment/codicodec-paper-repro.yaml`：官方目录是 Music2Latent training branch，核心为 continuous bottleneck UNet、mono random-channel、hop=512/fac=4、34304-sample crop、batch=16、800k iters、fp16 GradScaler、RAdam、10k warmup、cosine 到 final_lr=1e-6、无 FSQ/random_mix；当前 YAML 是 Mossland/CoDiCodec paper-style reconstruction 配置，核心为 Transformer+FSQ、stereo、hop=1024/fac=2、66560-sample crop、crops_per_file=16、128-card 20k-step sample-budget scaling、bf16 Lightning、gradient_clip=1、cosine 到 0、random_mix=0.5、FSQ dropout=0.75。两者不能视为 bit-identical 复现；该 YAML 更接近 CoDiCodec 论文参数，不是官方 Music2Latent public training code。
- 2026-06-14 19:20 UTC 已按用户要求评测本地 `codicodec-paper-repro` 10k checkpoint 的 raw/EMA reconstruction，并回填飞书现有 reconstruction 复现表。checkpoint 已 unwrap 到 `ckpt/codicodec-paper-repro-10000-raw` 与 `ckpt/codicodec-paper-repro-10000-ema`；由于 checkpoint 只有 5 个 task embedding，`MosslandCodecTransformer` 支持从 config 传 `task_names`。普通 `run.py`/`generate_waveform()` 只会输出模型固定窗口 `66560` frames 约 `1.509s`，不能用于 MusicCaps-HF full 10s reconstruction；`fulltrack_infer.py` 已扩展 `reconstruct`，按整段音频分块并用 overlap-add/crossfade 拼接。旧 hard-concat partial 目录已改名为 `runs/codicodec_paper_repro_10000_raw_fullclip_hardconcat_invalid_20260614`，不要使用。有效 full-clip 产物在 `runs/codicodec_paper_repro_10000_{raw,ema}_fullclip_xfade/`，prediction manifest 各 5355 行，抽检首/中/末条均为 `441000` frames/10s，seam 统计不再有硬拼尖峰。指标：raw SI-SDR `-36.786`、ViSQOL `3.713`、LSD `28.211`、MRSTFT `0.638`、FAD-CLAP `0.166`、FAD-VGGish `3.089`；EMA SI-SDR `-37.183`、ViSQOL `3.766`、LSD `27.849`、MRSTFT `0.643`、FAD-CLAP `0.171`、FAD-VGGish `3.251`。`generate_feishu_reconstruction_table.py` 已新增 `CoDiCodec paper-repro 10k (raw)` 与 `(EMA)` 两行，本地 `feishu_reconstruction_repro.csv` 为 29 数据行、13 列、0 空单元格；飞书 Wiki URL `H3pdwppcUikX6LkmN0Mcamo4nJd` 实际 docx `DC6cdohbXoGpyjx01wac1gDynHF` 已用 `block_replace` 更新到 revision `766`，新 reconstruction 复现表 block `doxcncBhaYAWjLsE2F3RMNM5GSd`，读回与本地 CSV 逐格一致。验证通过：相关 `py_compile` 与 `PYTHONPATH=$PWD python -m pytest -q tests/mossland_codec/test_eval_benchmark.py`（68 passed）。
- 2026-06-14 18:25 UTC 继续按用户要求减少飞书 reconstruction 表中的 `未复现`：新增 `scripts/mossland-codec/eval_benchmark/baselines/music2latent_baseline.py`，把官方 `music2latent 0.1.8` checkpoint 复制到 `checkpoints/music2latent/music2latent.pt`，完成 MusicCaps-HF full `5355/5521` 推理与全指标。Music2Latent 结果：SI-SDR `-3.093`、ViSQOL `4.099`、Mel/LSD `25.318`、STFT/MRSTFT `0.515`、FAD-CLAP `0.197`、FAD-VGGish `1.504`。`generate_feishu_reconstruction_table.py` 已填 Music2Latent 行，本地表仍为 29 数据行、13 列、0 空单元格，`未复现` 单元从 78 降到 72。飞书 docx token `DC6cdohbXoGpyjx01wac1gDynHF` 已写入 revision `765`，最新 reconstruction table block id `doxcnGOp3UqWHgPSSeuOCxYeOKc`，读回与本地 CSV 逐格一致。相关临时日志均写入 `logs/tmp/`。
- 2026-06-14 17:50 UTC 已完成用户当前要求的飞书 reconstruction 复现表收尾。飞书 docx token `DC6cdohbXoGpyjx01wac1gDynHF`，最新 reconstruction 表 block id `doxcncZ0WPg6NQ3IrDFjiUuubGd`，`block_replace` 写入 revision `764`；读回校验与本地 `scripts/mossland-codec/eval_benchmark/tables/feishu_reconstruction_repro.csv` 逐格一致：29 数据行、13 列、0 空单元格。按用户要求，`Mel distance` / `STFT distance` 已分别用本地 pipeline 的 `lsd/mean` / `mrstft/mean` 填满，应解释为本地工程 proxy 指标；MusicCaps-HF full 已测行的 `SI-SDR`、ViSQOL、Mel/LSD、STFT/MRSTFT、`FAD_clap`、VGGish `FAD` 均已填值。DAC n_quantizers=2/3/6 和 Opus 8/14/24 full ViSQOL 已补齐；Stable Audio 3 SAME-S/SAME-L 已测并加入表格。`visqol_eval.py` 已修复 Google ViSQOL 追加旧 `visqol_results.csv` 导致重复行的问题，运行前会删除 stale CSV。按用户日志要求，仓库根 `logs/*.log` 已清零，139 个旧 root log 已移到 `logs/tmp/`，后续临时评测日志写 `logs/tmp/`。
- 2026-06-14 17:15 UTC 按用户要求把四轨分离配置正式改成 A2A 多任务训练：新增 `scripts/mossland-a2a/`，`MosslandA2ATransformer` 复用 Mossland STFT representation、frontend、`Transformer_Diffusion`、task embedding 和 consistency loss。实验配置为 `scripts/configs/experiment/codicodec-paper-repro-a2a.yaml`，命令 `python -m scripts.train experiment=codicodec-paper-repro-a2a`；旧 `codicodec-paper-repro-four-stem-separation.yaml` 和临时 `musdb18hq-four-stem-a2a.yaml` 已删除。该配置在同一个训练流里用 `MosslandTaskRoutedDataset` 路由任务：四个 separation task 读 MUSDB18-HQ，`super_resolution` 和 `mono_to_stereo` 读 `/inspire/sj-ssd3/project/embodied-multimodality/public/Sonata/data/raw/NETEASE_SPIDER`。2026-06-14 17:25 UTC 修复 routed dataset 混合不同底层数据集时的 DataLoader collate：返回固定 `info` schema，避免 `relpath`/`timestamps` 等异构 metadata 触发 `KeyError`。2026-06-14 17:35 UTC 按用户要求把 codec/A2A 的 task embedding 默认初始化从全 0 改为 `normal_(0, 0.02)`，A2A 配置显式 `model.task_embedding_init_std=0.02`。2026-06-14 17:45 UTC 按用户要求把 A2A 改成更像 codec 但不压缩信息的 source encoder：source 频谱先过 `frontend_encoder_down` 和 token-to-token encoder，`num_latents` 必须等于 `data_length`、`bottleneck_channels` 必须等于 `dim`、`num_more_latents=0`、`use_fsq=false`，不使用 learned latent query，也不把 source 频谱直接 concat 到 decoder input。2026-06-14 18:20 UTC 修复真实多尺度 frontend 训练时报 `tensor a (32) vs tensor b (8)` 的问题：A2A 的 decoder multiscale features 现在从 encoded source tokens 经过 `frontend_pre_decoder_up(..., skip_output_layer=True)` 生成，并按 split factor 沿时间拼回，不能直接复用 `frontend_encoder_down` 的 raw features。
- 2026-06-14 16:55 UTC 已继续按用户要求补飞书现有非重建复现表空缺。重新用 `tmp/eval_metric_refs/tools/bazelisk` 构建 Google ViSQOL；Ubuntu 24/GCC 13 下需要在 ViSQOL repo 内执行 `../tools/bazelisk build -c opt --cxxopt=-include --cxxopt=cstdint //:visqol`，否则 TensorFlow Lite `spectrogram.cc` 会因缺 `<cstdint>` 构建失败。随后对 FlashSR MusicCaps-HF SR@16k 100 条 manifest 跑 `run_sharded_visqol_eval.py --num-shards 16`，结果 `visqol_moslqo/mean=1.7709846`、median `1.6909421`，输出在 `runs/baselines_musiccaps_sr100_16000_flashsr/visqol/summary.json`。`generate_feishu_existing_task_tables.py` 已把 FlashSR ViSQOL 填为 `1.771`，飞书 docx `DC6cdohbXoGpyjx01wac1gDynHF` 的 Audio Super-Resolution 复现表已用 `block_replace` 更新，读回 revision `759`，新表 block id `doxcnn4beQY1MdlltuaO2WWT6hc`。
- 2026-06-14 16:40 UTC 已按用户要求补测 Stable Audio 3 SAME 两个版本并更新飞书 reconstruction 复现表。SAME-S/SAME-L 官方 checkpoint 和 HF cache 均放在 `checkpoints/stable-audio-3/`；官方 repo 副本在 `tmp/eval_baseline_refs/stable-audio-3`。本地 MusicCaps-HF full `5355/5521` 指标：SAME-S SI-SDR `8.771`、ViSQOL `3.781`、FAD-CLAP `0.126`、FAD-VGGish `1.857`；SAME-L SI-SDR `11.484`、ViSQOL `3.541`、FAD-CLAP `0.138`、FAD-VGGish `0.824`。`generate_feishu_reconstruction_table.py` 已新增 SAME-S/SAME-L 两行，`tables/feishu_reconstruction_repro.csv` 现在 29 数据行、13 列、0 空单元格；飞书 docx `DC6cdohbXoGpyjx01wac1gDynHF` reconstruction 复现表已写入 revision `742`，新表 block id `doxcnYBNQkBJQTBcIsG5MQbzFDe`，读回与本地 CSV 逐格一致。ViSQOL 确认为 CPU-only binary，不能放 GPU；本机 192 线程上 128 分片可用且 I/O wait 约 0，后续只跑 ViSQOL 可试 160 分片，若同时跑 GPU 后评测/音频解码则保持 128 更稳。
- 2026-06-14 16:43 UTC 用户要求不要把一次性/临时评测日志平铺到仓库根 `logs/`；已删除无用日志 `logs/audiosr_sr16000_eval_clap.log` 并创建 `logs/tmp/`。后续临时日志优先写 `logs/tmp/`，只有稳定 watcher、长期任务或需要人工定位的命名日志才放 `logs/` 根。
- 2026-06-14 15:55 UTC 用户纠正：飞书文档里已有非重建任务复现表，不应追加新表。已删除上一轮误追加的 `非重建任务 Benchmark 复现` 章节（根 block `doxcnSqNNtjWbikUpjnHpefVsLh` 及其子 blocks），并用 `generate_feishu_existing_task_tables.py` 替换三张已有表：Source Separation table revision `731`、Audio Super-Resolution revision `733`、Mono-to-Stereo revision `734`，删除后 revision `735`。现有表只填直接实测值，未复现单元格保留 `未复现`；错误追加完成的 15:55 旧记录已作废。
- 2026-06-14 16:40 UTC 已按用户要求下载/复现并回填现有飞书表。新增 `fast_separation_metrics.py`，用已有 prediction manifest 快速计算 waveform SDR (`snr_db`) 与 SI-SDR。MUSDB18-HQ full-track 4-stem 已完成并回填飞书 Source Separation 现有复现表（revision `750`，新表 block `doxcnrDpyzpSEj9Qe3GEpKx0jHe`）：SCNet XL IHF SDR vocals/drums/bass/other/avg `11.422/11.816/9.236/7.883/10.089`，SI-SDR `10.699/11.523/7.408/6.797/9.106`；BS-RoFormer SDR `11.008/11.552/8.438/7.439/9.609`，SI-SDR `10.336/11.229/7.562/6.265/8.848`。Mel-RoFormer v1.0.11 checkpoint/config 已下载并跑完 50 tracks，但官方 inference raw outputs 全零，飞书保持 `未复现` 并说明异常。FlashSR 已完成 MusicCaps-HF SR@16k 100 条并回填飞书 ASR 现有表：LSD `40.799`、LSD-HF `50.587`、MRSTFT `1.103`、SI-SDR `12.755`、FAD-VGGish `1.302`，后续已补 ViSQOL `1.771`。SAGA-SR 官方仓库 `jakeoneijk/SAGA-SR` 当前只有 preprocess/train，TODO 包含上传 pretrained weights 和 inference/evaluation code，因此目前不能复现。
- 2026-06-14 16:07 UTC 已按用户要求继续补齐飞书 reconstruction 复现表并优先加速。使用 MusicCaps-HF full `5355/5521` 数据，补完 DAC n_quantizers=2 的 CLAP/VGGish 指标、Opus 8/14/24 的 CLAP/VGGish 指标、EnCodec 3/6/12 的 ViSQOL；飞书 docx `DC6cdohbXoGpyjx01wac1gDynHF` 中 reconstruction 表 block `doxcnChCcBLierTcKps8DrYwBAd` 已用 `tables/feishu_reconstruction_repro.xml` 替换，写入 revision `737`，读回校验与本地 `tables/feishu_reconstruction_repro.csv` 完全一致：28 数据行、13 列、0 空单元格。新增高速评测入口 `run_sharded_metric_eval.py`（GPU 分片跑 `run.py` 并聚合 FAD）和 `run_sharded_visqol_eval.py`（CPU-only ViSQOL 多分片、限线程、聚合），`audio_io.load_audio_segment()` 改为缓存 `torchaudio.transforms.Resample`，`visqol_eval.run_visqol()` 对旧 `Namespace` 缺少 `num_shards/shard_id` 做默认兼容。验证通过：相关 py_compile、`python -m pytest -q tests/mossland_codec/test_eval_benchmark.py`（68 passed）。当前已按用户要求停止无关后台下载/同步/评测进程，8 张 PPU/GPU 空闲。
- 2026-06-14 15:55 UTC 旧记录更正：曾把“除了重建的另外三个任务”作为新章节追加到飞书，这是错误操作；该章节已按用户纠正删除，后续只替换文档里已有的三张复现表，不再追加新表。
- 2026-06-14 15:20 UTC 用户要求下载 MUSDB18-HQ 到 `/inspire/sj-ssd3/project/embodied-multimodality/public/Sonata/data/dataset` 并基于 `codicodec-paper-repro.yaml` 写 0 压缩率四轨分离训练配置。已新增 `scripts.data.datasets.Musdb18HqSeparationDataset`，直接读取解压后的 `train/test/<track>/{mixture,vocals,drums,bass,other}.wav`，支持 index、按任务只加载需要的 stem、多 crop、固定逻辑 `length`，并可派生 `accompaniment=drums+bass+other`。`scripts/mossland-codec/tasks.py` 已扩展 `separate_drums/separate_bass/separate_other`，保留旧 `separate_accompaniment`。新增配置 `scripts/configs/experiment/musdb18hq-four-stem-separation.yaml`：只启用 `separate_vocals/separate_drums/separate_bass/separate_other`，使用 MUSDB18-HQ train split，沿用 CoDiCodec paper repro 的 12 层/512 维模型口径，显式 `use_fsq=false`、`num_latents=512`、`bottleneck_channels=512`，summary 浮点数 `512*512` 等于输入 representation 浮点数 `4*1024*64`，表示结构上 1:1 的 0 压缩率分轨训练。验证通过：`python -m py_compile scripts/data/datasets.py scripts/mossland-codec/tasks.py scripts/mossland-codec/models.py`、`python -m pytest -q tests/mossland_codec/test_wrapper_imports.py tests/mossland_codec/test_tasks.py tests/test_separation_dataset.py`（47 passed）、`python -m scripts.train experiment=musdb18hq-four-stem-separation --cfg job`。MUSDB18-HQ 已用 `--no-proxy` 下载到 `/inspire/sj-ssd3/project/embodied-multimodality/public/Sonata/data/dataset/musdb18hq/musdb18hq.zip`，文件大小精确匹配 Zenodo 记录 `22656664047` 字节；已解压到同目录，顶层就是 `train/` 和 `test/`，完整性检查为 train 100 首、test 50 首、共 750 个 wav、缺失 stem 数 0；yaml 的 `data.dataset.dataset.dirs` 已改为 `/inspire/sj-ssd3/project/embodied-multimodality/public/Sonata/data/dataset/musdb18hq`。训练 smoke 已通过：单卡 `crops_per_file=1` 1 step loss 约 `0.133`，默认 `crops_per_file=16`/`num_workers=0` 1 step loss 约 `0.0823`，默认 `crops_per_file=16`/`num_workers=8` 1 step loss 约 `0.0763`。
- 2026-06-14 14:25 UTC 已完成飞书 `Multi-task audio codec` benchmark 复现表写入。实验/代码集中在 `scripts/mossland-codec/eval_benchmark/`；已按用户要求停止所有无关后台任务，未启动新的评测或训练。飞书 docx token `DC6cdohbXoGpyjx01wac1gDynHF`，原 reconstruction 复现表 block id `TNEkd1SBJogrkzxhl3jcZAGjnRd`；user 身份 `赵国杰` 授权成功后，用 `tables/feishu_reconstruction_repro.xml` 执行 `docs +update --command block_replace` 成功。最新读回文档 revision `601`，匹配新表 block `doxcnChCcBLierTcKps8DrYwBAd`，且与本地 CSV 逐行一致：29 行含表头、28 个数据行、13 列、0 个空单元格，并包含 MusicCaps-HF、EnCodec/DAC/CoDiCodec 本地 ViSQOL 行和 `未复现` 标记。新增 `generate_feishu_reconstruction_table.py`，本地保留 `tables/feishu_reconstruction_repro.{csv,md,xml}`；只填本地已完成的 MusicCaps-HF reconstruction 指标，未跑模型/指标写 `未复现` 或 `—`。本地 EnCodec/DAC/CoDiCodec full 5355 条 ViSQOL summary：EnCodec mean `4.266315`，DAC mean `4.272492`，CoDiCodec mean `4.185847`。飞书表数据源明确为第三方 MusicCaps-HF 镜像 `5355/5521`，不是官方 YouTube 音频完整复现；飞书原表 `Mel distance`/`STFT distance` 不能用本地 `LSD`/`MRSTFT` 冒充。
- 2026-06-14 13:30 UTC 本轮新增 `scripts/mossland-codec/eval_benchmark/generate_tables.py`，可从本地 `summary.json` 和 ViSQOL CSV 生成复现表到 `scripts/mossland-codec/eval_benchmark/tables/`，当前已生成 `codec_full_musiccaps_hf`、`encodec_rate_quality_full_musiccaps_hf`、`semanticodec_100_musiccaps_hf`、`sr300_musiccaps_hf`、`separation_fulltrack_musdb18hq` 的 Markdown/CSV。已把仓库根 `runs/` 里的早期 smoke 产物迁入 `scripts/mossland-codec/eval_benchmark/runs/root_migrated/`，满足用户要求实验产物集中在 eval_benchmark 下。新增并启动 `scripts/mossland-codec/eval_benchmark/watch_semanticodec_full_metrics.sh`，PID `149894`，日志 `scripts/mossland-codec/eval_benchmark/logs/semanticodec_full_metrics_watcher.log`；它会等三档 SemantiCodec full manifest 完整后自动跑 CLAP/VGGish 并刷新 `tables/`。验证通过：`python -m py_compile scripts/mossland-codec/eval_benchmark/generate_tables.py`、`bash -n scripts/mossland-codec/eval_benchmark/watch_semanticodec_full_metrics.sh`、`python -m pytest -q tests/mossland_codec/test_eval_benchmark.py`，结果 `65 passed`。当前后台仍在跑 full codec ViSQOL 与 SemantiCodec full 5355：ViSQOL CSV 行数约 CoDiCodec/DAC/EnCodec/SNAC/WavTokenizer `4950/4866/4852/4942/4957`（含 header），Mossland full ViSQOL 已完整 `5356` 行；SemantiCodec full token-rate 25/50/100 日志进度约 `3000/2900/2900` of `5355`。`generate_tables.py` 会把未完成 full ViSQOL 标为 `ViSQOL partial N/5355`。
- 2026-06-14 当前用户要求用 `scripts/mossland-codec` 复现 `docs/papers/codicodec.pdf` 的 CoDiCodec 模型并分析当前 Mossland 模型/训练与论文差距，最终给一个 experiment YAML。已把正确参考仓库 `https://github.com/SonyCSLParis/music2latent` 的 `training` 分支 clone 到 `/tmp/music2latent-training`，HEAD `3786807aaf60ea498bcd2f97d7a4c1c2c67df999`；该分支是 Music2Latent 训练代码，不是 CoDiCodec 论文最终 Transformer/FSQ 代码。论文关键信息已从 PDF 抽取到 `/tmp/codicodec.txt`：CoDiCodec 采用 hop=1024/window=2048、frontend `[3,3,3,1]` 和 `[64,128,256,512]`、encoder/upsampler/consistency model 各 12 transformer blocks、hidden_dim=512、head_dim=128、mlp_mult=4、K=128/d_lat=4、FSQ levels 11^4、FSQ-dropout p=0.75、RAdam lr=1e-4 beta=(0.9,0.999)、cosine decay 到 0、EMA 0.9999、consistency step 初始 0.1/final exponent 2、random mixing p=0.5、batch 20、2M iterations、单 A100 约两周、约 150M 参数；训练数据为 MTG-Jamendo、DNS Challenge 4 speech/general audio、M4Singer，权重 `[4,1.5,4,1]`，MusicCaps 评测并确认不在训练集。当前 `scripts/configs/experiment/mossland-codec.yaml` 是 494M 多任务配置（dim=768、22 层、5 tasks、PreparedSeparationDataset/RoFormer stems、lr=5e-5、fsq_dropout=0.6、默认无 random_mix），不是论文同款。已新增并更新 `scripts/configs/experiment/codicodec-paper-repro.yaml`：用 MosslandCodecTransformer 配回论文主架构/训练超参，只跑 `reconstruct`，数据目录按用户要求设为 `/inspire/sj-ssd3/project/embodied-multimodality/public/Sonata/data/raw/NETEASE_SPIDER`，并启用 `random_mix_prob=0.5`；128 卡训练口径为每卡 effective batch 16、全局 batch 2048、`max_steps/lr_schedule_total_steps/consistency_step_total_steps=20000`、`lr_warmup_steps=100`，约 4096 万样本，接近论文 batch 20 跑 200 万步的 4000 万样本；仍不完全相同，因为数据不是论文四数据源加权混合，且原官方 CoDiCodec 训练代码不可得。
- 2026-06-14 13:28 UTC 修复 `codicodec-paper-repro` 真实训练启动失败：Hydra 报 `Error locating target 'scripts.data.datasets.Experiment_Dataset'` 的根因是 `scripts/data/datasets.py` 顶层 import 了当前环境缺失且未使用的 `aeiou.core.is_silence` 和 `pedalboard.io.AudioFile`；已删除这两个未使用 import。验证通过：直接 `import scripts.data.datasets`；`python -m scripts.train experiment=codicodec-paper-repro trainer.max_steps=1 trainer.num_sanity_val_steps=0 +trainer.limit_val_batches=0 callbacks.demo_callback.demo_every=999999 data.num_workers=2 trainer.devices=1 extras.print_config=false` 完成 1 step，模型参数 `148M`、loss 约 `0.0701`。`bash/start_train_multinode.sh` 默认加入 `HYDRA_FULL_ERROR=1`。
- 2026-06-14 13:36 UTC 修复 128 卡 run 的 DDP unused-parameters 失败和 raw 数据扫描压力：`codicodec-paper-repro.yaml` 改为 `trainer.strategy=ddp_find_unused_parameters_true`，因为 reconstruction-only 训练步会让部分 task/conditioning 参数不参与 loss；`SampleDataset` 新增 `index_file/index_wait_timeout_seconds`，有索引直接读，没有索引时用 `.lock` 让一个进程扫描并原子写入，其他 rank 等待，锁等待超时后会删除旧锁并重试。正式配置的索引路径是 `/inspire/sj-ssd3/project/embodied-multimodality/public/zhaoguojie/Mossland/tmp/codicodec-paper-repro/netease_spider_audio.index.list`。验证通过：小目录索引 smoke；本机 `CUDA_VISIBLE_DEVICES=3,4` 两卡 DDP 1-step，覆盖临时两条 wav、`crops_per_file=1`，完成且 loss 约 `0.0265`，未再触发 unused-parameters 错误。
- 2026-06-14 13:39 UTC 修复 `SampleDataset.index_file` 父目录不存在时创建 `.lock` 报 `FileNotFoundError`：现在抢锁前会先 `os.makedirs(index_dir, exist_ok=True)`。已用不存在父目录的临时 index 和正式 index 路径 smoke 验证；验证期间生成的 2 行 `tmp/codicodec-paper-repro/netease_spider_audio.index.list` 已删除，避免正式 run 误读。
- 2026-06-14 12:45 UTC 已按用户要求安装 `https://github.com/obra/superpowers` 和 `https://github.com/colbymchenry/codegraph`。`superpowers` 已在 Codex plugin cache 中可用：`~/.codex/.tmp/plugins/plugins/superpowers/`，manifest 版本 `5.1.3`，marketplace entry 指向 `./plugins/superpowers`；当前 API 会话无法执行交互式 `/plugins`，后续 Codex UI/CLI 重启后可从插件入口启用。`codegraph` 使用 standalone installer 安装到 `/root/.codegraph/versions/v1.0.1`，命令链接 `/root/.local/bin/codegraph`，已运行 `codegraph install --target codex --location global --yes` 写入 `~/.codex/config.toml` 和 `~/.codex/AGENTS.md`，并已 `codegraph telemetry off`。当前仓库已 `codegraph init .`，`.codegraph/` 被 `.gitignore` 忽略；验证通过：`codegraph --version` -> `1.0.1`，`codegraph status .` 显示 `1,593` files / `24,425` nodes / `66,386` edges，`codegraph query MosslandCodecTransformer` 与 `codegraph explore "MosslandCodecTransformer generate_waveform"` 可返回主项目符号和源码。后续涉及代码导航、调用关系、blast radius 时优先用 `codegraph explore/query/node/status`；注意 CodeGraph 1.0 会主动索引 `.gitignore` 隐藏的嵌入式 git repo，当前 `.codegraph/errors.log` 主要来自 `tmp/eval_metric_refs/google-research` 等临时外部 repo 的缺失路径，主项目索引仍可用，使用结果时不要把 `tmp/` baseline refs 误当 Mossland 主代码。
- 2026-06-14 11:03 UTC 已按用户要求临时汇报当前已测结果表并继续并发。Open-Unmix UMXL full-track `museval`、`eval_none`、`eval_clap` 已完成并写入 `docs/eval-benchmark-report.md`：vocals/accompaniment SDR mean `5.00559`/`15.2555`，distance SI-SDR `5.78740`/`13.3919`，FAD-CLAP `0.435731`/`0.137541`；UMXL 标注为额外/非标准训练数据口径。HDemucs-MMI full-track `museval`、`eval_none`、`eval_clap` 也已完成并写入报告：vocals/accompaniment SDR mean `6.18269`/`16.1895`，distance SI-SDR `8.12349`/`14.7610`，FAD-CLAP `0.311829`/`0.176019`；MMI 标注为额外训练/变体口径。Demucs MDX Extra full-track `museval`、`eval_none`、`eval_clap` 已完成并写入报告：vocals/accompaniment SDR mean `7.23599`/`17.7316`，distance SI-SDR `9.36715`/`16.1144`，FAD-CLAP `0.341449`/`0.137790`。BS-RoFormer v1.0.12 full-track `museval`、`eval_none`、`eval_clap` 已完成并写入报告：vocals/accompaniment SDR mean `9.44030`/`18.2987`，distance SI-SDR `10.3358`/`16.8025`，FAD-CLAP `0.308159`/`0.162335`。SCNet XL IHF full-track `museval`、`eval_none`、`eval_clap` 已完成并写入报告：vocals/accompaniment SDR mean `8.67012`/`20.1550`，distance SI-SDR `10.6988`/`17.6290`，FAD-CLAP `0.375005`/`0.128831`。当前仍在跑：full codec ViSQOL 五路约 `4691/4614/4600/4683/4697` CSV 行（CoDiCodec/DAC/EnCodec/SNAC/WavTokenizer，含 header）；SemantiCodec full token-rate 25/50/100 仍在推理。
- 2026-06-14 10:36 UTC 已把 MUSDB18-HQ full-track FAD-CLAP diagnostics 写入 `docs/eval-benchmark-report.md`：Mossland chunked、HTDemucs、HTDemucs-FT、Demucs MDX、Open-Unmix UMXHQ、ByteSep ResUNet143 均已 100 行完成。启动 BS-RoFormer MUSDB18HQ-only v1.0.12 full-track 推理到 `runs/baselines_musdb18hq_fulltrack_bs_roformer/`，日志 `logs/fulltrack_bs_roformer_infer.log`，当前约 `processed=17/50`，final manifest 会在 adapter 收尾后写出；`mdx_extra` full-track 推理约 `40/50`。MSST adapter 已扩展支持 `scnet`，SCNet XL IHF v1.0.15 config/checkpoint 已下载到 `tmp/eval_baseline_refs/music-source-separation-training/configs/downloaded/config_musdb18_scnet_xl_more_wide_v5.yaml` 和 `checkpoints/model_scnet_ep_36_sdr_10.0891.ckpt`，尚未启动推理。验证通过：`python -m py_compile scripts/mossland-codec/eval_benchmark/baselines/msst_baseline.py scripts/mossland-codec/eval_benchmark/run.py scripts/mossland-codec/eval_benchmark/aggregate_results.py`；`python -m pytest -q tests/mossland_codec/test_eval_benchmark.py` 结果 `64 passed`。
- 2026-06-14 10:27 UTC 用户要求临时列出当前已测结果并继续；已在对话中汇报压缩表：MusicCaps-HF full codec/reconstruction、SR 300 条、MUSDB18-HQ full-track museval。最新轮询：full codec ViSQOL 五路仍在跑，CSV 行数约 CoDiCodec/DAC/EnCodec/SNAC/WavTokenizer `3473/3416/3402/3464/3477`（含 header），Mossland full ViSQOL summary 已完成；full-track CLAP 六路仍活着但无 summary；SemantiCodec full token-rate 25/50/100 三路仍在推理；Open-Unmix UMXL `museval` 和 `eval_none` 已在跑但未出 summary；`mdx_extra` 正在推理约第 8 首。`hdemucs_mmi` full-track manifest 已完整 100 行，已启动 `museval` 和 `eval_none` 后评测：日志 `logs/fulltrack_hdemucs_mmi_museval.log`、`logs/fulltrack_hdemucs_mmi_eval_none.log`。新开 3 个只读子代理 Raman/Newton/Goodall 分别梳理 codec、separation、SR/stereo 的 SOTA/官方 baseline 缺口；主会话继续负责 `docs/eval-benchmark-report.md`、`docs/memory/current.md`、`docs/code-index.md` 写入。
- 2026-06-14 10:07 UTC 用户再次要求“先临时告诉目前已测结果列表，然后继续”。已在对话中汇报核心本地实测表：MusicCaps-HF full codec/reconstruction、AERO 300、MUSDB18-HQ full-track museval、MUSDB18-HQ full-track distance diagnostics。已确认当前 8 张 PPU 都有 full-track `eval_none`/`eval_clap` 进程占用显存，但 `PPU-Util` 显示 0%，这些旧任务主要卡在 CPU/音频 I/O/特征聚合且无中间日志；不要误判为空闲后杀掉，后续新任务优先用 `run.py --num-shards/--shard-id --progress-every`。full codec ViSQOL 五路仍在跑：CoDiCodec/DAC/EnCodec/SNAC/WavTokenizer CSV 约 `3293/3239/3226/3285/3298` 行（含 header），Mossland summary 已完成。已开 3 个只读子代理并行梳理 codec、MUSDB18-HQ separation、BWE/SR/stereo 后续官方/SOTA baseline 接入缺口；主会话继续负责 `docs/eval-benchmark-report.md`、`docs/memory/current.md`、`docs/code-index.md`。
- 2026-06-14 10:12 UTC 已根据子代理建议继续并发：启动 SemantiCodec full 5355 三档 `token-rate=25/50/100`，输出 `runs/baselines_musiccaps_full_semanticodec_tr{25,50,100}/`，日志 `logs/semanticodec_full_tr{25,50,100}.log`，当前已稳定产出约 `62/63/65` 个 wav。启动 separation 变体 full-track 推理：`hdemucs_mmi`、`mdx_extra`、Open-Unmix `umxl`，输出 `runs/baselines_musdb18hq_fulltrack_{hdemucs_mmi,mdx_extra,openunmix_umxl}/`，日志 `logs/fulltrack_{hdemucs_mmi,mdx_extra,openunmix_umxl}_infer.log`；当前三者在下载官方权重或首条准备阶段，尚未出 wav，`mdx_extra` 和 `umxl` 后续报告必须标注额外训练数据/潜在 MUSDB test 污染，不混作公平 SOTA 表。已派 worker Noether 写 `scripts/mossland-codec/eval_benchmark/baselines/msst_baseline.py` 和对应轻量测试，准备 ZFTurbo/MVSep `Music-Source-Separation-Training` adapter；主会话不改这些 worker-owned 文件。Demucs MDX full-track `eval_none` 已完成并写入报告 distance 表：vocals SI-SDR/SNR/LSD/MRSTFT `6.97355/8.11098/47.3609/0.391326`，accompaniment `12.8190/12.9669/14.6468/0.174720`。HTDemucs full-track `eval_none` 和所有 full-track `eval_clap` 仍在跑；full codec ViSQOL 五路约 `3367/3312/3299/3359/3371` 行（CoDiCodec/DAC/EnCodec/SNAC/WavTokenizer，含 header）。
- 2026-06-14 10:20 UTC Noether 完成 MSST adapter 第一版，新增 `baselines/msst_baseline.py` 和 3 个轻量测试；主会话验证通过：`python -m py_compile scripts/mossland-codec/eval_benchmark/baselines/msst_baseline.py scripts/mossland-codec/eval_benchmark/run.py scripts/mossland-codec/eval_benchmark/aggregate_results.py && python -m pytest -q tests/mossland_codec/test_eval_benchmark.py`，结果 `63 passed`。已更新 `docs/code-index.md`、`scripts/mossland-codec/eval_benchmark/README.md` 和 `docs/eval-benchmark-report.md`，记录 MSST adapter 仍是 smoke/待真实指标阶段。BS-RoFormer v1.0.12 config/ckpt 已下载：`configs/downloaded/config_bs_roformer_384_8_2_485100.yaml` 和 `checkpoints/model_bs_roformer_ep_17_sdr_9.6568.ckpt`；已启动 1-track smoke 到 `runs/baselines_musdb18hq_fulltrack_bs_roformer_smoke/`，日志 `logs/fulltrack_bs_roformer_smoke.log`。HTDemucs full-track `eval_none` 已完成并写入报告：vocals SI-SDR/SNR/LSD/MRSTFT `7.85745/8.56662/42.8458/0.362364`，accompaniment `14.6349/14.6318/11.7817/0.148055`。最新轮询：SemantiCodec full `146/145/146` wav，`hdemucs_mmi` `48` wav，Open-Unmix `umxl` `58` wav，`mdx_extra` 仍首条/无 wav；full-track CLAP 仍未落 summary。
- 2026-06-14 10:10 UTC 用户要求临时汇报已测结果表并继续。已在对话中汇报 codec/reconstruction full、EnCodec rate-quality full、SR/BWE 扩展、mono-to-stereo 100、MUSDB18-HQ full-track museval 核心表。随后轮询并更新 `docs/eval-benchmark-report.md`：AERO official `12-48` `hl=256` 的 100 条 VGGish 已完成，FAD-VGGish `4.84274`，CLAP/距离行已把 `pending` 替换为该值；MUSDB18-HQ full-track `eval_none` distance diagnostics 已完成并表四路：Mossland chunked、HTDemucs-FT、Open-Unmix、ByteSep ResUNet143。HTDemucs 与 Demucs MDX full-track `eval_none` 仍在后台运行；full codec ViSQOL 五路仍在跑，CSV 行数约 CoDiCodec `3072`、DAC `3023`、EnCodec `3010`、SNAC `3065`、WavTokenizer `3078`（含 header），Mossland full ViSQOL 已完成。
- 2026-06-14 10:20 UTC 为继续利用空闲 PPU 且不新增 ViSQOL，已启动 6 个后台任务并限制 `OMP/MKL/OPENBLAS/NUMEXPR=4`：AERO 12 kHz -> 48 kHz 300 条主 `hl=128` 推理在 PPU 2，输出 `runs/baselines_musiccaps_sr300_12000_aero/`，日志 `logs/aero_sr300_12000_hl128.log`；AERO `hl=256` 300 条推理在 PPU 3，输出 `runs/baselines_musiccaps_sr300_12000_aero_hl256/`，日志 `logs/aero_sr300_12000_hl256.log`；full-track CLAP 四路在 PPU 4-7：Mossland chunked、ByteSep ResUNet143、HTDemucs-FT、Open-Unmix，输出各自 `eval_clap/`，日志 `logs/fulltrack_{mossland,bytesep,htdemucs_ft,openunmix}_eval_clap.log`。暂不启动新的 VGGish/ViSQOL，先观察 CLAP 与 AERO 推理资源占用。
- 2026-06-14 10:25 UTC AERO `hl=128`/`hl=256` 300 条推理均已完成，两个 `aero_manifest.jsonl` 都是 300 行。已在 PPU 2/3 启动对应 300 条 `eval_clap`：`runs/baselines_musiccaps_sr300_12000_aero/eval_clap/` 和 `runs/baselines_musiccaps_sr300_12000_aero_hl256/eval_clap/`，日志 `logs/aero_sr300_12000_hl128_eval_clap.log`、`logs/aero_sr300_12000_hl256_eval_clap.log`。AERO 300 的 VGGish 尚未启动，等 CLAP 完成后接着排。
- 2026-06-14 10:32 UTC AERO 300 条 CLAP/VGGish 均已完成并写入 `docs/eval-benchmark-report.md` 的 300 条 SR 扩展表：`hl=128` FAD-CLAP `0.110126`、FAD-VGGish `0.578600`、SI-SDR `13.9995`、SNR `14.4371`、LSD `42.7750`、MRSTFT `0.286422`；`hl=256` FAD-CLAP `0.205793`、FAD-VGGish `3.38589`、SI-SDR `9.89004`、SNR `9.96797`、LSD `36.4117`、MRSTFT `0.355104`。随后在 PPU 2/3 启动 Demucs/MDX full-track `eval_clap`，输出 `runs/baselines_musdb18hq_fulltrack_demucs/eval_clap/` 与 `runs/baselines_musdb18hq_fulltrack_demucs_mdx/eval_clap/`，日志 `logs/fulltrack_demucs_eval_clap.log`、`logs/fulltrack_mdx_eval_clap.log`。
- 2026-06-14 10:45 UTC 为后续 long-form 指标并发，已增强 `scripts/mossland-codec/eval_benchmark/run.py`：新增 `--max-items`、`--num-shards/--shard-id` 和 `--progress-every`，默认全量行为不变；分片用于多个独立输出目录，正式 FAD 表仍需合并全量 rows 后重新聚合。已更新 `scripts/mossland-codec/eval_benchmark/README.md`、`docs/eval-benchmark-report.md` 和 `docs/code-index.md`。验证通过：`python -m py_compile scripts/mossland-codec/eval_benchmark/run.py`；`python -m pytest -q tests/mossland_codec/test_eval_benchmark.py` 结果 `58 passed`。当前 8 张 PPU 仍被 full-track `eval_none`/`eval_clap` 占用，full codec ViSQOL 约 EnCodec `3149`、DAC `3162`、SNAC `3207`、WavTokenizer `3220`、CoDiCodec `3214` 行（含 header），未新增 ViSQOL。
- 2026-06-14 11:05 UTC 新增 `scripts/mossland-codec/eval_benchmark/aggregate_results.py`，用于合并一个或多个 `results.jsonl`，重新计算距离指标聚合和 row embedding FAD，并输出合并后的 `results.jsonl`、`summary.json/csv/md`；README、报告和 code-index 已补充。验证通过：`python -m py_compile scripts/mossland-codec/eval_benchmark/aggregate_results.py scripts/mossland-codec/eval_benchmark/run.py`；`python -m pytest -q tests/mossland_codec/test_eval_benchmark.py` 结果 `59 passed`。后台状态未变：full-track `eval_none`/`eval_clap` 仍无 summary 但进程存活，full codec ViSQOL 约 EnCodec `3195`、DAC `3208`、SNAC `3253`、WavTokenizer `3266`、CoDiCodec `3261` 行（含 header）。
- 2026-06-14 08:45 UTC 用户要求先临时汇报已测结果表后继续；已在对话中汇报 codec/reconstruction、EnCodec rate-quality、SR300、mono-to-stereo100、MUSDB18-HQ full-track museval 核心表。随后轮询后台：SemantiCodec token-rate 25/50/100 的 100 条 VGGish 已完成并写入 `docs/eval-benchmark-report.md`，FAD-VGGish 分别为 `3.53085`、`3.73924`、`2.69508`；EnCodec 3/6/12 kbps full 5355 条 VGGish 也已完成并写入报告，FAD-VGGish 分别为 `2.92602`、`2.00266`、`1.40697`；A2SB 300 当前约 `257/300` wav 且 `eval_clap` 未生成；full codec ViSQOL 五路仍在跑，CSV 行数约 EnCodec `2568`、DAC `2580`、SNAC `2615`、WavTokenizer `2629`、CoDiCodec `2621`（含 header）。
- 2026-06-14 08:55 UTC 为继续利用空闲 PPU 且不增加 ViSQOL 压力，已启动 UniverSR SR300 16/24 kHz 推理：输出分别为 `runs/baselines_musiccaps_sr300_16000_universr/`、`runs/baselines_musiccaps_sr300_24000_universr/`，日志 `logs/universr_sr300_16000.log`、`logs/universr_sr300_24000.log`，使用 PPU 2/3，当前均已看到 `processed=10/300`。已启动 full-track separation `eval_none` 四路：Mossland chunked、ByteSep、HTDemucs-FT、Open-Unmix，分别在 PPU 4/5/6/7，日志 `logs/fulltrack_{mossland,bytesep,htdemucs_ft,openunmix}_eval_none.log`。A2SB 300 约 `267/300` wav，PPU 1 预留给 watcher 自动触发的 A2SB `eval_clap`。
- 2026-06-14 09:13 UTC A2SB 300 条 16 kHz low-band -> 44.1 kHz BWE 已完成推理和 CLAP/距离评测并写入 `docs/eval-benchmark-report.md`：`a2sb_manifest.jsonl` 300 行，FAD-CLAP `0.0287543`、SI-SDR `15.8270`、SNR `15.6961`、LSD `21.9842`、LSD-LF `5.20493`、LSD-HF `27.1967`、MRSTFT `0.191048`；VGGish/ViSQOL 未跑，表中标 pending。仍在后台运行：UniverSR SR300 16/24 kHz（约 `130/300`、`170/300`）、full-track `eval_none` 四路、full codec ViSQOL 五路。
- 2026-06-14 09:15 UTC 已启动 UniverSR SR300 CLAP watcher `logs/universr_sr300_eval_clap_watcher.log`，等待 `runs/baselines_musiccaps_sr300_16000_universr/universr_manifest.jsonl` 和 `runs/baselines_musiccaps_sr300_24000_universr/universr_manifest.jsonl` 到 300 行后自动运行 `eval_clap`，输出到各自 `eval_clap/`；不会启动 ViSQOL。
- 2026-06-14 09:36 UTC A2SB 300 与 UniverSR SR300 16/24 kHz 的 VGGish 已完成并写入 `docs/eval-benchmark-report.md`。A2SB300 FAD-VGGish `0.0517306`；UniverSR 16/24 kHz FAD-VGGish `12.8308`/`7.44455`。对应 ViSQOL 未跑，表中仍标 pending。仍在后台运行：full-track `eval_none` 六路、full codec ViSQOL 五路。
- 2026-06-14 09:32 UTC 为继续利用空闲 PPU，已追加启动 HTDemucs 与 Demucs MDX full-track `eval_none`，输出分别为 `runs/baselines_musdb18hq_fulltrack_demucs/eval_none/`、`runs/baselines_musdb18hq_fulltrack_demucs_mdx/eval_none/`，日志 `logs/fulltrack_demucs_eval_none.log`、`logs/fulltrack_mdx_eval_none.log`，使用 PPU 0/1。当前 full-track `eval_none` 共六路在跑：Mossland chunked、ByteSep、HTDemucs-FT、Open-Unmix、HTDemucs、Demucs MDX；full codec ViSQOL 五路仍在跑。
- 2026-06-14 07:37 UTC 用户要求先临时汇报已测结果后继续；已在对话中给出当前阶段表。随后补入新完成结果到 `docs/eval-benchmark-report.md`：EnCodec 48 kHz 12 kbps MusicCaps-HF full 5355 条 CLAP/距离评测完成，FAD-CLAP `0.0624319`、SI-SDR `8.72509`、SNR `9.33567`、LSD `17.6539`、MRSTFT `0.334432`；EnCodec 3/6/12/24 kbps rate-quality 表已完整。HTDemucs-FT 与 Demucs MDX MUSDB18-HQ full-track `museval_eval.py --keep-going` 也已完成并写入 full-track diagnostics：HTDemucs-FT vocals/accompaniment SDR mean `6.36377`/`14.4082`，Demucs MDX vocals/accompaniment SDR mean `5.40078`/`13.4701`，均 50/50 tracks 且无 errors。已启动 Mossland chunked full-track watcher `logs/mossland_fulltrack_chunked_museval_watcher.log`，等待 `runs/mossland_musdb18hq_test_fulltrack_chunked/prediction_manifest.jsonl` 到 100 行后自动跑 full-track museval。当前后台进度约：Mossland chunked full-track `30/100` rows、DiffStereo seed0 `93/100` wav、A2SB 300 `146/300` wav、full codec ViSQOL 各 baseline 约 `1950-2000/5355` CSV 行。
- 2026-06-14 07:55 UTC MusicCaps-HF 100 条 mono-to-stereo 扩展表继续补齐：DiffStereo official seed0 100 条完成 CLAP/VGGish/距离/空间指标，FAD-CLAP `0.319345`、FAD-VGGish `6.05737`、SI-SDR `0.805553`、fold-down SI-SDR `4.91585`、width `0.454957`、corr `0.650940`；DiffStereo seed1 已补 FAD-VGGish `6.00581`，Ambisonizer 已补 FAD-VGGish `2.27908`；s3a fallback decorrelator 100 条完成 CLAP/VGGish/距离/空间指标，FAD-CLAP `0.000894388`、FAD-VGGish `0.0258258`、SI-SDR `6.83952`、fold-down SI-SDR `60.9725`、width `0.324331`、corr `0.954480`。ViSQOL 仍暂缓，避免抢 full codec ViSQOL CPU。Mossland chunked full-track 已推理 `100/100` 并启动 full-track `museval_eval.py`，当前 museval 仍在跑；A2SB 300 约 `189/300` wav；full codec ViSQOL 约 `2100+/5355` 行。
- 2026-06-14 08:25 UTC Mossland chunked full-track `museval_eval.py --keep-going` 已完成并写入 `docs/eval-benchmark-report.md`，50/50 tracks、`eval.exit=0`、无 errors，manifest `metadata.prediction_source_length_ratio` 为 `1.0`。Full-track museval diagnostics：vocals SDR mean/median `-3.47622`/`-2.99503`、frame-median SDR mean/median `-1.95546`/`-1.92514`、SIR mean `0.993996`、SAR mean `-23.0928`；accompaniment SDR mean/median `-2.47170`/`-2.41407`、frame-median SDR mean/median `-2.43094`/`-2.37595`、SIR mean `14.2376`、SAR mean `-24.2739`。当前仍在后台运行：A2SB 300 推理约 `231/300` wav，stereo/A2SB watcher PID `479705` 会在 manifest 满 300 后自动跑 CLAP/距离评测；full codec ViSQOL 五路约 `2429-2487/5355` 行，继续不要加新 ViSQOL。
- 2026-06-14 08:35 UTC 已补齐 MusicCaps-HF 300 条 SR 扩展表的 VGGish FAD：Mossland SR@16/24/32k FAD-VGGish `1.91660`/`1.80922`/`1.79800`；AudioSR `1.01188`/`0.885993`/`0.881492`；FlowHigh `0.523028`/`0.511304`/`0.501722`；NU-Wave2 `0.525748`/`0.503030`/`0.504633`；FastWave `0.564108`/`0.564454`/`0.564130`。这些已写入 `docs/eval-benchmark-report.md`；SR300 ViSQOL 仍未排，避免抢 full codec ViSQOL。
- 2026-06-14 07:10-07:17 UTC 用户要求先临时汇报当前已测结果表后继续。已向用户汇报 codec reconstruction full、SemantiCodec 100、SR 300、A2SB 100、MUSDB18-HQ 10s separation 阶段结果。随后补入新结果：EnCodec 48 kHz rate-quality full MusicCaps-HF 已完成 3 kbps 与 6 kbps CLAP/距离评测，3 kbps FAD-CLAP `0.104829`、SI-SDR `3.50565`、LSD `18.3887`，6 kbps FAD-CLAP `0.0710060`、SI-SDR `6.17115`、LSD `17.9311`；12 kbps 推理仍在跑，约 `2715/5355` prediction wav，最终 manifest 会在 adapter 收尾时写入。Ambisonizer 官方 checkpoint 已下载到 `checkpoints/ambisonizer/ambisonizer_state_dict.pt`，SHA256 `1e40b98a21a464c77081cdf2c96a2a8944773a29c1964c77489ba607a754ce69`，1 条 smoke 成功，MusicCaps-HF seed0 100 条 mono-to-stereo CLAP/距离评测完成：FAD-CLAP `0.221796`、SI-SDR `3.96101`、fold-down SI-SDR `51.3382`、stereo width `0.543435`、channel corr `0.577997`。这些已写入 `docs/eval-benchmark-report.md`。
- 2026-06-14 07:19 UTC DiffStereo official sampler seed1 的 MusicCaps-HF 100 条 mono-to-stereo 已完成 `eval_none` 和 `eval_clap`，并写入 `docs/eval-benchmark-report.md` 100 条扩展表：FAD-CLAP `0.317013`、SI-SDR `0.963559`、SNR `0.288608`、LSD `31.7993`、MRSTFT `1.72279`、fold-down SI-SDR `4.95550`、stereo width `0.438714`、channel corr `0.671047`。DiffStereo seed0 仍在跑，约 `78/100` prediction wav。
- 2026-06-14 07:19 UTC 尝试下载 NU-Wave README 中的 Google Drive folder `1pJ6KCs34DNaTCqKXJZretD7MLcAOb4yR` 到 `checkpoints/nuwave/`，`logs/nuwave_gdrive_folder.log` 为空且没有落文件；NU-Wave 仍按缺 checkpoint/未接入处理。NU-Wave repo `tmp/eval_baseline_refs/nuwave` README 提到 issue #18 社区 checkpoint，可后续单独尝试。
- 2026-06-14 07:25 UTC 新增 `scripts/mossland-codec/eval_benchmark/fulltrack_infer.py`，在 eval 层为 Mossland full-track separation 做 chunked inference，不改模型核心。1 首 MUSDB18-HQ full-track smoke (`AM Contra - Heart Peripheral`) 已通过：vocals/accompaniment 两行 prediction 与 reference 都是 `9265664` frames，`museval_eval.py --keep-going` 无 errors。1-track museval 结果：vocals SDR mean `-6.01664`、accompaniment SDR mean `-2.92067`。这证明旧 `1.509s` full-track 问题已可通过 wrapper 修复；下一步可挂全量 `musdb18hq_test_fulltrack_manifest.jsonl` 到 `runs/mossland_musdb18hq_test_fulltrack_chunked/`。
- 2026-06-14 07:17 UTC 当前仍在后台运行：DiffStereo seed0/seed1 100 条（prediction wav 约 `74/100`、`95/100`；最终 manifest 通常收尾时一次性写）、A2SB 300（prediction wav 约 `116/300`）、HTDemucs-FT 和 Demucs MDX full-track `museval_eval.py`、EnCodec 12 kbps full reconstruction、full codec ViSQOL 五路。已有一个 stereo/A2SB watcher PID `479705`，会在 final manifest 写满后启动 `eval_none`/`eval_clap`；不要重复启动 watcher。EnCodec 6 kbps eval 已完成；Ambisonizer 100 条已完成。不要杀 CPU-heavy ViSQOL 进程，除非确认不再需要。
- 2026-06-14 06:48-06:55 UTC 已按用户要求临时汇报当前已完成实测结果表，口径仍是本地推理/统一 pipeline 指标，不照搬论文数值。已启动/分派后续并发：SemantiCodec 三档完成后补 CLAP/距离评测、EnCodec 3/6/12 kbps rate-quality 完成后补 CLAP/距离评测、HTDemucs-FT/MDX full-track 完成后补 `museval_eval.py`、Ambisonizer adapter 核查。主会话负责 `docs/eval-benchmark-report.md` 与记忆文档写入，worker 不写报告。
- 2026-06-14 06:55 UTC 新挂 EnCodec 48 kHz 12 kbps full MusicCaps-HF reconstruction 到 `runs/baselines_musiccaps_full_encodec_bw12/`，日志 `logs/encodec_musiccaps_full_bw12.log`，使用 `CUDA_VISIBLE_DEVICES=2`。当前长跑产物以 prediction wav 数计：SemantiCodec tr25/tr50/tr100 约 `35/36/92`，EnCodec 3/6/12 约 `2931/2332/0`，A2SB 300 约 `77`，DiffStereo seed0/seed1 约 `50/58`，HTDemucs-FT/MDX full-track wav 约 `94/60`；这些 adapter 多数结束时才一次性写 manifest，manifest missing 不代表失败。
- 2026-06-14 06:58 UTC SemantiCodec token-rate 25/50/100 的 MusicCaps-HF 100 条 reconstruction 推理和 CLAP/距离评测已全部完成并写入 `docs/eval-benchmark-report.md`。结果：tr25 FAD-CLAP `0.118665`、SI-SDR `-37.4404`、LSD `10.6071`；tr50 FAD-CLAP `0.135771`、SI-SDR `-34.4658`、LSD `10.0890`; tr100 FAD-CLAP `0.0838395`、SI-SDR `-35.1448`、LSD `9.98482`。Ambisonizer adapter `baselines/ambisonizer_baseline.py` 已实现并通过 `py_compile`/`--help`，官方 repo commit `37c8761` 已 clone；官方 Google Drive checkpoint `checkpoints/ambisonizer/ambisonizer_state_dict.pt` 仍缺失，`gdown 1S9VPkvPs0LI3oZzZRwoeLmdbm6BJpUph` 绕过代理后卡住且无文件，暂不进结果表。
- 2026-06-14 07:02 UTC EnCodec 12 kbps 首次启动日志为空且无产物，疑似和 SemantiCodec 抢同一物理 PPU 失败；已在空闲物理 PPU 2 重新启动，PID `798553`，日志仍为 `logs/encodec_musiccaps_full_bw12.log`。同一时刻 EnCodec 3/6 wav 约 `5327/4225`，A2SB 300 约 `93`，DiffStereo seed0/seed1 约 `60/73`，MDX full-track wav 已 `100` 且 watcher 已启动 `museval_eval`，HTDemucs-FT full-track `museval_eval` 仍在跑。
- 2026-06-14 07:04 UTC EnCodec 12 kbps 用普通 `nohup ... &` 后台方式会很快消失且日志为空；foreground 1 条 smoke 证明 `--bandwidth 12` 本身可跑通。已改用 `setsid -f` 重启，PID `824720`，物理 PPU 2，约 07:06 UTC 已产生 `549` 个 wav。EnCodec bw3 manifest 已 5355 行，watcher 已启动 `runs/baselines_musiccaps_full_encodec_bw3/eval_clap`；bw6 仍在推理约 `4657/5355`。
- 2026-06-14 正在扩展 `scripts/mossland-codec/eval_benchmark/` 官方 baseline 本地实测。已新增/接入 CoDiCodec、SNAC、WavTokenizer、NU-Wave2、FlowHigh、HTDemucs、Open-Unmix adapter，并把当前本地实测汇总写入 `docs/eval-benchmark-report.md` 顶部。新增 adapter 测试已跑 `python -m pytest -q tests/mossland_codec/test_eval_benchmark.py`，结果 `54 passed`。
- 当前评测口径：MusicCaps 仍使用第三方 HF 音频镜像 `mahendra0203/musiccaps_processed_full`，不是 Google 官方 YouTube 音频完整复现；MUSDB18-HQ separation 是 test split 非静音 10 秒窗口工程评测，不是 SiSEC full-track 官方口径；`fad_vggish` 是 `torchvggish_raw`，`fad_clap` 是 HF `laion/clap-htsat-unfused`。
- 已完成本地指标：Mossland、EnCodec、DAC、SNAC、WavTokenizer、CoDiCodec 的 MusicCaps-HF reconstruction 5355 条 full CLAP/VGGish/距离指标已完成；CoDiCodec 100 条 ViSQOL `4.17106` 也已完成但不混作 full 5355 统计。AudioSR、FlowHigh、NU-Wave2 的 16/24/32 kHz SR 100 条 CLAP/VGGish/ViSQOL 已完成。Mossland、HTDemucs、ByteSep ResUNet143 与 Open-Unmix MUSDB18-HQ 50 track 非静音 10 秒 CLAP/VGGish/museval 已完成。DiffStereo MusicCaps-HF mono-to-stereo seed0 前 20 条 official sampler 已完成。
- 当前仍有 eval_benchmark 后台指标进程：full codec ViSQOL 中 EnCodec、DAC、SNAC、WavTokenizer、CoDiCodec 5 路 5355 条仍在 Google ViSQOL binary 中运行，Mossland full 5355 ViSQOL 已完成，MOS-LQO mean `2.97158`、median `2.78838`。2026-06-14 06:23 UTC 轮询时 full codec ViSQOL 进度约 EnCodec `1346/5355`、DAC `1357/5355`、SNAC `1370/5355`、WavTokenizer `1378/5355`、CoDiCodec `1374/5355`（CSV 行数含 header）。FastWave 100 条/档 ViSQOL 已完成并写入报告：16k `2.11795`、24k `2.24579`、32k `2.39414`。A2SB 100 条 BWE/SR 推理和所有指标已完成并写入报告。AERO 100 条 12 kHz -> 48 kHz 推理和 CLAP/VGGish/ViSQOL/距离指标已完成。不要杀这些 CPU ViSQOL 评估进程，除非确认不再需要。
- 2026-06-14 已新增 `scripts/mossland-codec/eval_benchmark/baselines/universr_baseline.py`，接官方 `woongzip1/UniverSR` 与 HF `woongzip1/universr-audio`。已下载 checkpoint 到 `checkpoints/universr-audio`，已跑 MusicCaps-HF SR@16000/24000 各 100 条推理并完成 CLAP/VGGish/ViSQOL/距离指标。当前结果偏弱：16k FAD-CLAP `0.580648`、FAD-VGGish `15.9728`、ViSQOL `2.20985`、SI-SDR `-47.2237`、LSD `37.2084`；24k FAD-CLAP `0.209277`、FAD-VGGish `9.75928`、ViSQOL `2.31656`、SI-SDR `-45.1582`、LSD `29.9921`。需抽听/核对 adapter 的 bandwidth-limited 输入策略。
- 2026-06-14 已新增 `scripts/mossland-codec/eval_benchmark/baselines/aero_baseline.py`，接官方 AERO repo `tmp/eval_baseline_refs/aero` 与 Google Drive checkpoint `tmp/eval_baseline_refs/aero/checkpoints/12-48/aero-nfft=512-hl=128/checkpoint.th`。官方 repo 只带 `4-16` hydra config，adapter 复用同结构并覆盖 `lr_sr=12000/hr_sr=48000`，PyTorch 2.6+ 加载老 checkpoint 需 `weights_only=False`。已完成 MusicCaps-HF 100 条 12 kHz -> 48 kHz 推理和 CLAP/VGGish/ViSQOL/距离指标：FAD-CLAP `0.128436`、FAD-VGGish `0.833671`、ViSQOL `1.53094`、SI-SDR `13.9046`、SNR `14.3488`、LSD `43.2403`、LSD-LF `5.83694`、LSD-HF `50.5173`、MRSTFT `0.282232`。
- 2026-06-14 AERO adapter 接入后已验证：`python -m py_compile scripts/mossland-codec/eval_benchmark/baselines/aero_baseline.py scripts/mossland-codec/eval_benchmark/baselines/a2sb_baseline.py scripts/mossland-codec/eval_benchmark/baselines/s3a_baseline.py` 通过；`python -m pytest -q tests/mossland_codec/test_eval_benchmark.py` 结果 `54 passed`。
- 2026-06-14 FastWave 已由 worker 新增 `scripts/mossland-codec/eval_benchmark/baselines/fastwave_baseline.py`，官方 repo `tmp/eval_baseline_refs/fastwave/FastWave` commit `7569045`，官方 Google Drive checkpoint `checkpoints/fastwave/checkpoint-epoch140.pth`。1 条 smoke 已跑通，输出 48 kHz mono，统一 pipeline `fad-backend none` 指标：SI-SDR `10.5768`、SNR `10.7824`、LSD `29.2701`、LSD-LF `4.65085`、LSD-HF `35.7059`、MRSTFT `0.318847`。MusicCaps-HF SR 16/24/32 kHz 各 100 条推理已完成，`runs/baselines_musiccaps_sr100_fastwave/fastwave_manifest.jsonl` 300 行；CLAP/VGGish/ViSQOL/距离指标已完成并写入 `docs/eval-benchmark-report.md`，ViSQOL MOS-LQO：16k `2.11795`、24k `2.24579`、32k `2.39414`。
- 2026-06-14 AEROMamba 已由 worker 调查并跑通 1 条官方 `predict.py` smoke。官方 repo `tmp/eval_baseline_refs/aeromamba/aeromamba` commit `67d3c8a`；官方协议是 `11025 Hz -> 44100 Hz`，数据是 MUSDB18-HQ mixture / 私有 PianoEval，不对应当前 `16/24/32 kHz -> 48 kHz` 主 SR buckets。官方 SharePoint checkpoint 需要 Microsoft login，当前无法匿名下载；使用 HF 镜像 `innova-ai/AEROMamba` 的 `checkpoints/aeromamba/checkpoint.th` 约 415M。当前 Python 3.12/Torch 2.7 环境无法正常安装 `mamba-ssm==1.1.3.post1`；worker 用 tmp shim 和 `sitecustomize.py` 绕过 Mamba extension 导入并让 `torch.load(weights_only=False)`，1 秒 11.025 kHz 输入成功输出 44.1 kHz mono。严格复现需要 Python 3.10/Torch 1.12.1/CUDA 11.3 隔离环境；当前不放入主表，只作为工程 smoke。
- 2026-06-14 NVIDIA A2SB 已接入 `scripts/mossland-codec/eval_benchmark/baselines/a2sb_baseline.py`，官方 repo `tmp/eval_baseline_refs/a2sb/diffusion-audio-restoration`，HF checkpoint `nvidia/audio_to_audio_schrodinger_bridge` 位于 `checkpoints/a2sb`。A2SB 协议是 44.1 kHz mono bandwidth-extension/inpainting，不混入当前 `16/24/32 kHz -> 48 kHz` SR 表。MusicCaps-HF 100 条 16 kHz low-band -> 44.1 kHz BWE 推理已完成，`a2sb_manifest.jsonl` 100 行；CLAP/VGGish/ViSQOL/距离指标已写入报告：FAD-CLAP `0.0357285`、FAD-VGGish `0.0920404`、ViSQOL `4.14483`、SI-SDR `16.0159`、SNR `15.6913`、LSD `20.9284`、LSD-LF `5.03267`、LSD-HF `25.8952`、MRSTFT `0.187575`。
- 2026-06-14 `htdemucs_ft` 官方 Demucs stronger baseline 已完成 MUSDB18-HQ 非静音 10 秒 50 track 工程评测：通过 `scripts/mossland-codec/eval_benchmark/baselines/separation_baseline.py --model htdemucs_ft` 跑 `musdb18hq_test_10s_nonsilent_frame_manifest.jsonl`，输出 `runs/baselines_musdb18hq_10s_nonsilent_htdemucs_ft/htdemucs_ft_manifest.jsonl` 100 行。CLAP/VGGish/museval 已完成并写入报告：vocals FAD-CLAP `0.212945`、FAD-VGGish `0.940712`、SI-SDR `-4.05274`、museval SDR/SIR/SAR `5.92364`/`15.4301`/`8.98561`；accompaniment FAD-CLAP `0.0561765`、FAD-VGGish `0.291066`、SI-SDR `16.7488`、museval SDR/SIR/SAR `16.8394`/`26.8219`/`18.4925`。
- 2026-06-14 05:56-06:00 UTC 为利用空闲 PPU/GPU，已新挂多路仅推理后台任务，暂不启动新的 FAD/ViSQOL 指标以免抢 CPU：DiffStereo official sampler seed0 100 条 mono-to-stereo 输出到 `runs/baselines_musiccaps_stereo100_official/`、日志 `logs/diffstereo_stereo100_official.log`；A2SB 300 条 16 kHz low-band -> 44.1 kHz BWE 输出到 `runs/baselines_musiccaps_sr300_16000_a2sb/`、日志 `logs/a2sb_sr300_16000.log`；FastWave 300 条 16/24 kHz 输出到 `runs/baselines_musiccaps_sr300_{16000,24000}_fastwave/`、日志 `logs/fastwave_sr300_{16000,24000}.log`；NU-Wave2 300 条 16/24 kHz 输出到 `runs/baselines_musiccaps_sr300_{16000,24000}_nuwave2/`、日志 `logs/nuwave2_sr300_{16000,24000}.log`；FlowHigh 300 条 16/24 kHz 输出到 `runs/baselines_musiccaps_sr300_{16000,24000}_flowhigh/`、日志 `logs/flowhigh_sr300_{16000,24000}.log`。FastWave/NU-Wave2/FlowHigh 已看到 processed 进度；A2SB 官方子进程在 PPU-SMI 中显示占用 PPU 0，可能未按预期映射到物理 PPU 1，但仍在运行，不要贸然杀。
- 2026-06-14 06:04 UTC FlowHigh 300 条 16/24 kHz 推理已完成，`eval_none` 距离指标已完成：16k SI-SDR `14.7359`、SNR `5.24391`、LSD `43.2559`、LSD-LF `9.49549`、LSD-HF `53.5808`、MRSTFT `0.972326`；24k SI-SDR `15.1805`、SNR `5.39269`、LSD `42.5037`、LSD-LF `22.257`、LSD-HF `57.8109`、MRSTFT `0.966113`。已继续挂 FlowHigh 300 条 16/24 kHz `eval_clap` 到 `runs/baselines_musiccaps_sr300_{16000,24000}_flowhigh/eval_clap/`，日志 `logs/flowhigh_sr300_{16000,24000}_eval_clap.log`。
- 2026-06-14 06:06 UTC FlowHigh 300 条 16/24 kHz `eval_clap` 已完成：16k FAD-CLAP `0.0974628`，24k FAD-CLAP `0.092567`。已新增 `hf_musiccaps_sr32000_manifest_300.jsonl` 并挂 FastWave 300 条 32 kHz 到 `runs/baselines_musiccaps_sr300_32000_fastwave/`、日志 `logs/fastwave_sr300_32000.log`，FlowHigh 300 条 32 kHz 到 `runs/baselines_musiccaps_sr300_32000_flowhigh/`、日志 `logs/flowhigh_sr300_32000.log`。
- 2026-06-14 06:07 UTC 已继续挂 NU-Wave2 300 条 32 kHz 到 `runs/baselines_musiccaps_sr300_32000_nuwave2/`、日志 `logs/nuwave2_sr300_32000.log`。至此 FastWave/FlowHigh/NU-Wave2 的 300 条 16/24/32 kHz 推理任务均已覆盖；完成后优先跑 `fad-backend none` 和 CLAP，VGGish/ViSQOL 等 CPU-heavy 指标等 full codec ViSQOL 结束后再补。
- 2026-06-14 06:08 UTC FlowHigh 300 条 32 kHz 推理已完成，已挂 `eval_clap` 到 `runs/baselines_musiccaps_sr300_32000_flowhigh/eval_clap/`，日志 `logs/flowhigh_sr300_32000_eval_clap.log`。
- 2026-06-14 06:09 UTC FlowHigh 300 条 32 kHz `eval_clap` 已完成：FAD-CLAP `0.0910487`、SI-SDR `15.0304`、SNR `5.24973`、LSD `42.927`、LSD-LF `31.6377`、LSD-HF `63.6612`、MRSTFT `0.976736`。已生成 `hf_musiccaps_stereo_manifest_100_seed1.jsonl` 并挂 DiffStereo official sampler seed1 100 条到 `runs/baselines_musiccaps_stereo100_seed1_official/`、日志 `logs/diffstereo_stereo100_seed1_official.log`。
- 2026-06-14 06:12 UTC NU-Wave2 300 条 24 kHz `eval_clap` 已完成：FAD-CLAP `0.0904413`、SI-SDR `17.4865`、SNR `5.73583`、LSD `38.081`、LSD-LF `15.9323`、LSD-HF `53.2899`、MRSTFT `0.934664`。已挂 AERO 12 kHz -> 48 kHz hl=256 100 条变体到 `runs/baselines_musiccaps_sr100_12000_aero_hl256/`，日志 `logs/aero_sr100_12000_hl256.log`；这是同一 12-48 checkpoint family 的补充工程评测。
- 2026-06-14 06:13 UTC AERO hl=256 100 条推理完成，NU-Wave2 300 条 16 kHz 推理完成。已挂 NU-Wave2 16 kHz `eval_clap` 到 `runs/baselines_musiccaps_sr300_16000_nuwave2/eval_clap/`、日志 `logs/nuwave2_sr300_16000_eval_clap.log`；AERO hl=256 `eval_clap` 到 `runs/baselines_musiccaps_sr100_12000_aero_hl256/eval_clap/`、日志 `logs/aero_sr100_12000_hl256_eval_clap.log`。
- 2026-06-14 06:14 UTC NU-Wave2 300 条 16 kHz `eval_clap` 已完成：FAD-CLAP `0.115517`、SI-SDR `16.2993`、SNR `5.49372`、LSD `41.3222`、LSD-LF `9.02383`、LSD-HF `51.135`、MRSTFT `0.958072`。AERO hl=256 100 条 `eval_clap` 已完成：FAD-CLAP `0.221728`、SI-SDR `10.3779`、SNR `10.9615`、LSD `40.0532`、LSD-LF `10.4855`、LSD-HF `46.466`、MRSTFT `0.338673`。FastWave 300 条 16 kHz 推理完成，已挂 `eval_clap` 到 `runs/baselines_musiccaps_sr300_16000_fastwave/eval_clap/`、日志 `logs/fastwave_sr300_16000_eval_clap.log`。
- 2026-06-14 06:16 UTC FastWave 300 条 16/24 kHz `eval_clap` 已完成并写入报告 300 条扩展表：16k FAD-CLAP `0.130382`、SI-SDR `15.5690`、SNR `5.29748`、LSD `47.2015`、LSD-LF `10.4046`、LSD-HF `57.1456`、MRSTFT `0.983728`；24k FAD-CLAP `0.142833`、SI-SDR `15.9226`、SNR `5.39215`、LSD `46.7266`、LSD-LF `20.5868`、LSD-HF `62.4462`、MRSTFT `0.974695`。
- 2026-06-14 06:17 UTC 已启动 AudioSR basic 300 条 16/24/32 kHz 推理到 `runs/baselines_musiccaps_sr300_{16000,24000,32000}_audiosr/`，日志 `logs/audiosr_sr300_{16000,24000,32000}.log`，使用官方 repo `tmp/eval_baseline_refs/sr/versatile_audio_super_resolution`、`ddim_steps=50`、`guidance_scale=3.5`。
- 2026-06-14 06:24 UTC NU-Wave2 300 条 32 kHz `eval_clap` 已完成并写入报告 300 条扩展表：FAD-CLAP `0.110399`、SI-SDR `17.9776`、SNR `5.81245`、LSD `35.4909`、LSD-LF `21.8000`、LSD-HF `57.3792`、MRSTFT `0.921167`。FastWave 300 条 32 kHz `eval_clap` 也已完成并写入报告：FAD-CLAP `0.188266`、SI-SDR `16.0120`、SNR `5.40189`、LSD `45.9800`、LSD-LF `28.0167`、LSD-HF `68.7630`、MRSTFT `0.969998`。当前扩大任务还在跑：AudioSR 16/24/32 kHz 300 条推理、DiffStereo seed0/seed1 100 条、A2SB 300 条；VGGish/ViSQOL 等 CPU-heavy 指标等 full codec ViSQOL 完成后再补。
- 2026-06-14 06:31 UTC Mossland `ckpt/mossland-codec0613` MusicCaps-HF SR 300 条 16/24/32 kHz `eval_clap` 已完成并写入报告 300 条扩展表：16k FAD-CLAP `0.389379`、SI-SDR `-13.0506`、SNR `-1.04210`、LSD `27.0823`、LSD-LF `11.0471`、LSD-HF `32.7578`、MRSTFT mean/median `340.202/0.537197`；24k FAD-CLAP `0.399308`、SI-SDR `-13.0788`、SNR `-1.08349`、LSD `29.4036`、LSD-LF `19.9805`、LSD-HF `37.5154`、MRSTFT `340.231/0.557360`；32k FAD-CLAP `0.396576`、SI-SDR `-13.0902`、SNR `-1.07464`、LSD `29.2560`、LSD-LF `25.7763`、LSD-HF `36.3179`、MRSTFT `340.229/0.556367`。已挂 HTDemucs-FT full-track 到 `runs/baselines_musdb18hq_fulltrack_htdemucs_ft/`，日志 `logs/htdemucs_ft_fulltrack.log`，完成后需跑 `museval_eval.py` 并写入 full-track diagnostics。
- 2026-06-14 06:34 UTC 已挂官方 Demucs `mdx` full-track 到 `runs/baselines_musdb18hq_fulltrack_demucs_mdx/`，日志 `logs/demucs_mdx_fulltrack.log`。`mdx` 是 Demucs 官方 MUSDB-HQ/MDX challenge 相关强 baseline；完成后需跑 `museval_eval.py`，并在报告中与 `htdemucs`、`htdemucs_ft`、Open-Unmix、ByteSep full-track diagnostics 分开标注协议。
- 2026-06-14 06:44 UTC AudioSR basic 300 条 16/24/32 kHz `eval_clap` 已完成并写入报告 300 条扩展表：16k FAD-CLAP `0.0988956`、SI-SDR `11.0552`、SNR `5.19934`、LSD `31.0045`、LSD-LF `12.0428`、LSD-HF `37.5498`、MRSTFT `0.682118`；24k FAD-CLAP `0.0916977`、SI-SDR `11.2467`、SNR `5.26633`、LSD `31.3427`、LSD-LF `22.3917`、LSD-HF `39.0024`、MRSTFT `0.681901`；32k FAD-CLAP `0.0915464`、SI-SDR `11.2810`、SNR `5.28213`、LSD `31.2945`、LSD-LF `27.9154`、LSD-HF `37.8979`、MRSTFT `0.680452`。SemantiCodec adapter `baselines/semanticodec_baseline.py` 已新增并通过 1 条 MusicCaps-HF smoke，官方 repo 位于 `tmp/eval_baseline_refs/semanticodec/SemantiCodec-inference` commit `7c05d42`，权重缓存到 `checkpoints/semanticodec`；当前已挂 SemantiCodec token-rate 25/50/100 的 100 条 reconstruction 到 `runs/baselines_musiccaps_semanticodec100_tr{25,50,100}/`。已安装 `vector-quantize-pytorch` 及其依赖以支持官方 SemantiCodec。
- 2026-06-14 06:44 UTC 已挂 EnCodec 48 kHz rate-quality 补充：3 kbps 到 `runs/baselines_musiccaps_full_encodec_bw3/`、6 kbps 到 `runs/baselines_musiccaps_full_encodec_bw6/`，日志 `logs/encodec_musiccaps_full_bw{3,6}.log`。12 kbps 尚未挂，等 PPU 空闲后补。已派 worker 实现 Ambisonizer mono-to-stereo adapter，写入范围限定 `baselines/ambisonizer_baseline.py`。
- 2026-06-14 已生成 MUSDB18-HQ full-track manifest 并完成 Mossland、HTDemucs、Open-Unmix、ByteSep ResUNet143 full-track estimates。HTDemucs、Open-Unmix、ByteSep full-track `museval_eval.py --keep-going` 均已 50/50 tracks 有效完成：HTDemucs vocals/accompaniment SDR mean `6.02866`/`15.9517`，Open-Unmix `2.76104`/`14.0779`，ByteSep `5.96973`/`17.2533`。Mossland full-track museval 15/50 有效的根因已确认：full-track predictions 全部只有 `1.509297s`，而 references 为 `76.19s` 到 `430.38s`；旧 `museval_eval.py` 按 min length 截断后只评了歌曲开头 1.509 秒，35 首在此前缀内至少一个 reference stem 全零。不是数据坏或 reference 路径错。Mossland full-track 结果作废；要复现 SiSEC 口径需先实现 chunked full-track inference。`museval_eval.py` 现已新增 `--length-check auto|always|off`，默认 `auto` 会保护 `duration_seconds is None` 的 full-track rows，明显 prediction/reference 长度不匹配会报错或在 `--keep-going` 下写入 `errors.jsonl`，10 秒窗口评测保持默认可用。
- 2026-06-14 在兄弟工作区 `low-resource-dialects-quality-sources` 完成 TTS 小语种/方言 query sourcing 修正：CQE 现在优先生成目标语/方言 native query，translator 支持 `source_query_lang: native` + `translate_to_zh: false` 直接导出 native crawl URLs，报告 `tts_low_resource_dialect_data_expansion.md` 已更新。已跑 examples/version_1 单测、translator py_compile、TTS tree 校验/单测、YAML 解析和 prompt 旧规则扫描；未执行真实 API/crawl。
- Mossland 代理基础设施已从 `moss-training-framework` 的通用规则迁移而来。
- 仓库文档和 agent harness 面向人阅读的输出已统一改为中文。
- `scripts/mossland-codec` 第一版已改为自包含多任务 codec，实现 `MosslandCodecTransformer`、训练 wrapper、任务数据适配层、Hydra 配置、separation 预处理和 prepared separation dataset。
- `scripts/mossland-codec` 已移除 `hparams.py` 和 `hparams_inference.py`；模型、音频表示、噪声调度和推理默认项通过 `MosslandCodecTransformer(...)`、Hydra `model:` 或通过 `scripts.factory.load_model()` 从 `config.yaml/checkpoint.ckpt` 读取。
- 已完成 `scripts/mossland-codec/tasks.py` 5 个任务的评估指标调研，报告在 `docs/evaluation-metrics.md`，论文 PDF 在 `docs/papers/`；`mono_to_stereo` 按生成式 stereo rendering/upmix 任务处理，不把某个双声道 reference 当唯一真值。
- 2026-06-13 新增 `scripts/mossland-codec/eval_benchmark/` 初版评估 pipeline：manifest 固定样本、可选通过 `scripts.factory.load_model(ckpt_dir=...)` 加载 `ckpt/mossland-codec0613` 并调用 `model.generate_waveform(src, task_id=...)` 生成预测、计算 SNR/SI-SDR/LSD/MRSTFT、super-resolution LF/HF LSD 与高频能量、mono-to-stereo fold-down/width/correlation、当前 `fad_mel_proxy`，输出 `results.jsonl` 和 `summary.json`。`--device` 控制模型推理设备，`--metrics-device` 控制 STFT/FAD proxy 指标设备，默认有 CUDA 时指标走 GPU。新增测试 `tests/mossland_codec/test_eval_benchmark.py`。
- 2026-06-13 已把 CoDiCodec 论文 PDF 加入 `docs/papers/codicodec.pdf`，并拉取官方仓库到 `tmp/codicodec` 参考。论文在 MusicCaps 上报告 SI-SDR、ViSQOL、VGGish FAD 和 CLAP FAD/FAD_clap；官方仓库当前只包含 codec 推理代码，没有评估脚本。当前 eval pipeline 的 `fad_mel_proxy` 只用于跑通流程和回归测试，不能当 CoDiCodec 论文 FAD 复现；后续需接入独立 VGGish/CLAP 后端。
- 2026-06-13 继续完善 eval pipeline：新增 `build_manifest.py` 从 prepared stems 生成五任务 manifest；新增 `fad_backends.py`，支持 `--fad-backend mel_proxy|clap|none`，其中 `clap` 使用 Hugging Face `laion/clap-htsat-unfused` 并缓存到 `checkpoints/clap`，用于 CoDiCodec 风格 `FAD_clap`。已安装 `hydra-core` 并验证 `scripts.factory.load_model(ckpt_dir='ckpt/mossland-codec0613')` 能加载 `MosslandCodecTransformer(sample_rate=44100,num_latents=128)`。已用 `tmp/34078087` 生成 1 秒五任务 manifest，并在 GPU 上跑通 `ckpt/mossland-codec0613` 五任务 smoke，输出在 `scripts/mossland-codec/eval_benchmark/runs/smoke_tmp_34078087/`。CLAP checkpoint 实际下载仍被当前 `HF_ENDPOINT`/proxy SSL 阻断，`fad_clap` 尚未完成权重 smoke。
- 2026-06-13 按用户提醒绕过代理后，CLAP checkpoint 已可从 Hugging Face 官方站点下载：运行时需清掉 `HTTP_PROXY/HTTPS_PROXY/ALL_PROXY` 并设置 `HF_ENDPOINT=https://huggingface.co`。`laion/clap-htsat-unfused` 已缓存到 `checkpoints/clap`，随机音频 CLAP 512 维 embedding smoke 通过；`scripts/mossland-codec/eval_benchmark/runs/smoke_tmp_34078087_clap/` 已跑通五任务 `--fad-backend clap`，但每个 bucket 只有 1 条样本，所以 `fad_clap` 汇总为 NaN 是预期。
- 2026-06-13 `museval/mir_eval` 已绕过代理安装。`museval` 在当前 Python 3.12 仍因 `musdb/stempeg/future` 引用已移除 `imp` 导入失败，不能当 SiSEC 2018 BSS Eval v4 复现；`metrics.py` 先接入 `mir_eval_bss_images_sdr_db/isr_db/sar_db` 作为 separation teacher-stem diagnostic。`run.py` 现在同时输出 `summary.json/csv/md`。`scripts/mossland-codec/eval_benchmark/runs/smoke_tmp_34078087_bss/summary.md` 已包含 BSS diagnostic 表格。
- 2026-06-13 已修复 `datasets/pyarrow` 并下载开源 `google/MusicCaps` metadata 到 `scripts/mossland-codec/eval_benchmark/data/musiccaps/musiccaps_train_metadata.jsonl`（5521 条）。新增 `build_musiccaps_manifest.py`：给定本地按 `ytid` 命名的音频目录后生成 CoDiCodec-style reconstruction manifest；当前本地未抓 YouTube 音频，`musiccaps_reconstruct_manifest.jsonl` 为 0 行，`musiccaps_missing_audio.jsonl` 记录 5521 条缺失。
- 2026-06-13 新增 `download_musiccaps_audio.py`：用 `yt-dlp` 下载 MusicCaps YouTube 音频并用 `ffmpeg` 按 `start_s/end_s` 裁成 `{ytid}.wav`，支持 `--use-env-proxy`、`--cookies`、`--cookies-from-browser`、`--timeout-seconds`。实测直连 YouTube 为 `Network is unreachable`；使用当前代理能访问 YouTube 但会触发 “Sign in to confirm you’re not a bot”，因此需要 YouTube cookies 或内部 MusicCaps 音频镜像才能实际下载音频。
- 2026-06-13 eval pipeline 继续优化：`download_public_dataset.py` 支持 `--no-proxy`、`--head`、`.part` 断点、registry 文件大小校验和 `--progress-seconds`；`run.py` 默认复用确定性输出路径下已有的非空 prediction wav，传 `--overwrite-predictions` 才重算，便于 GPU 长评估中断恢复。`scripts/mossland-codec/eval_benchmark/data/` 和 `runs/` 已加入 `.gitignore`。
- 2026-06-13 正在并发下载公开 separation 数据：MUSDB18-HQ 后台进程 `python -m scripts.mossland-codec.eval_benchmark.download_public_dataset --dataset musdb18hq --no-proxy` 仍在跑，目标 `scripts/mossland-codec/eval_benchmark/data/musdb18hq/musdb18hq.zip.part`，期望完成文件大小 `22656664047`，2026-06-13 23:18 UTC 约 18.1GB；MUSDB18 compressed 后台进程 `--dataset musdb18` 仍在跑，目标 `scripts/mossland-codec/eval_benchmark/data/musdb18/musdb18.zip.part`，期望完成大小 `4684228845`，同一时间约 3.8GB。如果进程中断，直接重跑对应命令会复用 `.part`。
- 2026-06-13 新增 `download_musiccaps_hf_audio.py`：已从第三方 Hugging Face 镜像 `mahendra0203/musiccaps_processed_full` 下载 5355 个 `{youtube_id}.wav` 到 `scripts/mossland-codec/eval_benchmark/data/musiccaps/hf_audio/`，构建 `hf_musiccaps_reconstruct_manifest.jsonl` 5355 行，`hf_musiccaps_missing.jsonl` 166 行。该镜像不是 Google 官方音频发布，正式报告必须标注来源并确认许可。
- 2026-06-13 新增 `scripts/mossland-codec/eval_benchmark/baselines/` baseline adapter 约定；`common.py` 负责 baseline prediction path 和输出 manifest，`encodec_baseline.py` 已接入 Facebook Research EnCodec 官方 48k model，`dac_baseline.py` 已接入官方 Descript DAC，`sr_baseline.py` 已接入官方 AudioSR，`diffstereo_baseline.py` 已接入官方 DiffStereo。EnCodec/DAC/Mossland 已完成 10 条 MusicCaps-HF reconstruction smoke；AudioSR 与 DiffStereo 最初各完成 1 条 smoke，后续已分别扩到 SR@16000 10 条和 mono-to-stereo official sampler 5 条小评测。EnCodec 5355 条 MusicCaps-HF full run 正在 CPU 后台跑；DAC 5355 条 full run 已用 `setsid -f` 在 GPU 后台启动。
- 2026-06-13 已接入 `--fad-backend vggish`：使用 `tmp/eval_metric_refs/torchvggish` 的 PyTorch VGGish port 和 raw 128-D embedding，权重缓存到 `checkpoints/vggish/torch_hub/checkpoints/vggish-10086976.pth`。10 条 MusicCaps-HF smoke 已得到 `fad_vggish`：Mossland 7.99991、EnCodec 4.92134、DAC 2.40878；该后端报告时标为 `torchvggish_raw`。
- 2026-06-13 MUSDB18-HQ/MUSDB18 zip 均已下载且大小校验通过；MUSDB18-HQ 已解压到 `data/musdb18hq/{train,test}`，test split 有 50 首 wav stem。已生成 `data/musdb18hq_test_10s_manifest.jsonl` 100 行，并用 `ckpt/mossland-codec0613` 跑完 100 条 10 秒 separation eval；`runs/mossland_musdb18hq_test_10s_vggish/summary.md` 已补 `fad_vggish`，vocals 3.58069、accompaniment 4.69870。
- 2026-06-13 EnCodec full run 首次中断在 2041/5355，原因是官方 48k decoder 对末尾不足 full segment 的 overlap-add 报 shape mismatch；`encodec_baseline.py` 已加 segment-grid padding 并通过测试，full run 已用 `setsid -f` 续跑。DAC full run 仍在 GPU 后台跑。
- 2026-06-13 指标后端探测结论：Google Research 官方 FAD 代码可访问，但 TF1/Python2 风格且需要 TensorFlow/Beam/VGGish checkpoint，不适合直接塞当前 Python 3.12 Torch 主环境；ViSQOL 官方无 Linux 预编译 release，需要下载 Bazel 5.1.0 构建，后续适合外部 binary wrapper 接入。
- 2026-06-13 新增 `build_audio_task_manifest.py`：可从已有单音频/reconstruction manifest 派生 reconstruction、super-resolution bucket 和 mono-to-stereo 多 seed 样本。已从 MusicCaps-HF 前 10 条生成 `data/musiccaps/hf_musiccaps_multitask_manifest_10.jsonl` 60 行，并用 `ckpt/mossland-codec0613` 在 `cuda:1` 跑完 VGGish 指标，输出 `runs/mossland_musiccaps_multitask10_vggish/summary.md`。结果：reconstruct `fad_vggish=7.99986`；SR@16000/24000/32000 分别 `8.25946/8.26317/8.09181`；mono_to_stereo `6.77844`。
- 2026-06-13 为提高 baseline 并发，已终止单路 DAC full run，改为 4 个 disjoint shard 并行跑在 `cuda:0..3`：manifest 在 `data/musiccaps/dac_shards/hf_musiccaps_reconstruct_dac_shard{0..3}.jsonl`，日志 `logs/dac_musiccaps_full_shard{0..3}.log`，输出 manifest `runs/baselines_musiccaps_full/dac_manifest_shard{0..3}.jsonl`。另有 merge watcher `logs/dac_musiccaps_full_merge_watcher.log`，4 个 shard manifest 齐全后按原始顺序合并成 `dac_manifest.jsonl`。为避免 CPU 过饱和，EnCodec full 已用 `OMP_NUM_THREADS/MKL_NUM_THREADS/OPENBLAS_NUM_THREADS/NUMEXPR_NUM_THREADS=32` 续跑，DAC 每 shard 用同组 env 限为 8 线程。
- 2026-06-14 DAC MusicCaps-HF 5355 条 full 推理和指标已完成：`runs/baselines_musiccaps_full/dac_manifest.jsonl` 5355 行；`dac_eval_clap/summary.md` 得 `fad_clap=0.075158`、SI-SDR `9.72614`、SNR `4.72790`、LSD `22.4124`、MRSTFT `0.557487`；`dac_eval_vggish/summary.md` 得 `fad_vggish=0.713645`。该结果仍基于第三方 MusicCaps-HF 音频镜像。
- 2026-06-14 EnCodec MusicCaps-HF 5355 条 full 推理和指标已完成：`runs/baselines_musiccaps_full/encodec_manifest.jsonl` 5355 行；`encodec_eval_clap/summary.md` 得 `fad_clap=0.0408916`、SI-SDR `10.8863`、SNR `11.2612`、LSD `16.7002`、MRSTFT `0.282905`；`encodec_eval_vggish/summary.md` 得 `fad_vggish=0.981195`。该结果仍基于第三方 MusicCaps-HF 音频镜像。
- 2026-06-14 已新增 `visqol_eval.py`：读取带 `prediction_path` 的 manifest/results JSONL，把 reference/prediction 转成 48 kHz mono 16-bit WAV，调用本地 Google ViSQOL binary `tmp/eval_metric_refs/visqol/bazel-bin/visqol` 和 audio model `tmp/eval_metric_refs/visqol/model/libsvm_nu_svr_model.txt`，输出 `visqol_moslqo`。已用 5 个并行 bucket 跑完 Mossland MusicCaps-HF 100-source multitask ViSQOL：reconstruct `2.93772`、SR@16000 `3.28623`、SR@24000 `2.91487`、SR@32000 `2.91912`、mono_to_stereo `2.81490`。stereo 任务会被 ViSQOL downmix，只能看单声道音质。
- 2026-06-14 Mossland MusicCaps-HF 100-source multitask 表已写入 `docs/eval-benchmark-report.md`：VGGish/CLAP/ViSQOL 均已完成。该表仍使用第三方 `mahendra0203/musiccaps_processed_full` 音频镜像，不是 Google 官方 MusicCaps 音频发布。
- 2026-06-14 AudioSR baseline 已扩到 MusicCaps-HF SR@16000/24000/32000 各 100 条，官方 `haoheliu/audiosr_basic`、默认 50 DDIM steps、guidance 3.5；完整 100 条表见 `docs/eval-benchmark-report.md`。此前 10 条 SR@16000 小评测只保留为历史 smoke。
- 2026-06-14 DiffStereo baseline 已扩到 MusicCaps-HF mono-to-stereo seed0 前 20 条 official sampler：官方 `model_epoch_80000.pt`、24 kHz、9.1 秒 chunk、空 `timestep_respacing` 1000-step sampler。20 条结果：`fad_clap=0.403286`、`fad_vggish=9.54824`、ViSQOL `2.85661`、SI-SDR `0.773159`、SNR `0.575298`、LSD `31.3813`、MRSTFT `1.5132`、fold-down SI-SDR `4.85469`、width `0.45854`、corr `0.644269`。这仍不是 DiffStereo 论文完整 benchmark/MOS 复现。
- 2026-06-14 已新增 `museval_eval.py`：对已有 separation results 按 track 配对 vocals/accompaniment，局部安装 `imp.reload` shim 兼容 Python 3.12/future，然后调用 `museval.evaluate(..., mode='v4')`。`runs/mossland_musdb18hq_test_10s_museval/` 已跑 MUSDB18-HQ test 起点 0 的 10 秒工程评测：50 首里 15 首有效、35 首因某个 reference source 全静音被 BSS Eval v4 拒绝。有效结果：vocals SDR `-8.05049`、SIR `1.04050`、SAR `-12.4066`、ISR `-1.53980`；accompaniment SDR `-3.71643`、SIR `5.16531`、SAR `-4.44980`、ISR `-0.141063`。这不是 SiSEC full-track 口径，后续应选非静音窗口或跑 full-track estimates。
- 2026-06-14 用户明确最终报告口径：各任务需要 Mossland 与对应官方 baseline 的本地实测指标对比表；这些指标必须来自本仓库本地推理和统一 pipeline 评测，不能照搬论文数值。同时必须检查每个 baseline 的官方 checkpoint、数据集、采样率、推理参数和指标后端是否与论文协议对齐，保证 pipeline 有能力复现论文数值；不满足时标为工程评测或未复现。
- 2026-06-14 已更新 `build_musdb_manifest.py`：新增 `--select-non-silent --non-silent-frame-seconds`，为 MUSDB18-HQ 10 秒片段选择每个 1 秒 frame 内 vocals/accompaniment 都非静音的窗口，匹配 `museval` v4 的 all-zero source 约束。`data/musdb18hq_test_10s_nonsilent_frame_manifest.jsonl` 100 行覆盖 50 首 test track；Mossland `ckpt/mossland-codec0613` 已跑 `runs/mossland_musdb18hq_test_10s_nonsilent_frame_vggish/`，paired-source `museval` 已跑 `runs/mossland_musdb18hq_test_10s_nonsilent_frame_museval/`，50/50 tracks 有效，无 errors。结果：vocals museval SDR `-3.64439`、SIR `3.83420`、SAR `-7.08427`、ISR `0.189689`；accompaniment SDR `-2.14869`、SIR `9.57009`、SAR `-5.84080`、ISR `0.649102`。仍是 10 秒工程评测，不是 SiSEC full-track。
- 2026-06-14 AudioSR、FlowHigh、NU-Wave2 的 MusicCaps-HF SR@16000/24000/32000 各 100 条推理和 CLAP/VGGish/ViSQOL 指标均已完成；DiffStereo mono-to-stereo seed0 前 20 条 official sampler 指标也已完成。
- 2026-06-14 AERO/NU-Wave 状态：本地有官方仓库副本但缺官方 checkpoint 且依赖栈较老；AERO 当前 clone 只看到 `4->16 kHz` configs，NU-Wave 固定 x2 `24->48 kHz`。NU-Wave2 官方 48 kHz target checkpoint 已接入并完成 MusicCaps-HF 三个 SR bucket 的 100 条工程评测。
- 2026-06-13 已挂后台 full-metric watcher（`logs/baselines_musiccaps_full_eval_watcher.log`）：等待 `runs/baselines_musiccaps_full/encodec_manifest.jsonl` 和合并后的 `dac_manifest.jsonl` 生成后，自动分别运行 full `--fad-backend clap` 与 `--fad-backend vggish`，输出到 `runs/baselines_musiccaps_full/{encodec,dac}_eval_{clap,vggish}/`；为避免和 DAC shard 抢卡，full metric 使用 `cuda:4/cuda:5`，并用 `OMP/MKL/OPENBLAS/NUMEXPR=16` 限制 CPU 线程。
- 2026-06-12 审查并修正 `scripts/tools/local_queue`：旧 worker 在 lease 过期并被新 worker 抢占后不能再提交结果或记录失败；`write_manifest()` 不允许对已有 job 追加，避免重复 manifest。对应测试在 `tests/test_local_queue.py`。
- 2026-06-12 新增 `scripts/tools/oss_tools`，目录内保存 `bin/rclone`，本机 `rclone.conf` 放在同目录但被 `.gitignore` 忽略；封装 OSS 上传、下载、存在检查和删除，小文件实测耗时已写入 `OssClient` 注释。
- 2026-06-12 新增并更新 `scripts/data_processing/oss_prepare_separation.py` 和 `scripts/data_processing/separation_core.py`：`local_queue` handler 按 task 从 OSS 下载单条音频/视频，先用 `ffprobe` 预读时长、失败回退 `torchaudio.info()`，默认大于等于 600 秒直接上传 `metadata.json(status=skiplong, reason=max_duration_exceeded)`；短音频直接使用 data_processing core 分离，不再依赖 `scripts.data.prepare_separation.process_file()`，每个 worker 进程缓存一个 RoFormer separator，任务后清理临时 tensor/CUDA cache 但不释放模型；支持 `.webm/.mp4` 通过 `ffmpeg` 抽音频流，输出 `mixture/vocals/accompaniment` 固定 44100 Hz。
- 2026-06-12 新增 `scripts/data_processing/oss_prepare_separation_pipeline.py` 并让 `bash/prepare_youtubemusic_seperation/03_start_workers.sh` 默认使用它。普通 worker 单卡内串行下载/预读、GPU 推理、保存、上传；pipeline worker 每 GPU 进程默认 `prefetch=4/download_workers=2/upload_workers=2/max_pending_uploads=2`，提前下载/预读后续任务，并把 MP3 保存和 OSS 上传放入后台线程，让 GPU 主线程更连续地推理。设置 `USE_PIPELINE_WORKER=0` 可回退旧串行 worker。
- 2026-06-13 `bash/prepare_youtubemusic_seperation/` 保留旧目录名，但默认配置已切到 `DATASET_NAME=NETEASE_SPIDER`：输入 `qz_oss2:embodied-multimodality/public/Sonata/data/raw/NETEASE_SPIDER`，输出 `qz_oss2:embodied-multimodality/public/Sonata/data/source_seperation/NETEASE_SPIDER`，`RUN_DIR=tmp/prepare_netease_spider_seperation`，`JOB=netease_spider_source_seperation`。若以后复用跑 YouTube，需要显式覆盖 `DATASET_NAME/SOURCE_PREFIX/OUTPUT_PREFIX/DATES/RUN_DIR/JOB/CONFIG_HASH`。
- 2026-06-12 在 PPU 开发机上验证 `scripts/data_processing/oss_prepare_separation.py` 单条数据可跑通。`/inspire/sj-ssd3/project/embodied-multimodality/public/zhaoguojie/envs/main_env` 已升到 Python 3.12.3，并通过 `lib/python3.12/site-packages/ppu_system_site_packages.pth` 引用 `/usr/local/lib/python3.12/site-packages` 里的 PPU 版 `torch 2.7.0/torchaudio 2.7.0`；RoFormer 额外依赖 `librosa/beartype/rotary-embedding-torch/local-attention/hyper-connections` 已安装到 `main_env`。
- 2026-06-12 正确 OSS bucket 是 `qz_oss2:embodied-multimodality/public/...`。单条 smoke test 输入 `qz_oss2:embodied-multimodality/public/Sonata/data/raw/NETEASE_SPIDER/audio/20260520/115844.mp3`，输出 `qz_oss2:embodied-multimodality/public/zhaoguojie/Mossland/tests/oss_prepare_separation_single/audio/20260520/115844/`，队列 job `single_ppu_smoke_20260612_correct_bucket` 结果 `success=True`，OSS 上存在 `mixture.mp3/vocals.mp3/accompaniment.mp3/metadata.json(status=done)`。
- 2026-06-12 新增 `bash/push_oss_files.sh` 和 `bash/pull_oss_files.sh`，分别负责本地 `NETEASE_SPIDER` 上传到 OSS、从 OSS 拉回本地；两者使用 `scripts/tools/oss_tools/bin/rclone` 和本机 ignored `rclone.conf`。
- 2026-06-12 按用户要求删除 OSS 输出前缀 `qz_oss2:embodied-multimodality/public/Sonata/data/source_seperation/YouTube_Music`；删除前约 `414813` 个对象、`1.022 TiB`，删除后验证为 `0` 对象。

## 稳定决策

- 使用 `docs/` 作为持久项目记忆根目录。
- 在 Mossland 专属代码出现前，保持导入的 agent harness 通用。
- 除非内容已成为当前 Mossland 需求、决策或工作流，不复制训练框架的长篇文档。
- 面向人阅读的文档、agent harness 说明、hook 提醒、脚本输出和未来 harness 设定默认使用中文；命令名、路径、scope、事件名、JSON key 和代码标识保留英文接口。
- 完全由机器管理的 harness 代码集中在 `agent-code/`；`.codex/hooks.json` 仍留在 `.codex/` 作为 Codex 配置入口。
- `scripts/tools/local_queue` 的一致性边界是“任务可以重复执行，但只有当前 lease owner 能提交结果或记录失败”；manifest 一旦生成不可就地追加，处理逻辑或配置变化应新建 job。
- `scripts/tools/oss_tools/rclone.conf` 含本机 OSS 配置并被 `.gitignore` 忽略；不要把配置内容写入文档或日志。若迁移到新机器，复制本机 config 或设置 `OSS_TOOLS_RCLONE_CONFIG`。
- 当前 Mossland codec 训练包目录是 `scripts/mossland_codec/`；旧 hyphen 实现已归档为 `scripts/mossland_codec_old/`。不要再创建当前用途的 `scripts/mossland-codec/`。
- 当前模型入口统一命名为 `MosslandCodec`，Hydra target 是 `scripts.mossland_codec.models.MosslandCodec`；`MosslandCodecTransformer` 只属于旧实现历史记录。
- `scripts/codicodec/` 和 `scripts/configs/experiment/codicodec.yaml` 保留为独立参考实现；当前 `scripts/mossland_codec/` 不 import `scripts.codicodec`。
- `scripts/music2latent/` 现已按用户要求集成自包含 Music2Latent-style consistency autoencoder；不要再恢复“只抽 training_base、不集成完整 music2latent”的旧规则。该模块不使用官方全局 `hparams.py`，入口走 Hydra/Lightning。
- 不再新增或恢复旧 `scripts/mossland-codec/hparams.py`、`scripts/mossland-codec/hparams_inference.py`；当前新包参数进入 `MosslandCodec(...)` 构造签名或 Hydra `model:` 配置。
- `prepare_separation.py` 默认使用本地 `checkpoints/mel-band-roformer-vocal-model/`，包含 `config_vocals_mel_band_roformer.yaml` 和 `MelBandRoformer.ckpt`；`checkpoints/` 是本地大文件目录，不提交。代码不内置网络 URL 或 config 字典，`load_model_config(None)` 必须读取本地默认 config 文件。
- `prepare_separation.py` 参考 KimberleyJensen/Mel-Band-Roformer-Vocal-Model 官方 `inference.py`：模型只直接输出 `training.target_instrument=vocals`，`_select_vocals()` 只接受精确 key `vocals`；`accompaniment.mp3` 由 `mixture - vocals` 生成，不读取 `other/Instrumental` stem。
- `prepare_separation.py` 默认写 `mixture.mp3`、`vocals.mp3`、`accompaniment.mp3` 和 `metadata.json`；跳过逻辑只认三份 stem mp3 与 `metadata.json(status=done)`。不要再生成 wav stem。
- `prepare_separation.py` 的输出目录严格保留 `--source-root` 下的相对路径，例如 `NETEASE_SPIDER/audio/20260520/1001230.mp3` 输出到 `NETEASE_SPIDER_SEPERATION_NEW/audio/20260520/1001230/`，不要再使用 `audio__20260520__1001230` 扁平目录。
- 当前 PPU 开发机有 8 张 `PPU-ZW810`，`nvidia-smi` 实际显示 `PPU-SMI`，PyTorch 仍通过 `cuda` 设备接口访问；不要从 PyTorch 官方 CUDA 源覆盖 PPU 定制 `torch/torchaudio`。旧 RTX 4090 记忆只适用于之前 Notebook 环境。`NETEASE_SPIDER` 分解目标目录历史上是 `/inspire/qb-ilm2/project/embodied-multimodality/public/zhaoguojie/data/NETEASE_SPIDER_SEPERATION_NEW`，OSS 处理入口使用 `qz_oss2:embodied-multimodality/public/...`。
- `prepare_separation.py` 支持 `--devices` 单机多卡 worker；父进程会聚合 worker 写入的 `progress.jsonl`，终端显示总文件进度 `files done/skipped/skiplong/errors / total`，不再显示 `workers 0/N`。多机多卡不要在每台机器对同一 manifest 直接用 `--devices`，应每张 GPU 起一个独立进程并用全局 `--num-shards/--shard-id` 分片。4 台机器每台 8 卡的启动脚本已写在 `bash/prepare_seperation/node0.sh` 到 `node3.sh`。当前机器只有一张 RTX 4090，实际应用 `--device cuda:0` 单 worker；`--max-files N` 会扫描短路，适合本地小样本测试。
- `prepare_separation.py` 的 `process_files()` 不能在启动时对整份 manifest 全量预扫 pending/skipped。624 万级 manifest 会长时间停在 `files 0%`，worker 日志栈在 `filter_pending_files -> separation_status -> safe_stem_id`。当前实现按文件逐条检查终止状态，第一个 pending 文件立即进入时长检查和 RoFormer 推理，模型只在首个需要推理的文件前懒加载。
- `prepare_separation.py` 支持异步 MP3 保存：`--save-workers` 控制后台保存线程数，`--max-pending-writes` 控制最多缓存多少个待保存结果。当前单卡建议 `--save-workers 2 --max-pending-writes 4`，用于让 RoFormer 推理和上一首 MP3 写入重叠，缓解 GPU/CPU 都低的同步等待问题。
- `prepare_separation.py` 支持 `--chunk-batch-size` 控制 RoFormer 单次前向的 chunk 数；2026-06-08 本机 2 个 NETEASE_SPIDER 样本上 `2` 比默认 `1` 略慢，当前推荐保持 `--chunk-batch-size 1`，不要盲目调大。
- `prepare_separation.py` 写 `mixture.mp3` 时，如果源文件已经是 44100 Hz、2 声道 mp3，会直接复制源 mp3，避免原音频二次编码；不匹配时仍重编码 mixture。
- 2026-06-09 旧 `NETEASE_SPIDER_SEPERATION` 抽样 8 个 done 条目，每个只解码前 15 秒：源 MP3 与保存的 `mixture.mp3` 均为 44100 Hz 双声道，L/R 声道都不是完全相同或 `1e-7` 内近似相同；8/8 的 `mixture.mp3` 与源 MP3 字节完全相同，这是直接复制优化预期行为，不代表 L/R 被复制成同一声道。
- `prepare_separation.py` 默认只处理短于 10 分钟的音频：`--max-duration-seconds 600` 会在完整加载和 RoFormer 推理前探测时长，优先使用 `ffprobe`，再回退到 `torchaudio.info()`；超过或等于阈值的条目写 `metadata.json(status=skiplong, reason=max_duration_exceeded)` 和 `progress.jsonl(status=skiplong)`。传 `--max-duration-seconds 0` 可关闭限制。worker 重新扫到旧 `metadata(status=error)` 时会先按当前上限重判时长，超长则改写成 `skiplong` 并直接跳过，不进入 RoFormer。
- `prepare_separation.py` 多卡 launcher 会在 worker 处理 pending 文件前写 `progress.jsonl(status=started)`；若 worker native 崩溃（如 `rc=-11`），父进程按最后 `started` 文件写 `metadata.json(status=error, reason=worker_crash)` 并重启同一 shard。`--worker-restarts` 默认 100；重启后 `done/skiplong` 都是终止状态，旧 `error` 会先做时长重判，仍不超长才作为错误终止态跳过；除非 `--overwrite` 不会重复推理。
- `PreparedSeparationDataset` 使用 prepared root 下的 `index.list` 做持久索引；没有索引时用 `fast_scandir` 扫描 `audio/` 子树里的 `mixture.mp3`，筛出同目录包含 `vocals.mp3/accompaniment.mp3` 的条目并写入索引。它不再读取 `prepare_separation.py` 的 progress 日志。当前配置 `length=1000000` 固定逻辑 epoch 长度，真实读取时对当前 `item_dirs` 数量取余；wrapper 的 `index_data_every_step=null` 表示训练中不重建索引，改成正整数后 `MosslandCodecTrainingWrapper.on_train_batch_end()` 会按 `trainer.global_step` 调用底层 dataset 的 `rebuild_index()`，该方法会重新扫描并直接替换 `self.item_dirs`。当前配置 `max_duration_seconds=300`，dataset 只在选中单条 item 准备读取前用 `ffprobe` 探测 `mixture.mp3` 时长，若大于等于 5 分钟则从当前 worker 的 item list 跳过，不在训练启动时全量 probe index。
- `PreparedSeparationDataset.crops_per_file` 会一次加载同一个 prepared item 的 mp3 stem 后裁多个 crop；当前 `mossland-codec.yaml` 用 `crops_per_file=16`、`train_batch_size=1`，由 `MosslandTaskBatch.from_payload()` 把 `[1, 16, C, T]` 展平成有效 batch 16，减少每 step mp3 读取数。
- `MosslandTaskDataset` 会先抽样任务，再调用 dataset 的任务感知读取接口，让 `PreparedSeparationDataset` 按任务解码：单任务路径非分离任务只读 `mixture.mp3`，分离任务只额外读对应目标 stem；多 crop 路径会对每个 crop 独立随机任务，并通过 `get_item_for_tasks(index, task_ids)` 只解码任务列表需要的 stem 并集。分离任务只接受显式 `mixture` 和目标 stem，不再做 `mix`、stem 求和等兜底；旧的包装式 separation stem dataset 已删除。
- 多卡训练不要把 DataLoader 预取开太大。旧 `num_workers=10,prefetch_factor=4` 在 8 卡、`train_batch_size=4` 时首批前会排队约 1280 个 prepared items、三千级 mp3 解码请求，容易让共享存储长时间卡在 `Epoch 0 0/N`；当前主配置是 `num_workers=6,prefetch_factor=2`。
- `mossland-codec.yaml` 当前模型容量约 494M 参数：`dim=768`、`num_layers=22`、`num_layers_encoder=22`、`cond_channels=768`、`frontend_multipliers_list=[1,2,4,12]`。压缩行为相关 `hop=1024`、`fac=2`、`spec_length=32`、`num_latents=128`、`fsq_levels=[11,11,11,11]` 和 `frontend_freq_downsample_list=[0,1,0]` 保持不变。
- `scripts/mossland-codec/transformer_layers.py` 当前 attention 使用 PyTorch SDPA，并在 CUDA bf16/fp16 无显式 tensor mask 时用 `sdpa_kernel(SDPBackend.FLASH_ATTENTION)` 强制 flash backend。decoder 的 block causal mask 不再构造大 float mask，而是用两段无 mask attention 保持语义并避免 flash 退化。
- `MosslandTaskDataset` 是唯一的训练任务抽样和任务 batch 构造入口；`MosslandTaskBatch` 负责标准 payload 的 `src/target/task_id` 三字段 `to_payload/from_payload`，`MosslandCodecTrainingWrapper` 只消费这个 payload，不再维护 `active_tasks/task_weights/low_sample_rate`，也不再 fallback 构造任务。
- 当前 5 个 Mossland 任务 ID 是 `reconstruct`、`separate_vocals`、`separate_accompaniment`、`super_resolution`、`mono_to_stereo`；旧 `reconstruct_music` 和 `music_bandwidth_extension` 不再作为任务名使用。
- 当前 `MosslandCodecTrainingWrapper` 负责把 `MosslandTaskBatch` 的 `src/target/task_id` 转为 representation、RVQ latent 和 consistency loss；demo callback 直接调用当前模型或 EMA 模型，并保存 `src -> target -> discrete8/16/32 -> continuous`。
- demo callback 必须在 demo 生成前后清理 CUDA cache，避免 demo 显存与训练显存重叠导致 OOM；每个 demo 样本会同时保存 `quantized` 与 `continuous` 两份 `src/target/generated` 对比音频，用于判断 FSQ quantized path 与 continuous path 的差异。
- `MosslandCodecTrainingWrapper.random_mix_prob` 只允许 reconstruction-only batch 使用；检测到 separation、mono/stereo 或 `super_resolution` 等非 reconstruct 任务会报错，避免破坏 `src/target` 对应关系。主多任务 `mossland-codec.yaml` 仍不配置 random mix；`codicodec-paper-repro.yaml` 为论文风格 reconstruction 配置启用 `random_mix_prob=0.5`。
- `super_resolution` 的 `low_sample_rate` 支持单个 int、显式 rate 列表或 `[min, max]` 范围；当前 `mossland-codec.yaml` 配置为 `[8000, 40000]`，会从区间内固定 bucket（8000、11025、12000、16000、22050、24000、32000、40000）采样，不采任意整数。audio super-resolution 降质用缓存的 `torchaudio.transforms.Resample` 做降采样再升采样，保留 sinc lowpass/anti-alias 行为；长度对齐只裁剪或重复尾部 sample，避免整段插值阻塞 DataLoader。mono-to-stereo 也不再写 `channel_mode`，只通过 `task_id="mono_to_stereo"` 区分。
- `separate_accompaniment` 的 target 来自 RoFormer prepared `accompaniment.mp3` teacher stem，不是人工干净伴奏。2026-06-09 run `2026-06-09_16-52-30` 的 demo target 与 src 余弦相似度约 `0.998`，抽样 prepared 条目也常见 `accompaniment` 接近 `mixture`；这表示 teacher stem 可能有人声残留或分离失败，后续需要保存 demo sidecar 元数据并考虑 separation 数据质量过滤。
- `oss_prepare_separation.py` 上传的 `metadata.json` 会保留 worker 临时本地 `source_path`，并额外写 `source_oss_path` 指向远端输入；receipt 里也有 `input_audio/output_dir`。
- 2026-06-12 PPU 实测 `YouTube_Music` 100 条混合样本：60 `.mp3`、20 `.webm`、20 `.mp4`，8 worker 用 181 秒完成，状态 99 `done`、1 `skiplong`、0 failed；抽查 `.webm/.mp4` 输出三个 stem 都是 44100 Hz、2 声道。测试输入和队列配置在 `tmp/oss_prepare_youtube_music_100/`，测试输出在 OSS `qz_oss2:embodied-multimodality/public/zhaoguojie/Mossland/tests/oss_prepare_separation_youtube_music_100/`。

## 下一步

- 未来新增或修改文档、hook 提醒、脚本输出和 harness 设定时，默认继续使用中文。
- 验证入口使用 `agent-code/scripts/agent/check.sh <scope>`。
- Mossland codec 单机训练入口使用 `python scripts/train.py experiment=mossland-codec` 或 `bash/start_train.sh`；当前直接按文件路径运行 `scripts/train.py` 时需要把仓库根目录放进 `PYTHONPATH`。
- Mossland codec 多节点训练入口为 `bash/start_train_multinode.sh`：平台提供 `PET_MASTER_ADDR/PET_MASTER_PORT/PET_NPROC_PER_NODE/PET_NNODES/PET_NODE_RANK`，脚本映射到 Lightning 需要的 `MASTER_ADDR/MASTER_PORT/NODE_RANK`，并传 `trainer.devices=${PET_NPROC_PER_NODE}`、`trainer.num_nodes=${PET_NNODES}`；每个节点启动一个父进程，由 Lightning 本地派生 GPU 子进程。可用 `DRY_RUN=1` 只打印命令。
- 本机单卡连通性短跑命令和注意事项见 `docs/agent/commands.md` 的 “Mossland codec 训练短跑”。2026-06-09 已验证 1 step 可完成并写出 ckpt/demo。
- Mossland codec 架构和 separation 数据准备细节见 `docs/mossland-codec.md`。
- Mossland 多任务评估指标、论文来源和相关代码仓库见 `docs/evaluation-metrics.md`；论文索引见 `docs/papers/README.md`。
- 旧 Mossland eval pipeline 已随旧实现归档到 `scripts/mossland_codec_old/`；当前新 `scripts/mossland_codec/` 只保留训练和 debug 数据入口，尚未迁入旧 full eval pipeline。
- Mossland 评估复现阶段性报告见 `docs/eval-benchmark-report.md`，记录当前 MusicCaps-HF reconstruction full baseline、Mossland 100-source 多任务、AudioSR/FlowHigh/NU-Wave2 100 条 SR baseline、DiffStereo 20 条 official sampler 小评测、MUSDB18-HQ 10 秒 separation 和论文指标后端缺口。
- EnCodec/DAC MusicCaps-HF full reconstruction 已完成；Mossland MusicCaps-HF 100-source multitask 已完成 VGGish、CLAP 和 ViSQOL。后续若继续扩大，应优先确认官方 MusicCaps 音频来源，或明确标注第三方镜像工程评测。
- MUSDB18-HQ 100 条 10 秒 separation 表格已写入 `docs/eval-benchmark-report.md`，包含 `fad_vggish`、`mir_eval` diagnostic 和 paired-source `museval` v4 工程评测；后续若要 SiSEC 口径，应跑 full-track 或至少选择 vocals/accompaniment 都非静音的窗口。
- 旧 `scripts/mossland-codec/inference.py` / `EncoderDecoder` 属于 `scripts/mossland_codec_old/` 历史实现；当前新训练包还没有稳定推理 helper 文档化入口。
- `2026-06-09_09-00-36` 训练 run 的 `latent/std` 后期两档切换已在 `docs/memory/progress.md` 记录：更像 FSQ latent 饱和/极端码使用风险，不是已证实的 NaN 式训练崩溃；后续优先补 pre-FSQ 与 code usage 监控。

## 阻塞项

- 无。
